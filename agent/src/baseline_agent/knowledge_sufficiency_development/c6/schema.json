{
  "type": "object",
  "properties": {
    "sufficient": {"type": "boolean"},
    "evidence": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "chunk": {"type": "integer", "minimum": 1, "maximum": 5},
          "quote": {"type": "string", "pattern": "^[\\s\\S]{1,24}$"}
        },
        "required": ["chunk", "quote"],
        "additionalProperties": false
      }
    }
  },
  "required": ["sufficient", "evidence"],
  "additionalProperties": false
}
