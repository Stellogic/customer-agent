# #174 暂停交接与结构评估入口

## 停止节点

按用户要求，第14轮已结束，结果与失败保留，提交后停止。PR #214保持Draft，#174/#226保持OPEN；没有可自动重跑的授权目录。不要把“收尾完成”解释成“真实验收通过”。没有运行最终完整门禁，也不合并或关票。恢复需用户明确继续。

当前产品修复已推送：任务归属审计、空白流片段、发布错误分类、action v6当前动作/证据契约、固定确认按钮复用Spring确认、已发布回复不再从头纠正。Agent472 passed/3 skipped，Backend check通过；均为组件证据，不能替代最终完整门禁。最新运行受测HEAD见run14报告，后续文档提交不改变该历史HEAD。

最好的一轮是run12：前两场景通过、三项调查完成；第三场景未确认建单。run13被暂时性请求错误、回复schema及后续发布错误阻断。run14又回到重复扣款证据适用性拒绝。单次行动和回复诊断都曾通过，不能推断整条链路稳定。

## 优先检查的结构问题（候选，不是已批准重构方案）

1. **证据契约的归属。** Spring的EvidenceSufficiencyPolicy定义要求；Agent提示、当前请求契约和模型输出再次表达这些要求。run09/11/14均出现重复扣款适用性拒绝，v5/v6提示调整没有稳定消除。先补充“缺失的受控适用性类别／所属能力”的最小拒绝证据，再决定是否由能力结果携带统一的可选适用性元数据，减少两端规则重复。模型自主选择证据与Spring权威校验是当前契约，不能未经下一步设计决定就自动补满声明或删除校验。

2. **回复生成与发布的责任边界。** 目前适配器同时解析SSE、解析部分JSON、校验正文、调用发布回调、处理重试；graph另有纠正循环，Spring维护流状态。已复现过跨层异常归类和已发布片段后重发问题。优先评估一个明确的“尚未发布／已开始发布／结束”状态约定与错误返回，统一纠正资格，不先引入新框架。需要保留真实增量输出、知识路径授权前缓冲和安全正文约束。

3. **验收与诊断分层、费用证据。** 五场景失败即停保留，但每次应先验证出错的单个接缝，再进入完整发布验收；单次诊断通过仅代表该样本。受理usage未持久化使失败轮整笔预留无法结算，应优先补齐同一模型审计接口的最小usage记录。不能用预算预留当实扣，也不能因为模型调用HTTP200就判业务成功。

已发现的固定确认命令被模型重新理解问题已做最小修复，暂无证据支持为此再新增确认接口或整体改写受理模块。是否需要进一步重构，应从上述重复故障和具体责任边界出发，不以“验收失败次数多”直接推导全面重写。

## 预算与恢复约束

总授权19.14元；35笔PENDING完整保留，历史核对上界2元加后续预留15.1元为17.1元，保守未预留2.04元。不是平台账户余额或实际扣费。本次新增可用3.14元中预留了1.1元。暂停期间不消费余量。

恢复时先回读最新origin/main、PR、账本与权威门禁锁，保护共享工作树及baseline容器。不要重跑旧RunId，不回填缺失的原始响应；密钥、余额和原始载荷不得公开。先形成范围小、可验证的重构或诊断决定，再冻结新的验证运行。
