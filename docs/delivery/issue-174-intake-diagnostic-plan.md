# Issue #174 单次受理诊断冻结

## 第四阶段：正确回复路径的边界观察

第二轮完整验收在正确回答包裹问题后仍未推进，不能把第三阶段的旧回复结果直接用作根因。冻结 `issue174-intake-diagnostic-04`，最多初次受理及一次追加请求；追加内容与第2版验收一致，按初次返回且页面实际显示的固定问题选择包裹或重复扣款确认。其他问题或初次人工协助立即停止，不发送猜测回复，不创建工单。

保留已有五笔 PENDING 共 2.30 元与已结算 3.810222 元；新增预留 0.10 元，累计占用上界 6.210222 元，低于授权 8 元。调用后仍保留本轮 PENDING，不自动重跑、不改提示、模型或发布标准。

本轮区分三项假设：接口已转人工协助；接口仍澄清原问题；接口返回下一问题但页面未显示。沿用现有响应允许列表和数据库协助原因计数，增加 responseMessageVisible（在标题五秒观察及响应完成后，检查响应所对应固定文案是否可见）；它不是五秒延迟通过指标。未知文案不导出，布尔值为 null。诊断 passed 仅代表观察完成，不是发布验收通过。

入口：`pwsh ./scripts/issue174-live-acceptance.ps1 -ConfirmProviderSpend -IntakeDiagnostic -RunId issue174-intake-diagnostic-04`。以下前三阶段为历史冻结与结果，不覆盖。

用户在原累计 5 元预算之外明确追加 3 元，当前项目累计上限为 8 元。本诊断不代表发布验收重跑或通过，不变更五场景的既有通过标准。

- 运行标识：`issue174-intake-diagnostic-01`，只执行一次。
- 固定 `deepseek-v4-flash`、既有受理提示与 schema，不更换模型或改写提示。
- 复用 L174-01 的合成订单及两段原输入，最多发起两次受理请求；若首段未返回澄清状态则停止，不创建工单，不触发调查/审批/执行。
- 客户受理适配器单次 POST，无自动重试，输出上限 600 token。为两个受理请求预留 100000 micro-CNY（0.10 元）；不据此声称已获得实际 usage。
- 原已结算 3810222 micro-CNY 和旧轮 1000000 micro-CNY PENDING 全部保留；诊断开始后合计占用上界 4910222 micro-CNY，低于新上限 8000000 micro-CNY。
- 保留原 5000 ms 页面断言结果，额外观察同一请求至 30000 ms，只用于区分等待与结果差异，不将超过 5 秒计为原场景通过。
- 仅保存 HTTP 状态、耗时、允许列表中的受理状态/问题类别、问题数量和 5 秒内标题可见布尔值。不得保存 ID、URL、正文、完整响应、trace、页面快照或凭据。只读解析响应后丢弃未允许字段。
- 原始 Playwright 错误上下文仍留在临时容器中，不导出；专用 JSON 通过 `/diagnostics` 挂载直接保存。
- 诊断有调用后无论观察完成或失败均保留本轮预留 PENDING，不能重新使用；不得自动重跑。
- 所有 Compose 资源与镜像使用新 run ID，清理只针对本轮；运行遵守共享门禁锁。

入口：`pwsh ./scripts/issue174-live-acceptance.ps1 -ConfirmProviderSpend -IntakeDiagnostic -RunId issue174-intake-diagnostic-01`。

诊断完成后先评估证据再决定下一步；不能由诊断自动转入整套发布验收、合入或关票。

## 本次结果

测试提交 `a761826`，运行标识 `issue174-intake-diagnostic-01`。用例加载检查通过，诊断观察完成（Playwright 1 passed，12.0 秒）；这里的 passed 仅表示诊断程序执行完成，不是 L174-01 通过。

初次请求 HTTP 201，3989 ms，NEEDS_CLARIFICATION，issues 数量 0；追加确认请求 HTTP 201，2803 ms，仍为 NEEDS_CLARIFICATION，issues 数量 0。原 5 秒标题观察为 false。证据见 `issue-174-intake-diagnostic-result.json`。

这次观察排除了“只因第二次响应超过 5 秒”的解释，但不能反推旧轮的确切原因。状态未进入确认，也未形成任何拟建问题。Spring 的 `JdbcCustomerIntakeService` 在 Agent 不可用时也可创建人工协助受理并返回类似状态，因此 HTTP 201 不等于模型理解成功；两次 HTTP 操作也不等于两次供应商调用。当前诊断没有记录人工协助原因或供应商失败分类，不能进一步断言模型分类、契约校验或传输哪一环失败。

本次无后续调用。累计已结算仍为 3.810222 元，旧轮 1 元及本次 0.10 元均保留 PENDING，预算占用上界 4.910222 元，剩余未预留额度 3.089778 元。专属资源清理完成，锁回读 FREE。产品接缝的安全错误分类证据仍是下一步所需；本票不通过修改提示、阈值或业务逻辑来掩盖失败。

## 第二阶段：错误分类定向诊断

第一阶段没有观察受理协助原因，不能区分正常澄清与 Agent 不可用后的人工协助。第二阶段仅为补齐此判别，不更换模型、输入或提示，不是盲目重试以求通过。

冻结运行标识为 `issue174-intake-diagnostic-02`。仍最多两次请求；若首次响应的静态服务端文案表示已转人工协助，立即停止，不发送第二次。新增预留 0.10 元，已有两笔 PENDING 共 1.10 元完整保留；累计占用上界 5.010222 元，小于用户已授权 8 元。

在容器清理前，将 Agent 日志仅在内存中匹配固定的异常类别、四条固定错误消息和三位 HTTP 状态码；仅保存这些计数/布尔值/数值。数据库只读取 AGENT_UNAVAILABLE 协助数量和受理/回复记录数。不得导出原始日志、异常详情、原始响应、提示或请求标识。浏览器只增加与代码中固定人工协助文案比较所得的 assisted 布尔值，不保存文案本身。

入口沿用 -IntakeDiagnostic，但 RunId 改为 issue174-intake-diagnostic-02。旧运行标识不能重用，调用后保留新增 PENDING；本轮结果不作为原五场景通过证据。

第二阶段执行结果（提交 87d5d6b）：诊断观察完成，首次/追加请求分别为 4333/3305 ms，HTTP 201，NEEDS_CLARIFICATION，issues=0，assisted=false；5秒目标标题仍不可见。数据库 AGENT_UNAVAILABLE 协助数为0，受理1条、回复1条；白名单异常和固定错误消息均未命中。结果支持排除本轮人工协助退化和超5秒响应，不证明所有可能错误都不存在。下一判别点为模型选择的订单候选及待澄清问题状态，不应放宽发布验收断言。两份安全结果见 issue-174-intake-diagnostic-02-result.json 和 issue-174-intake-diagnostic-02-errors.json。此前两轮与本轮预留均保留，PENDING总计1.20元，累计占用上界5.010222元。

## 第三阶段：候选订单与澄清目标

冻结 run ID 为 issue174-intake-diagnostic-03，最多两次原受理请求，模型和输入不变。仅增加候选订单是否存在/是否匹配合成fixture的布尔值、四条固定服务端澄清文案对应的受控类别、数据库可见订单数量与待澄清问题枚举。仍不保存订单引用、文案、正文或原始响应。

该阶段用于区分“未识别订单”“澄清不同问题”“已有候选但未形成问题”，不把同一输入重复运行成功当成验收通过。既有三笔PENDING共1.20元保留，新增0.10元，预算占用上界5.110222元，小于8元。若此轮仍不能收敛，先回到实现规格与适配器验证，不继续付费观察同一流程。

第三阶段结果（9974bf2）：首次候选匹配fixture，澄清类别PACKAGE_NOT_RECEIVED；旧脚本固定回复重复扣款。追加后数据库出现1条AGENT_UNAVAILABLE，待澄清队列仍为PACKAGE_NOT_RECEIVED、DUPLICATE_CHARGE；日志白名单异常未命中。此证据与Spring只能处理队列头的校验相符，但不声称已捕获具体被拒绝的原始模型返回。

据#153逐项澄清规格修正验收交互，按页面提问回复、每类最多一次，并保持每轮响应+UI共用5秒截止、最终精确两类/两工单/共享记录要求。未改变产品代码或模型提示。确定性focused浏览器用例1 passed（8.5秒），专属资源清理成功。Standards/Spec复核均PASS。真实复验独立run/report02，旧诊断均保留。
