# #162 五分钟延迟自动解决

## 范围与依据

本票只替换 Agent 无补偿结论的即时解决路径，不改变人工手动解决和补偿执行权限。
规格来源为 GitHub #162 和 `docs/specs/issue-149.md` 第 38–45 条。

复用仓库已有 `@Scheduled`、PostgreSQL 持久化和 `TicketAuthorityLock`，不引入任务队列依赖。
Spring 的[调度说明](https://docs.spring.io/spring-framework/reference/integration/scheduling.html)
提供周期扫描机制，但进程内调度本身不保存业务期限；期限保存在数据库。
PostgreSQL [显式锁](https://www.postgresql.org/docs/17/explicit-locking.html)
用于串行化客户消息和到期决策。该组合沿用现有七十二小时关闭调度；五分钟是本项目规格，不是外部默认值。

## 业务边界

- `auto-resolution-v1` 是 Spring 白名单：权威状态解释、纯规则说明、已完成的非补偿性核对。
  白名单同时核对结论理由与风险场景，不因问题类型或模型评分单独授权。
  首版将无异常的零延迟物流核对归入已完成核对；退款/重复扣款仍转人工。
  所有允许分支都要求全部客户原文符合版本化的明确查询问句；无法明确区分的操作诉求转人工。
  已经由 Spring 校验并持久化的订单澄清确认码不作为新的业务诉求，但普通续回复仍重新检查。
- 完整公开回复与候选在同一事务持久化。候选关联回复 ID、结论证据、Agent 代次和最新客户消息序号；
  期限固定为持久化回复后五分钟，刷新、重登和重启不重置。
- 调度取得工单权威锁后重读最新工单、代次、客户消息版本、完整回复、证据来源/时效/冲突、
  最新订单事实、待办、补偿和人工偏好。已接受新消息先取消候选；先完成解决的并发消息须按既有重开路径处理。
- 订单事实、待办和补偿写入共享既有订单额度锁，避免到期读取与事实变更穿插。
  未通过重校验时停止自动解决并展示重新评估；客户可继续回复触发新调查。
- 客户取消联合当前候选截止时间与公开 Agent 代次仲裁，旧页面不能取消新候选。取消重试不重设期限。
- v2 客户快照/SSE 只公开状态和截止时间；v1 快照字段不变。前端归零只等待 Spring 结果，绝不自行变为已解决。
- 解决后继续使用既有 `TicketResolutionTransition` 的七十二小时关闭期限与重开语义。

## 验证状态

2026-08-30 已同步 `origin/main=4cfd0efd63416c78801c318edc858abeb92c1219`，
保留 #164 补偿公开投影与事件校验。协调已授予本票独占验证及完整交付窗口。

聚焦迭代中发现并修复了以下实际失败，没有将失败记录为通过：

- 首次旧 backend 空档在 E2E 未分类预检失败，RunId 未捕获，未进入编译。
- `issue162-backend-20260830-a1` 在伪造身份头负向测试登记检查失败，未进入编译。
- `issue162-format-backend-20260830-a2`：后端编译、Spotless、Checkstyle 通过，239 项测试中一项失败；
  原因是本票测试正文被辅助工厂构造成了补偿意图，已修正，保留禁止提前宣称解决的断言。
- `issue162-components-20260830-a3` 在本票验收脚本的 Ruff 注释标点检查失败，已修复。
- `issue162-components-20260830-a4`：Agent 类型检查与 233 项测试通过；前端编译通过，
  测试代码有一处未等待 `act` 的 lint 失败，已修复。

整票 Standards / Spec 静态审查在 `5db5a32` 均为 PASS；后续验证修复继续增量确认。
`scripts/test-auto-resolution.ps1` 接入真实 PostgreSQL、能力 HTTP 和 Spring 调度：
固定时钟推进、完整回复后候选、截止时刻已接受消息优先、到期失效矩阵、重启幂等与七十二小时关闭。
浏览器用例在 Serial 组覆盖桌面与窄屏公开状态、刷新恢复和取消。

`issue162-components-20260830-a6` 三组件聚焦 PASS：backend `check` 通过，Agent 类型与 233 项测试通过，
frontend 格式/lint/类型/构建通过，164 项测试通过、3 项既有跳过（本票5项全通过）。
`issue162-acceptance-20260830-a5` 真实持久化全部阶段 PASS；1440px/390px 两项浏览器验收 PASS。
各次结束后均一次确认锁 FREE 并通知协调；聚焦验收自有容器、卷、网络与镜像清理回读为空。

PR #204 仅转 Ready 一次，集中代码/安全审查均已完成。唯一有效 P2 指出历史终态补偿提案永久阻断后续查询。
已将提案阻断限定为既有活动状态 `PENDING_APPROVAL/APPROVED`，实际补偿与额度占用的阻断不变；
增加单个历史 `REJECTED` 提案的真实候选创建/到期回归，同时保留新增待审批提案的阻断案例。
`issue162-review-fix-20260830-a8` 因验收脚本编辑误插两行导致缩进错误而失败，静态审查和 Ruff 均定位，已删除。
修复后增量 Standards / Spec 均 PASS。`issue162-review-fix-20260830-a10` 后端/Agent 组件复验 PASS；
`issue162-acceptance-20260830-a9` 历史提案回归、原持久化矩阵与两项浏览器复验均 PASS，清理回读为空。

尚未运行最终完整门禁或合入。聚焦与静态 PASS 不代表正式交付；最终证据以 PR #204 的门禁记录为准。
