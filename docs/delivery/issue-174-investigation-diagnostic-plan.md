# #174 调查 schema 定向诊断冻结

RunId：`issue174-investigation-diagnostic-01`。用户已确认当前至少3.8元预算并授权继续。历史平台累计不足2元按用户提供的2元上界核对，原十一笔 PENDING 和全部估算记录不改。新预留1元，同时受当前可用3.8元与项目累计8元限制；调用后保留本轮预留，不自动重跑。

仅执行 #173 A 的原合成多问题受理与建单路径，最多初次、两次澄清和一次建单确认（4次受理操作），最多创建2张工单/2个调查代次。调查动作及供应商尝试每代最多8，token每代16000、时间120秒、费用10000 micro-USD；受理操作按最多4次估算。受理与行动合计最多20次，另保留每代业务判断最多3次、客户沟通最多2次，合计全链最多30次。token沿用整体已持久化上限200000；判断/沟通的usage和费用纳入既有聚合计数及本轮1元预留，受理usage不完整保留说明。模型固定 deepseek-v4-flash、受理 intake-v3、调查 investigation-action-v3，不改提示、schema或产品校验。

已知原六步限制不足七步正常路径，诊断使用产品默认八步。此处只观察第一场景建单后的 SCHEMA_MISMATCH 来源，不重跑五场景发布验收，也不将诊断 passed 当作业务调查成功。

诊断生成未跟踪的源码副本，仅将 `raise _DeepSeekActionResponseFailure(...)` 的构造改为一个转发函数：打印固定前缀、调用位置行号、现有枚举，再返回原异常。所有条件、输入、请求和抛出行为保留。副本只读挂载到本轮 agent-server；不修改产品源码、不合入插桩，也不应用到发布运行。没有在原函数前插入行，因此记录位置对应被测提交的原文件行号；多行调用记录起始调用行。

只在内存读取 Agent 日志，持久化正则允许的分类/行号与已有聚合指标；不保存原始日志、模型响应、prompt、工具结果、订单ID、客户正文、凭据或 trace。若未复现，只记本轮未观察到，不宣称历史 schema 失败已修复。

等待已产生的调查终态后提取分类；浏览器失败仍尝试保存观测，并停止。所有容器、卷、网络与镜像按本轮所有权清理。预算账本的新增记录独立标为诊断，后续运行必须重新核对新增消费，不可将其纳入旧核对点而漏计。

入口：`pwsh ./scripts/issue174-live-acceptance.ps1 -ConfirmProviderSpend -InvestigationDiagnostic -RunId issue174-investigation-diagnostic-01`。

## 运行结果

被测提交 `c905e28324f9e0094714d1b23caaa422afbed3e0`，观察完成、进程退出0。#173 A 受理/原子建单/刷新恢复 1 passed（12.0秒），随后两个调查代次均转人工。聚合指标为14次行动逻辑调用/供应商尝试、10049已知token、估算4773 micro-USD；业务判断与客户沟通未进入。两项失败均为 OUTPUT_TRUNCATED，见[脱敏结果](issue-174-investigation-diagnostic-01-result.json)。这不是发布通过。

当前行动适配器只有在供应商响应 incomplete_details.reason=max_output_tokens 时归类 OUTPUT_TRUNCATED；其默认单次输出为128 token，结论提交 schema 包含 evidence 数组及可能的 knowledgeQuery。因此本轮证明存在输出限额截断，不能将其当作原 SCHEMA_MISMATCH 的确切根因。14次调用与两条完整七步路径一致，但没有逐代次动作序列，不据此断言截断必然发生在第七次。

插桩行号列表为空，尽管原源码截断分支也使用该异常构造。这意味着本轮没有证明运行服务实际导入了挂载的插桩副本；不能将空列表解释为没有解析失败。保留此诊断局限，不伪造行号。后续若需要再次使用插桩，应先在无供应商调用的服务检查中确认导入模块来源和插桩生效，再决定是否调用。已有聚合 OUTPUT_TRUNCATED 证据独立有效，无需为取得重复错误分类继续重跑。

本轮独立预留1元保留，未将其记作平台实扣。它位于用户账单核对点之后，下一轮必须另计，不得被旧历史累计上界覆盖。没有删除旧预留或改写历史估算。资源清理后主机锁回读 FREE，未触发完整发布复验。

下一实施范围是调查输出额度与结构化结论的匹配：基于受控结论结构建立离线回归，区分简单动作选择与证据提交输出需求，保持现有证据与 Spring 校验、不换模型。具体额度及真实复验应在对应实施范围明确，不在 #174 纯验收票中悄悄改产品限制或将本轮认定为修复成功。
