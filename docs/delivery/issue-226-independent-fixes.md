# #226 确认、审计归属与回复发布独立交付

## 范围与基线

本次从 `origin/main@474e6068ca1f562a60d67518bba1619a1a3582ef` 开始，按 #226 当前有效验收独立提取产品修复。旧 PR #214 及 #174 的所有运行、失败和费用记录继续保留，不合并混合分支，也不关闭或恢复 #174。

已确认的问题是：并发判断和沟通通过共享审计列表的偏移读取记录，会混入别的调用；合法纯空白流片段被拒绝；Spring 发布回调的 HTTP 失败被归到供应商；已经尝试发布的回复仍从第 0 块纠正重发；客户点击明确确认后还可能被模型解释为继续澄清。

移植来源为 `3571f26`、`79c3acb`、`1658362`、`1f51c28` 中对应产品文件及回归，不整批移植提交中的真实验收脚本、冻结配置与运行报告。原作者与红绿证据保留在旧提交历史。

## 修复边界

- 审计保留全量记录，并按实际 asyncio 任务读取本次判断与沟通的记录。复用标准库 [current_task](https://docs.python.org/3.13/library/asyncio-task.html#asyncio.current_task) 和 [WeakKeyDictionary](https://docs.python.org/3.13/library/weakref.html#weakref.WeakKeyDictionary)，不增加依赖或长期持有已结束的任务。
- 接受非空的纯空白 delta，保留片段、累计正文上限和发布授权。
- 发布回调失败独立记录为 `PUBLIC_REPLY_PUBLISH_FAILED`；开始发布后停止从头纠正。尚未发布的路径保留既有有限纠正。
- 仅在普通受理 `READY_TO_CONFIRM` 且消息精确为 UI 命令“确认提交”时复用 Spring 的原确认事务；其他自然语言仍由模型理解。既有人工协助、过期、版本、幂等、重复问题及原子建单检查继续有效。

## 提示候选归属

本票不移植 action v5/v6。旧分支上的候选仍让模型逐次选择全部必读工具、填写必需证据关系，未稳定消除重复扣款的证据适用性拒绝。候选源码、回归与 `issue-226-evidence-applicability-candidate.md` 留在旧历史；其中显式说明当前允许动作的思路可由 #228/#229 按 #227 新职责重新采用，不能将未验证的候选宣称为已解决的真实故障。

## 验证记录

- 在新主线只移入旧回归、未移入产品修复时：Agent `3 failed, 466 passed, 3 skipped`。三项失败分别为并发审计串入、发布错误分类、发布后重复纠正；不是环境或配置失败。
- 移入修复后 Agent 格式、lint、类型检查通过，`469 passed, 3 skipped`；随后补充沟通并发专项如下。完整门禁以最终版本的交付记录为准，不由组件检查替代。
- 另补沟通并发流回归：旧 graph 将成功调用计为两次，修复后沟通模型测试 `33 passed`，全局两条审计均保留。新增测试的换行格式随后按检查结果统一。
- Spring 空白 delta 回归在旧实现上 `275 tests completed, 1 failed`，修复后 Backend 规范检查通过。
- 确认专项 `red01` 已在真实浏览器中复现 `CONFIRMED` 预期与 `READY_TO_CONFIRM` 实际结果的差异。首轮专项脚本清理漏掉 smoke profile，残留经本轮协调者按精确项目核对后清理；已修复脚本的 profile 和失败记录保留，不改写该轮失败。`green01` 静态检查通过，但起步阶段未到达待确认页面，运行失败且资源清理通过；尚不算产品绿灯。
- 专项脚本补充已有状态接口的有界连通检查、受理响应断言和持久证据目录后，`issue226-confirm-green02` 为 `1 passed (12.6s)`；自然语言两次实际调用受控 Agent，明确确认不再新增模型调用，原子创建两张工单且刷新恢复。Prettier、ESLint、全 TypeScript 检查通过，容器、卷、网络与本轮镜像回读清理通过，`paidCalls=0`。不据此追溯认定 green01 的具体原因。
- Standards 与 Spec 静态审查均为 PASS、0 项产品阻塞发现；最终运行结果仍需完成，静态审查不替代完整门禁。
- #173/#153/#155 等既有真实浏览器路径仍作为产品行为回归；受控外部响应的故障复现仅补充诊断，不能当成真实模型质量证明。

## 真实调用与后续

#226 不需要新增付费调用。用户本轮为 #227 核心路径验证授权新增总预算 3 元，不叠加旧剩余额度；由 #230 在实现完成后冻结样本、次数、尝试、token、成本、时间与停止条件，记录测试工单等预期副作用。未结算调用保守计入预留，不将程序估算当供应商结算。预算授权不恢复 #174，也不授权无限重跑。
