# 整批诊断与待审批回复要求

## 既有证据与预算

issue230-real-rejectionprobe01 在b780811上仅运行待审批样本1，失败、其余9未运行；已捕获工单审计 CUSTOMER_REPLY_CONTAINS_UNAPPROVED_PROMISE。4次调用、4245 token，全部结算，估算0.046737元，累计0.514785元。资源清理通过。用户随后明确将剩余预算调整为2.8元，因此后续以此为新增可用预算，不把2.8元当作历史累计上限；供应商实际账单仍未知。

## 待审批回复请求缺口

Spring要求两句精确状态说明，且其余正文不出现“补偿/退款”。当前模型提示只要求“必需措辞”，输入也未提供具体内容。已用真实适配器MockTransport请求测试复现Flash/Pro均遗漏两句要求。补齐为“补偿建议正在等待人工审批”和“审批完成前不会执行补偿或退款”，并明确其余正文限制；不修改Spring校验或业务资格，不自动修复模型输出。普通回复提示v4，受同一改动影响的知识回复审计提示v2，schema不变。

37项回复适配器测试通过。上一轮被拒正文没有留存，不能把这个已证实的请求缺口当作该次拒绝的唯一原因；旧失败不改写。

## 整批收集错误

用户允许下一轮一次运行全部场景、集中分析问题。新增显式整批模式，默认仍首败停止；整批模式保持单worker、零重试，每例独立，普通断言失败继续后续场景。未结账本、预算停止、期限或额度上限停止后续案例，保留NOT_RUN。付费调用仍由既有共享账本逐次预留约束。此模式用于收集完整问题清单，不放宽十个样本的业务断言，也不把诊断运行当作验收通过。

批量审查发现预留超限原来只拒绝当前请求，后续较小请求仍可能通过。现将CORE_BUDGET_LIMIT与停止时间写入既有账本，跨用例/重开账本仍停止；在途调用继续正常结算，不抹去预留。回归先复现后续较小请求被错误放行，再验证持久停止。

验证：37项回复适配器、10项预算、9项用例间预算检查通过。前端首次规范检查发现测试helper未进入构建镜像，已沿用src/test-support布局并补COPY；issue230-batch-frontend02为243通过、3跳过。issue230-batch-agent02为580通过、3跳过。格式/静态/类型及Standards、Spec（含预算和打包修复）通过。下一轮冻结十样本Pro整批诊断，上限1.5元、80次、160000 token、30分钟；不批准或执行补偿。

## 十样本整批诊断结果

issue230-real-allpro-batch01在6005755运行，6通过、3失败、1未运行：两项无补偿、两项待审批、支付样本1、拆票样本1通过；支付样本2、拆票样本2、生成围栏样本1失败。最后一例因未结账本停止，批量停止规则实际生效。

拆票样本2的支付工单两次回复均为DOMAIN_GROUNDED_NARRATIVE校验失败，HTTP200；支付样本2只有一次成功动作调用，无回复调用，缺少具体动作终态诊断，不能把两项失败混为同一原因。生成围栏样本1旧代次SUPERSEDED，同票有STALE_OR_OUT_OF_SCOPE_GENERATION；旧回复发布失败且未读到终态用量，形成PENDING。新代次完成必需读取后UNKNOWN_ACTION、无模型调用，与预算拦截一致，但旧报告不能唯一证明被拦截的原异常。

用户报告期间短暂断网。现有格式/业务校验失败及旧代次拒绝发布证据不能统一归因网络；需保留环境影响的不确定性。不要因网络恢复清除费用预留或重复整批。

本批43个已知尝试、47076个已知token，已知估算0.514800元，另有一笔PENDING预留0.082206元，用量未知，故总调用/总token/总估算保持未知。历史已知估算累计1.029585元；以本批前用户新增剩余2.8元计算，扣除已知估算和未结预留后暂余2.202994元，此数不是供应商账单或新调用授权放行。资源清理通过；未结费用核实前停止付费，继续离线修复。

## 发布拒绝后的同流计量与诊断

离线复现确认，发布回调拒绝会提前退出流读取，导致同一响应后续终态用量丢失。修复为首次拒绝后停止正文发布，仍在原期限内读取同一响应供失败计量；不新增请求、不重试，也不修改 Spring 围栏。取得用量后仍报告 PUBLICATION_FAILED；缺用量、断流或期限耗尽保留未知预留，取消继续传播。此修复不回填历史 PENDING。

诊断另保留无 failureClassification 的 HANDOFF_SELECTED，避免主动转人工的动作证据丢失；不据此改写历史支付样本2的未知原因。计量诊断10项测试通过；流读取首轮41项通过，Agent规范检查585项通过、3跳过，类型检查无错误。复审发现 incomplete/failed 终态带用量的分支仍丢失计量，新增两项测试先复现失败，再补充仅在发布已停止时保留失败终态载荷；修复后43项通过，Standards/Spec复审通过。

最终 issue230-terminal-usage-agent01 规范检查退出0：587项通过、3跳过，格式、静态和类型检查通过。本轮尚未执行最终全栈门禁，未提交或合入；#230尚未完成，付费复验等待历史未结用量核实。

## 新授权后的复验边界

用户随后将从现在起可使用的剩余预算调整为2.3元，并明确继续。因此新建独立授权记录，历史0.082206元未知预留仍原样保留，不伪记为结算，不占用或释放旧账本来重跑。新预算不代表供应商账单核实。冻结修复后的完整十样本Pro复验：单轮最多1.5元、80次尝试、160000 token、30分钟；单worker、零案例重试，普通失败继续收集，新未知用量或预算停止则终止后续案例。仍不批准或执行补偿；保留所有失败与未运行分母，不能靠重跑次数宣称验收通过。

## 修复后十样本复验结果

issue230-real-allpro-batch02 在8e51e61执行，5通过、5失败、0未运行，资源清理通过。完整报告保存在该轮本地证据目录，不公开原始浏览器轨迹或账本。

| 场景 | 样本1 | 样本2 | 失败证据 |
| --- | --- | --- | --- |
| 物流无补偿 | PASS | FAIL | 样本2必需事实已读取，动作模型返回INVALID_JSON |
| 物流待审批 | PASS | PASS | 无 |
| 支付说明后交人工 | FAIL | FAIL | 样本1两次回复均DOMAIN_GROUNDED_NARRATIVE；样本2明确选择HANDOFF，未生成支付说明 |
| 同订单拆票与恢复 | FAIL | FAIL | 两例物流票均完成；支付票分别为动作SCHEMA_MISMATCH和两次回复DOMAIN_GROUNDED_NARRATIVE |
| 生成中追加消息隔离 | PASS | PASS | 两例旧回复发布被拒均SETTLED，新代次完成 |

本轮50次调用、56908 token均有用量，估算0.635094元；未知用量、PENDING、在途预留、缺失来源均为0。两次旧代次PUBLIC_REPLY_PUBLISH_FAILED均取得用量，且两个生成隔离样本通过，为同流计量修复提供真实产品证据。历史未知预留仍不变；以用户新授权2.3元扣除本轮估算，暂余1.664906元，供应商实际账单未知。

各失败供应商响应均HTTP200。此证据支持当前模型与提示配置尚不能稳定满足动作结构及支付回复合同，不能统一归因断网，也不证明整个架构必须重构或更换供应商必然成功。支付回复的拒绝正文未保存，动作INVALID_JSON/SCHEMA_MISMATCH也缺少原始内容，不能猜测具体违规句或JSON形态。下一步优先对相同受控输入做小规模跨供应商对照，再决定适配或提示调整；不继续原样整批重跑，不放宽合同或替换为固定业务答案来制造通过。需要先明确用户可用的其他供应商和模型，再核对其接口、价格与调用边界。本票保持未完成，未执行最终全栈门禁、未合入或关闭。

## 自由正文边界合入后的十样本复验

issue230-real-allpro-batch03 在已合入 `main` 的 c3dbb55b6a113501870905bcf9307d5a41cbabc4 执行，原始结果保留为7通过、3失败、0未运行。51次供应商尝试、56092 token均已结算，程序估算0.635580元；未知用量、PENDING、在途预留及缺失来源均为0，资源清理通过。以用户新增2.3元预算扣除batch02与batch03程序估算后暂余1.029326元；历史未知预留与供应商实际账单继续分开记录。本轮没有批准或执行补偿。

三项失败不能归为同一种问题。支付样本1在完成订单、补偿与支付读取后由动作模型明确选择 `HANDOFF`，代次进入 `HANDED_OFF`；这是安全的人工终点，但没有完成本场景要求的“支付事实说明后交人工”，保留为一次模型质量失败。支付样本2的回复说明“当前处于全额退款状态”，拆票样本2的支付回复说明“没有完全退款的历史状态”，均与对应权威事实一致；旧验收正则只接受少数固定句式，造成两项误报。运行时Spring和Agent均已按ADR 0011接受正文，没有发布失败或业务越权。

核心矩阵验收随后移除自由正文的支付、退款、未决和人工关键词正则，改为精确核对数据库中的 `PAYMENT=PAID`、`REFUND_STATUS`、已接受结论、`DUPLICATE_CHARGE` 人工原因、共享队列，以及补偿提案和执行均为0；公开正文只要求非空并在客户页面可见。该修改没有把主动提前 `HANDED_OFF` 改成通过，原始batch03结果文件也没有重写。离线 issue230-core-payment-acceptance04 的十个浏览器场景全部通过，计量采集与资源清理通过，外部付费调用为0。前三次离线准备分别因格式检查和格式器未加载仓库配置而在业务执行前停止，均完成清理；最终使用与正式矩阵相同的配置复现并修正。

因此，batch03按当前职责边界可解释为九个样本满足产品行为、一个样本安全转人工但未达到该样本的回复质量目标；这不宣称10/10稳定，也不要求更换供应商。是否接受这一重复表现完成#230，应与“无需每次百分之百自动完成”的用户决定一起判定；#174继续保持暂停。
