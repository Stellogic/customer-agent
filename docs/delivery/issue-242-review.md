# Issue #242 合入前审查

审查固定点：`adacdd74f0492ad10f49a064170c2039c85b0b7c`；受审提交：`b0fc7fb8393bd63b94e9d41af1be7ced5725c72c`。差异命令为 `git diff adacdd74f0492ad10f49a064170c2039c85b0b7c...HEAD`。四个实现与证据提交分别为 `acaa731`、`dc7767d`、`3f0027c`、`b0fc7fb`。

PR #243 只转换为 Ready 一次；以下集中风险审查只执行一轮，双轴确认另行记录。审查使用 Codex 独立子任务，不调用项目真实供应商模型。规范来源为 `AGENTS.md`、`docs/agents/test-gate-lock.md`、`docs/agents/domain.md` 与 `docs/agents/issue-tracker.md`；规格来源为已回读的 [Issue #242](https://github.com/Stellogic/customer-agent/issues/242)。

## 集中风险审查

结论：**0 项需要修复的阻塞发现**。检查普通操作可触发的权限、并发、持久化、兼容性与预算风险，不将无代码证据的猜测或风格偏好列为阻塞项。

- 审批恢复只保存当前主体作用域内的原请求 ID、提案版本 ID 和原租期；后端幂等领取仍按审批人和请求 ID 查回执，审批视图继续校验主体、token/version、有效期及禁止自审。刷新不会续租，也不会让存储记录成为权限依据。
- 异步领取、视图及决定回包核对当前请求或租约引用；原页面迟到响应不能恢复旧证据或清除新页面领取记录。
- 两处秒/小时校验与数据库整数商契约一致，类型检查仍在，Spring 按精确秒数确定金额的权威保持。
- 10 元只改变新账本创建允许的上限，既有账本、锁、预留、未知用量停止逻辑不变；零请求恢复保留初次启动失败、原授权和原期限。
- 实际评测 SHA、失败和 NOT_RUN、正文不足、未知用量及预留均保存，没有将程序必读或业务状态通过冒充自主行动/语义通过。

本审查不等于最终完整门禁通过，也不能证明未执行的九个真实能力场景。正式门禁结果在 [PR #243](https://github.com/Stellogic/customer-agent/pull/243) 关联最终受测 SHA 与 Run ID。

## Standards

结论：**0 项硬违反，0 项需处理的启发式代码味道，最严重等级：无。**

已核对 AGENTS、测试锁、领域和 Issue 跟踪规范，以及 ADR 0003/0008/0010/0011。两处秒关系修正贴合领域定义；审批恢复采用主体隔离的最小请求记录，继续由 Spring 租约授权；评测复用既有锁、采集及隔离路径，保留失败、未知计量与中文数据。少量项目专用重复不值得为此引入抽象。

完整门禁属于此后的交付步骤，不把尚未运行最终门禁或 NOT_RUN 能力当成规范违反。

## Spec

结论：**0 项发现，最高严重度：无。**

- 两项修复符合“Agent 与数据库的小时派生规则一致”及“通过既有幂等领取与权威审批视图恢复”：精确秒数保留、不放宽事实一致性；审批仅保存当前身份的最小请求标识，恢复后校验权威视图。
- 聚焦红绿与真实浏览器回归已留证。10 元上限调整有明确追加授权，未见无依据范围扩张。
- 符合“未知费用……停止并记录 FAIL/NOT_RUN”：保留 2 PASS / 1 FAIL / 9 NOT_RUN、11 次请求及 PENDING 预留；零请求启动恢复沿用原账本和期限。
- 符合“结构通过率……不得等同回答正确率”：两条正文直接回答均为 0；程序必读、模型选择与未测能力分开记录，没有宣称全面达标。
- 最终门禁、合入与远端回读属于后续交付步骤，文档未虚称完成。#149 排除，#174/#227 保持边界。

双轴总结：Standards 0 项、Spec 0 项；各轴均无最严重问题。此文提交后冻结所有受版本控制内容，从仓库根目录运行 `pwsh ./scripts/check.ps1 -Issue 242 -RunId issue242-final-20260915`；只在该完整门禁通过后合入，结果关联 PR #243，不在门禁后修改本仓库文件。
