# 真实多问题受理失败：实施修复承接

## 当前阻塞与所有权

2026-09-04 回读 GitHub：#153（同订单多问题受理）已关闭，#174 仍开放；当前没有开放的受理修复票。#174 的正式 AC 限定本票只增加验收场景、预算门禁和证据，产品缺口须回对应实施范围处理。因此 PR #214 不能夹带产品修复后宣称原冻结验收已通过。

建议另开 #153 的后续修复票，标题：`[修复] 定位并修复真实模型受理确认后错误转人工`。保留 #153 历史交付和 #174 所有失败证据，不重开或改写旧完成记录。

## 已证实的复现

复现入口及冻结约束见 [第四阶段诊断](issue-174-intake-diagnostic-plan.md)。运行提交 `3a65485`，真实浏览器经 Spring 和 LangGraph 调用固定 DeepSeek。客户描述同订单包裹未收到且疑似重复扣款；页面要求确认包裹，客户正确回答后，数据库创建 AGENT_UNAVAILABLE 协助，两个 pending 类别仍原样保留，未形成拟建工单。请求耗时 3609/2453 ms，不是已证实的超时故障。

已有 RunId 不可重用。诊断的 passed 只代表观察完成，不能当作发布场景通过。响应 assisted=false 的文案匹配缺陷及其证据边界已在诊断文档说明，不修改原始结果。

## 最小修复范围与验证要求

1. 在受理网关保留最小的受控失败分类，区分传输、解析、状态一致性校验；在服务层区分网关失败与服务端理解校验失败。只记录枚举和必要计数，不记录异常正文、订单标识、客户正文、prompt、模型响应或密钥。当前所有这些路径被归并为 IntakeAgentUnavailableException，无法从既有记录确定底层原因。
2. 用原浏览器受理路径取得分类证据。不得用独立 provider 请求冒充产品复现；不得再次随机重跑求绿。沿用累计账本与冻结调用上限，新增运行须另设 RunId，已有六笔 PENDING 共 2.40 元完整保留。
3. 基于实际分类，先建立对应失败回归测试，再做最小修复。不预判模型、网络或 Spring 谁错误，不提前放宽 hasConsistentShape，不跳过澄清、人工协助或客户最终确认。
4. 验证逐项澄清后精确得到 PACKAGE_NOT_RECEIVED 与 DUPLICATE_CHARGE，一次确认创建两个独立工单并共享受理记录；失败转人工仍有效。使用已有 AgentServerIntakeUnderstandingGatewayTest、CustomerIntakeV2ApiTest、agent/tests/test_intake_model.py 及 #173 A 浏览器接缝承接，不增加无关架构。
5. 实施修复须按仓库要求完成聚焦验证、双轴审查和最终完整门禁，经独立 PR 合入后，再为 #174 冻结新的发布复验。产品提示若确需改变，应在实施票明确版本变化，不悄悄改写 #174 原冻结版本或失败结果。

## 当前资源和费用

诊断04已退出并清理，锁回读 FREE。累计已结算 3.810222 元，加 PENDING 2.40 元，占用上界 6.210222 元，距离授权 8 元尚未预留 1.789778 元。该余额不是新的可重复重试授权，也不能视为已取得全部 provider usage。

未执行最终完整门禁、PR Ready、合入或关票。本文只是可审阅的修复承接方案，不代表新修复票已创建或产品问题已解决。
