# #217 真实调查单场景冻结

2026-09-05。仅恢复 #217 的真实验证，不作为 #174 五场景发布验收。

被测整合提交 `d9d81da799673f840350a5d6c6e528666c236a13` 在 #217 提交 `35468f7` 上整合 #174 的已有验收工具 `4506299`。两者调查行动模型源码相同；业务实现沿用最新主线 `ca0977a` 加独立结论额度修复。另包含与 #217 相同的 Agent test 阶段固定 Node/npm 修复；不改变运行阶段。原 #217 与 #174 分支均未覆盖。

入口：`pwsh ./.local/issue217-live-validation.ps1 -ConfirmProviderSpend -InvestigationDiagnostic -RunId issue217-live-20260905a`。派生差异保存在 `.local/issue217-runner.diff`，只供本次验证；不合入产品 PR。运行开始前工作树必须干净。

只运行现有 #173 D 真实浏览器场景一次：独立数据库生成一张延迟23小时的合成订单，客户经 React → Spring 受理并确认建单，LangGraph/DeepSeek 完成调查和客户回复，浏览器确认五分钟自动解决候选及取消后的刷新恢复。不得写入工单/回复/结论等结果，不调用内部 Agent API 创建结果，不替换模型响应。指标采集只读原生检查点。

模型固定 deepseek-v4-flash；动作上限8、供应商尝试每调查代次8、16000 token、120秒、10000 micro-USD；全链尝试上限20，本轮预留上限1元。维持既有受理、行动、判断、沟通模型和协议。浏览器不重试；如仍有 SCHEMA_MISMATCH 或其他失败，仅保存聚合分类并停止，不能认定额度修复解决全部模型问题。

成功必须同时满足浏览器通过、恰好一个完成的调查代次、行动/业务判断/客户回复都有真实供应商尝试、无失败分类、已知usage可信且未超出本轮上限。只核对这个场景的产品行为；不外推通用回答语义质量。

预算使用唯一共享账本。保留原12笔 PENDING（4.8元）；核对点之前11笔风险预留和3.810222元估算不当作平台实扣重复累加。用户提供历史实扣不足2元按2元上界，之后 diagnostic01 的1元仍独立占用，再加本轮1元：累计保守占用4元，小于8元授权；用户当时至少3.8元可用减去两笔1元后剩余1.8元。本次仍保留整轮预留，不声称平台实扣或自动重跑。

独占 Issue217 测试锁，项目 `customer-agent-issue217-live-20260905a`、对应独立卷/网络、动态环回端口和精确镜像标签。启动前内存核对有效配置，启动后无供应商调用核对模块路径与128/1024默认值。无解析插桩，不导出浏览器正文、截图或trace，不保存模型原始输出/密钥；只保存现有聚合指标。仅清理已记录启动的本轮Compose和实际存在的本轮标签，回读资源为空后释放锁。

执行前静态双轴审查通过；离线产品回归19/19通过。Agent 全量静态检查 issue217-static-20260905e 通过：Ruff format/lint、Pyright 0 errors；pytest 453 passed、3 skipped。真实结果待运行。
