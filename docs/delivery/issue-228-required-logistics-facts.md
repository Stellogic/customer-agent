# #228 物流必需事实与证据职责交付

## 基线与范围

继承已合入的 #226（PR #231），从 `origin/main@17e9a98ae2aeaedbc2bc9b6bebac521cc6b202ec` 建立独立工作树。实现 #227 的首个物流切片，保留共享工作树、旧 PR #214、#174 历史及暂停状态。职责决定见 ADR 0010，旧 ADR 0006 不改写。

Spring 的 `EvidenceSufficiencyPolicy.LOGISTICS_DELAY_REQUIRED` 是物流必需规则的唯一业务来源。既有授权能力目录增量返回 `requiredFacts`，包括策略版本、场景、事实类型、能力、结果字段、实际证据槽位及适用类别。其他场景返回 null，继续消费旧路径；重复扣款迁移由 #229 承接。

程序先确认订单，再依据目录读取尚未取得的必需能力，策略随 LangGraph 断点保存。五项能力包括订单、物流、支付退款、补偿待办和适用政策；不把订单规则列为必读。证据关系只绑定实际能力返回的当前代次引用。Spring 原有权限、代次、事实漂移、冲突、资格与审批检查保持。

模型在必读完成后决定是否补查、提交或转人工，并继续承担知识查询语义和自然回复。保留现有订单歧义澄清及恢复，不新增任意调查追问协议。DeepSeek 使用 prompt `investigation-action-v7` / schema `investigation-action-v4`；v5/v6 仍是旧历史候选。程序动作消耗动作数与时间，不制造模型调用、供应商尝试或费用记录；补充模型调用照常计量。

## 验证与诊断

- 旧 Spring 目录：新增 HTTP 契约用例 `2 failed`，均因缺少 requiredFacts。实现后 Backend 的完整构建测试目标与格式检查通过。
- 必读循环回归先红后绿；覆盖新实例从断点恢复、五次真实能力执行、一次模型决定、固定证据合并及调用费用守恒。
- 新增 DeepSeek transport 回归在旧实现 `8 failed`；实现后与既有模型回归通过。覆盖补查、提交空补充证据、已有订单歧义澄清及错误响应拒绝。
- Agent 全部回归 `481 passed, 3 skipped`；后续补充失败能力归属后，相关 loop/graph `54 passed`，格式、lint、类型检查通过。最终完整门禁仍以受测版本的 PR 交付记录为准。
- 独立 Spec 审查发现必读失败没有记录失败能力。新增回归先 `1 failed`，修复后保留实际能力及错误码，模型记录及费用仍为零。Standards / Spec 对该增量均 PASS、0 项。
- 浏览器 `red01` 被测试格式检查拦住，不计作业务失败证据。`red02` 的无补偿路径明确因读取 READ_ORDER_RULES 失败；待审批断言需要读取真实最新公共投影，随后改为一次刷新核对 PENDING_REVIEW。
- 浏览器 `green01` 的无补偿路径通过；待审批与必需事实核验通过，但旧并发点击未命中活动代次。保存的 trace 显示两个真实消息请求相隔约两秒。测试改为汇合两个真实请求，在第一条消息创建的代次确为 ACTIVE 时发送第二条，保持该具体代次 SUPERSEDED、单次终止事件及零旧回复断言，不伪造业务响应或重试业务。
- 上述浏览器轮次均保存原始结果并完成本轮容器、卷、网络、镜像清理。后续绿色结果与最终完整门禁绑定实际提交记录，不覆盖失败分母。

## 证据边界

本票只使用确定性模型及受控外部 provider 响应，新增付费调用为零。离线产品验证不等于真实模型稳定或知识回答质量通过。用户给出的新增总预算 3 元由 #230 冻结实际样本、次数、尝试、token、时间和副作用后使用，旧未结算记录不清零；不恢复 #174。

局部日志保存在本工作树 `.local`，不提交原始运行输出。最终交付须完成最终版本的仓库完整门禁、PR 合入、Issue 关闭及 origin/main 回读。

浏览器最终专项 issue228-logistics-green02：2 passed（25.4 秒），Prettier、ESLint、TypeScript 通过，实际代次隔离及两个物流终点均通过；cleanupPassed=true，paidCalls=0。完整门禁尚待该实现固定提交后执行。
