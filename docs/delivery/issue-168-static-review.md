# #168 静态预开发与双轴审查记录

阶段状态：`CODE_READY_NO_TESTS`，**仅指以下已实现范围，不代表整票完成或任何实测通过**。

本文件先保留 2026-08-30 首轮审查记录；2026-08-31 接口复核及 RRF 适配的独立增量双轴结论见末节，不沿用旧 PASS 为新实现背书。

## 审查基点

- 起点 main：`2ca9d097da1f93d4cdf3eeef347c62cf51f0e058`。
- 分支：`codex/issue-168-static-ablation`。
- 实现提交：`05e405b6f4dc4a1f3e44a60f2a0830079bc90385`。
- 比较：`git diff 2ca9d097da1f93d4cdf3eeef347c62cf51f0e058...05e405b6f4dc4a1f3e44a60f2a0830079bc90385`。
- #190 前置只读：PR203 @ `4317fd35897d2b2665dff0d21219a16cd23ad5d6`。

两个独立审查代理按当前 worktree 的 `.agents/skills/code-review/SKILL.md` 并行静态审查，没有执行被禁止的验证命令。

## 已实现范围

四个独立模块：三路消融编排与失败证据保存、ONNX feature-extraction 导出/CPU 张量执行、向量/归一化/排序一致性比较、新进程资源测量。新增四份测试源码、[输入契约及交接说明](../eval/issue-168-static-contract.md)、[未运行报告模板](../eval/issue-168-report-template.json)。

#190 的编码器、评分器、默认检索、模型准备、依赖/锁文件、Compose、门禁入口及 #189 冻结资产均未修改。

## Standards

**PASS，0 项发现。**

中文产物、复用成熟能力、职责归属和静态提交规则均符合仓库要求。十二项 smell baseline 未发现需要修复的问题；明确的集成回调不是无需求扩展。未运行测试/格式/类型/构建/模型/锁命令；工具可发现的事项不在本轴结论内。

## Spec

**PASS，0 项发现，仅限已实现的隔离静态预开发范围。**

消融指标与前置契约对齐；ONNX 包装固定 CLS/L2 和 CPU；一致性区分版本、顺序与拒答；资源测量区分冷启动/首推理/稳态，RSS 使用 OS 高水位。没有虚构指标、复制前置或切换默认。已声明的真实接线和正式验收不作为此阶段遗漏。

两轴均无有效发现，因此本轮无修复提交；本记录只补充审查证据，不更改被审实现。

## 尚未运行与剩余依赖

- 所有测试、格式、类型检查、构建、模型下载/准备、ONNX 导出/推理、质量/资源评测、本地完整门禁均未运行。
- 没有获取或查询测试门禁锁，没有模型调用，没有 CI，没有转 Ready、合并或关票。
- 等待 #190 冻结质量实测 PASS、完整门禁 PASS、合入 main 且关票。当前不能把 PyTorch 视为合格基线。
- 等待明确分路评分接缝、feature model/tokenizer 加载和 ONNX 文本适配，接入依赖清单；缺口已通知协调任务。
- 同步届时最新 main、核对接口/共享文件/迁移编号（本票没有迁移）、完成增量 CR 后，仍需协调明确放行持锁验证。锁 FREE 不构成许可；结束须通知 `LOCK_RELEASED`，不轮询不绕锁。
- 原 ONNX 验收、可复现收益、容差预先冻结、资源比较与证据归档要求全部保留。

两轴合计：Standards 0；Spec 0。该静态结论不能替代运行验证。

## 2026-08-31 RRF-only 增量审查

- 基点：上一阶段 HEAD `dd119bfb7f1d9c1a8a6e746c6f77258564543fb6`。
- 实现提交：`6b340f20b29be40be2440fd19a811189f4a094a6`。
- 比较：`git diff dd119bfb7f1d9c1a8a6e746c6f77258564543fb6...6b340f20b29be40be2440fd19a811189f4a094a6`。
- 前置只读参考：PR203 @ `5402bd4c438ff68fc9bbc4a01e55080b12499ce9`，没有合入本分支。

两个新的独立代理审查本轮增量，均只读，未运行任何测试、格式、lint、类型、构建、模型、评测或锁命令。

**Standards：PASS，0 项发现。** 公开接口适配职责明确，模式参数只服务当前部分执行需要，无需增加抽象层；文档区分旧结论、本轮范围和未运行状态，未触及前置独占文件。

**Spec：PASS，0 项发现。** 原样委派 `run_query/metrics`，未冒用单路评分；RRF-only 成功返回 `PARTIAL`，未执行的单路保持空数据与 `NOT_RUN`，查询异常传播并记录 `ERROR`。文档如实记录公共接缝需求，没有声称 c5 或真实比较合格。

本轮没有需修复的发现。实际贡献是一个 RRF-only 适配、显式部分执行报告语义、两份新增测试函数源码和交接更新，未完成三路真实消融或 ONNX 文本接线。所有实际验证仍为 `NOT_RUN`，阶段为 `CODE_READY_NO_TESTS`。

协调任务已收悉并确认：单路独立评分/拒答与 encoder 公共加载/分词需求进入后续集成清单；不为本票修改 c5 受测候选，待其候选验证后明确接口修改归属。这不是后续运行、同步/合入前置或修改共享文件的授权。
