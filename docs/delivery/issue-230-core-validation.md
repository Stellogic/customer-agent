# #230 多问题处理、调用计量与核心验证

## 当前状态

实施中。前置支付路径 #229 已经由 PR #233 合入，main 为 97ba3f2，本票已对齐该版本。真实核心计划尚未冻结、没有执行付费模型调用。本票不能据普通门禁关闭，#174 与完整知识质量评测继续暂停，PR #214 不变。

用户已授权本轮真实模型预算 3 元。新增预算尚未使用；历史支出及未结算 PENDING 独立保留，不重置、不自动释放。

## 已验证的小切片

- 真实供应商 HTTP 适配接缝保存受理成功的实际响应 ID、模型、提示及 schema、token、缓存元数据。供应商缺失的字段保持未知。
- HTTP 200 已返回 usage，但业务答案解析失败时，适配器保留该次 usage 和 `SCHEMA_MISMATCH`；Spring 的解析失败与状态一致性拒绝异常也保留已获得的证据，不向客户返回原响应或异常正文。
- 同一受理模型实例并发运行两次真实 LangGraph 受理：一条完整 usage，另一条没有 usage。节点在自身 asyncio 任务内读取审计增量，响应 ID 与消耗分别归属正确；未知 token 和费用为 null。
- `issue230-intake-green02`：Agent 7 项通过，Spring 8 项通过，格式、lint、类型检查通过。`issue230-graph-green01`：受理图与适配器共 8 项通过（1.04 秒），lint、类型检查通过。均未请求外部供应商。

## 失败证据与待验证实现

`intake-red01` 的 Agent 挂载参数写错，未选中目标测试，不计为行为 RED。修正入口后的 `intake-red02`、`intake-red03` 分别复现缺少成功审计、解析失败时丢失 usage；第一片曾通过行为测试但类型检查失败，修正显式 transport 注入后通过，原日志保留。

`issue230-graph-red01` 已复现图输出缺少 `intake_call_evidence`，随后绿色验证如上。`tracers-red04` 又复现受理图异常未返回费用证据、调查图缺少按票计量；前置业务结果均通过。修正后 `tracers-green02` 的 12 项测试通过，覆盖受理受控失败、两票并发调查、已知/未知 usage 和预算预留。装饰器最初丢失具名 state 参数导致两处类型检查失败，保留具名协议后源码类型检查零错误；未放松检查。

独立 PostgreSQL 回归使用真实 Flyway、Spring 事务和正式恢复接口，供应商侧仅用本地 HTTP 替身。`db-red01`、`db-red02` 停在测试角色权限，属于夹具问题。按现有授权使用 Spring 应用角色后，`db-red03` 通过业务 503 与回滚前置断言，复现没有独立调用记录。专用表 V46 和独立事务记录器实现后，`db-green01` 通过，证明回滚保留消耗、同键重试新增实际调用、成功重放不再次调用。迁移从当前源码挂载执行，未沿用缓存镜像内的旧迁移；隔离资源清理读回通过。

调用记录以实际模型调用为单位，覆盖开始受理、补充消息、恢复，以及确认或重复处理后推进下一订单。开始受理预分配关联 ID，记录不依赖尚未提交的业务行，也不以请求键唯一约束覆盖真实重试。供应商分类与 Spring 业务拒绝分类分别保留。

## 预算与计量边界

受理操作数不等于供应商尝试数：普通单订单确认不再重新理解已确认内容，但推进下一订单仍可能发生新调用。精确计量必须来自调用证据，不能根据操作数推算。

现有 `costMicros` 采用美元程序估算；新的人民币预算不得与它直接相加。核心报告应分别列实际 usage、按冻结价格计算的程序估算、供应商结算和未结算预留。供应商结算不可获得时保持未知。

2026-09-07 核对的 [DeepSeek 官方价格](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)列出 Flash 0731 高峰输入未命中 3 元/百万 token、输出 9 元/百万 token。候选预算策略按高峰、不计缓存折扣预留。请求 JSON 字节数两倍加余量仅是工程预留，无法证明供应商内部组装的数学上界；不得称为精确计费。实际 usage 以 [供应商返回值](https://api-docs.deepseek.com/zh-cn/quick_start/token_usage/)为准。

实现仅服务冻结核心诊断：跨运行保留的宿主账本共享本次授权，固定旁路文件锁保护原子更新。已通过模块级并发预留、未知 usage 保留、已知 usage 释放差额与超预留停止测试；四类模型实际 POST 前的预算接入与工厂配置测试已通过；端到端验证仍待完成。所有重试必须计入同一上限。未知 usage、超预留或重启遗留未完成请求停止后续付费请求，保留预留。账本不能只存在于会随 Compose 清理的数据库卷中。

## 后续交付条件

先完成调用归属、故障、未知 usage、持久化及多问题浏览器离线回归，再根据实际规模冻结五类核心场景的两个固定样本、受测版本、模型/提示/schema、次数、尝试/token/费用/时间上限及停止条件。固定范围包括物流无补偿、物流待审批、支付说明后交人工、同订单拆票与恢复、生成中追加消息隔离。

所有场景从 React 正式入口进入 Spring、PostgreSQL 与 LangGraph；内部查询仅辅助诊断，不能代替产品成功。保留失败和未运行的完整分母，不追加次数直到通过。真实计划未执行或核心行为失败时，本票保持未完成，列明阻塞。

## 2026-09-07 主动暂停交接

用户要求尽快保存代码并停止。本次保存为开发检查点，尚未完成验证，不代表可合入或可关闭 #230。

已补齐但仍待运行验证：四类模型工厂共享预算账本、可选受控供应商地址、受理 HTTP/超时/非 JSON 失败审计及图回传、按调用去重且保留未知量的汇总与只读采集入口、受控供应商浏览器运行脚本。新增测试尚未执行；语法检查不能替代行为、格式和类型检查。Spring 当前将受理失败统一归为 RESPONSE_PARSE，供应商分类仍保存在 evidence；需继续核对该分类语义。

#229 的 9cf711a 最终门禁在本次暂停时主动停止，日志保存在其工作树 .local/full-gate-9cf711a-r2-user-paused.log，不计通过；PR #233 尚未合入。#230 暂基于 3772269，需待 #229 正式交付后再对齐 main，保留本检查点改动。

恢复顺序：先确认测试资源为空；完成 #229 最终门禁与交付；对齐本分支；运行新增 Agent 聚焦测试及静态检查、Spring 回归和受控供应商浏览器回归，再冻结真实核心计划。完整门禁、双轴审查和真实核心验证均未完成。真实模型新增支出为零，3 元授权预算尚未使用；#174 继续暂停。


## 恢复后的验证记录

2026-09-07 用户恢复任务。#229 最终完整门禁 issue229-final-9cf711a-r3 通过后已合入，#174 仍 OPEN。本票开发检查点在对齐 main 后为 06511b3。

core-green03：36 项聚焦测试通过，首次静态检查发现导入排序、注释标点与供应商费用求和的类型收窄问题；修正后 agent-green05 全量 Agent 回归 537 passed / 3 skipped，lint 与类型检查通过。随后 format06 完成三个适配器的纯格式整理。Spring static04 真实 HTTP 网关失败分类回归通过，供应商故障现在与答案解析错误分开，原始响应与秘密不进入客户结果。正常双票及未知 usage、供应商拒绝的端到端回归仍待验证。

端到端诊断：normal01 停在新测试缺少 Node 环境类型声明，业务未运行；normal02/03 在受理请求前失败，随后使用已安装的 BlockBuster 复现同步账本读取触发 BlockingError。改为工作线程执行预留/结算后，blocking-green02 的 14 项测试通过。normal04 已保留实际传输失败审计与 PENDING，normal05 的只读连通性检查进一步证实 Docker 自动注入代理使内部供应商请求走代理并发生 DNS 失败。离线运行器增加 no_proxy=*；normal06 因 PowerShell 对大小写同名 JSON 键的限制停在配置解析，改用小写单键。各轮按独立标识保留日志与结果，均不计产品通过、均无外部付费请求。

正常双票离线回归 normal07 通过：一次确认两张工单、各自事实和业务终点、分组与刷新、客服接手、双票并发屏障，以及 Spring/checkpoint/供应商实收/宿主账本计量一致。实际受控 HTTP 请求7次，756个合成usage token，按固定价格的程序估算3276微元人民币；这是离线夹具usage，不是供应商收费。缺失证据与未结算预留均为零。

missing_usage 的 missing04 与 provider40001 故障回归通过，分别保留未知用量与 PENDING；通过真实客户界面再次开始受理时没有新增供应商请求或正式工单，按现有合同进入受理协助。早期 missing01 错把该合同预期为503，missing02停在新矩阵文件的类型声明，missing03错把被JSON省略的空字段预期为显式null；修正测试合同后通过，未修改产品以迎合测试。预算发送前拒绝现明确返回零调用证据，供应商失败、未知用量和程序预算停止可区分。

取消预留/结算的事件控制测试通过；阻塞检测及发送前停止证据等后续18项聚焦回归通过。完整真实核心计划仍未执行；矩阵中的真实流式追加消息正在离线验证，不能据上述故障回归宣称整个核心矩阵或真实模型通过。

matrix01 首项失败、其余9项未运行：合成订单编号含下划线，不符合现有回复合同；改用项目已有连字符格式，未放松业务合同。matrix02 的五类场景、每类两个样本共10项业务断言通过，但计量未通过：旧代次取消后节点不再返回 checkpoint，导致2个旧代次的6次已结算调用没有归属。52次受控HTTP尝试的已知合成usage为7956 token，程序估算38376微元，不能当作实际供应商收费，也不计整个矩阵通过。

最小修复把本轮账本的工单、代次和逻辑调用归属提前到请求前持久化，结算保存已有受控调用元数据。聚合按显式归属与 attemptId 补齐被取消节点的证据，不按时间或相邻调用猜测；仍缺证据或缺 usage 时保留未知。新增取消后恢复、并发上下文隔离、持久停止及在途结算回归。agent-green10 全量545 passed / 3 skipped，类型检查零错误；静态检查发现夹具注释标点，已修正。

真实入口直接从受测源码读取提示与 schema 版本；首次场景结束或失败后先持久停止新调用，最多等待30秒让在途请求返回 usage，再采集与清理。计量不完整不能返回成功。real-config01 使用合成凭据解析实际 Compose 模板，通过四模型模式、外部供应商路径、账本挂载及源码版本清单检查，没有启动服务或访问供应商。普通门禁纳入双工单回归；十场景流式矩阵由专用入口运行。

matrix03 通过：10/10业务场景、52次受控HTTP调用与52次逻辑调用对账一致；7956个合成usage token、38376微元程序估算，缺失归属与未结算均为零。两次旧代次的调用由持久归属恢复，不依赖取消节点返回。资源清理读回通过。上述数据仍全部来自离线供应商夹具，外部新增支出为零。

## 真实核心运行边界

基于离线52次调用规模，真实计划固定为五类各两个样本、总分母10，不重试测试，首次失败停止后续场景。预备上限为80次供应商尝试、240000 token、3元人民币；从实际启动冻结时刻起30分钟，单代次调查时限120秒，其余有效调查上限由入口回读记录。版本固定为干净受测提交，四模型的提示/schema从该源码读取，模型为 deepseek-v4-flash。只有入口将这些参数连同提交写入专属宿主账本与 real-core-plan.json 后才算冻结；本段不表示已经执行。

样本只预置合成订单：无补偿延迟6/12小时，待审批及拆票48/80小时，支付两样本分别未全额退款/已全额退款，生成中追加使用两条固定客户补充语句。工单、事实、回复与提案均经真实产品产生。预期副作用包括合成工单、公开回复、客服领取并释放支付工单与待审批提案；不批准、不执行补偿。出现业务断言失败、关键usage未知、预算/截止时间停止或全局故障时保留失败及未运行分母，不换账本或追加次数。真实结果未通过时 #230 保持 OPEN。

## 集中审查后的诊断与判定修正

Standards 对8f8c9a6静态审查通过；Spec 指出报告遗漏业务层拒绝分类，以及正常物流可能未被验收明确排除技术转人工。修复复用现有 checkpoint 的调查、判断和公开回复证据，仅投影类别、阶段、能力结果与同代次调用标识。供应商适配器记录的 PUBLIC_REPLY_PUBLISH_FAILED 与业务层 INVALID_MODEL_OUTPUT 分别保留；结论被 Spring 拒绝的 FACT_CONFLICT 不改写成供应商失败。矩阵现在明确要求正常物流保持 AGENT 且没有人工失败终点，拆票两样本还核对客服接手后的支付/退款事实、未决公开回复和释放领取。

四类诊断复用边界：供应商拒绝与usage缺失已由专用浏览器入口验证；格式失败复用 DeepSeek intake/communication 适配器的 SCHEMA_MISMATCH 回归；发布失败复用 test_spring_publication_http_error_is_not_a_provider_rejection；结论证据拒绝复用 test_conclusion_tool_retry_exhaustion_uses_the_same_stable_handoff_identity 的 fact_conflict 分支。新增 collector 参数回归对上述受控字段的本代次归属、供应商分类独立性及原文排除进行核对。这些接缝诊断不是额外真实场景成功，不增加冻结真实矩阵的分母。

precommit14 的规范化 Agent 检查548 passed / 3 skipped，格式、lint、类型检查通过。matrix04 在新增严格断言处首项失败、9项未运行：原先所有样本都携带疑似重复扣款标记，且无补偿问句不在既有低风险查询白名单，因此 Spring 按原业务规则公开说明后交人工，并非供应商技术失败。matrix03 的旧判定不足以替代此次严格核心条件。调整仅限合成输入：纯物流样本不携带重复扣款风险，无补偿样本使用既有“请解释物流状态”查询；支付及拆票样本仍保留支付风险。业务规则及严格断言不变，真实计划仍未执行。

matrix05 的加强矩阵10/10通过，含低风险Agent处理、客服事实/未决回复和旧代次围栏；计量及清理通过。集中平台风险审查在8f8c9a6完成、无发现；Standards/Spec 对随后五文件修正静态复核均通过。付费前置离线检查完成，真实结果仍待按冻结入口生成。
