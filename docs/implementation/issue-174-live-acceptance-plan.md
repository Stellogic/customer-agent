# #174 真实 DeepSeek 发布验收

状态：**第 3 版 INCOMPLETE：L174-01 PASS，L174-02 FAIL，其余三项 NOT_RUN**。基线：`origin/main@23475db`，已合入 #215 / PR #216 的受理修复。详见 [第三轮结果](../delivery/issue-174-live-03-result.md)。第2版仍为 INCOMPLETE；全部旧失败与诊断结果保留，不回写。

## 第 3 版复验冻结

后续准备：已将运行器的单代次动作/尝试上限从错误的6修正为产品默认8；这是新版草案，不修改第3版失败结果。第3版调用前清单归档为 [run03 freeze](../delivery/issue-174-live-03-freeze.json)。当前场景清单 revision=4、DRAFT_NOT_AUTHORIZED，入口在读取密钥或写账本之前拒绝执行。新 RunId、账本准入和报告路径尚未冻结，不能用旧运行标识重跑。总费用上限、全局调用上限和五项结果标准没有提高。现有未预留0.389778元不足1元发布预留，schema失败仍需定位；此处准备不代表修复全部阻塞。

用户在确认修复票已完成后要求继续发布验收。固定模型仍为 deepseek-v4-flash；受理提示改为实施票已交付的 intake-v3，模型 schema 按首次识别、澄清、其他受理路径分别为 customer_intake_issue_assessments、customer_intake_clarification、customer_intake_understanding。其他接缝版本及五场景的输入、阈值、分段预算和调用上限不变。原第2版浏览器交互已经逐项回答当前问题，可兼容首次一项拟建、另一项待澄清的合法状态，不更改产品实现。

RunId 冻结为 `issue174-live-03`，入口 `pwsh ./scripts/issue174-live-acceptance.ps1 -ConfirmProviderSpend -RunId issue174-live-03`，新报告写入 `docs/delivery/issue-174-live-report-03.json`。调用前已结算 3.810222 元，十笔 PENDING 共 2.80 元完整保留，剩余未预留 1.389778 元；本轮最多预留1元，累计占用上界7.610222元，不超过授权8元。脚本在锁内再次核对旧预留身份与金额。失败立即停止并保留整轮预留，不自动重跑。

#215 的真实三请求建单成功和完整离线门禁仅作为修复前置证据，不能代替本票五场景真实发布验收。以下第2版和第1版段落保留为历史冻结，以本节及 revision=3 场景清单为当前运行依据。

## 第 2 版复验修正

诊断03证明候选订单匹配，但首个澄清问题为 PACKAGE_NOT_RECEIVED；旧用例固定回答重复扣款，随后数据库出现 AGENT_UNAVAILABLE。`AgentServerIntakeUnderstandingGateway` 要求只处理待澄清队列头部；#153 规格要求逐项澄清不确定问题，并未规定两类问题的固定提问顺序。修正验收交互为回答当前页面问题，每类最多一次、最多两轮；最终仍须得到两个精确类别并一次确认创建两张独立工单。不改产品代码、模型或提示，不放宽五秒响应等待，不减少五场景要求。

用户额外授权 3 元后，累计预算上限为 8 元。旧四笔 PENDING 共 1.30 元保留，已结算 3.810222 元；本轮新增预留最多 1 元，调用前占用上界 6.110222 元。场景分段预算和调用/尝试/token 上限保持不变。入口 RunId 为 `issue174-live-02`，报告单独写入 `docs/delivery/issue-174-live-report-02.json`，不会覆盖旧失败报告。下方旧起点和命令属于第1版历史冻结记录，当前以本段及修订版场景清单为准。

[独立合成场景清单](issue-174-live-scenarios.json) 已在任何本轮供应商调用前冻结；下方保留 2026-09-01 静态设计作为历史，不再代表当前前置和预算状态。协调任务已明确授权本轮真实 DeepSeek 运行与正式交付。

规格依据：[GitHub #174](https://github.com/Stellogic/customer-agent/issues/174)、[父规格 #149](../specs/issue-149.md)、[票据镜像](../tickets/spec-149/issue-174.md)。2026-09-01 只读回读时，#174 仍为 OPEN，尚无对应 PR；本次以协调任务的明确派发为所有权依据。

## 本轮冻结配置

- 前置：#173 已正式完成，PR #210 已合入，Issue #173 已关闭；`issue173-final-20260903b` 的 RAG 64/64、浏览器 59/59 与六阶段检查均 PASS。
- 分层证据：#190 检索 64/64 已交付；#169 客户回答为用户接受的 `KNOWN_LIMITATIONS`，语义 35/48，不改阈值也不重跑；#170 已交付，但客服回答质量仍为 `NOT_EVALUATED`，不得外推。
- 模型与回退：全部生成接缝固定 `deepseek-v4-flash`，formal 模式开启，shadow 关闭，不允许 fixed fake 回退、换模型或随机重跑。
- 场景：固定运行 #173 A、D、B 与本票 L174-04、L174-05，共五项。它们覆盖自然语言多问题、不同调查终态、追加消息围栏、客户/客服知识引用和签收未收到的安全转人工。
- 上限：逻辑调用 100、供应商尝试 100、已持久化 token 200000；行动循环每代费用上限 10000 micro-USD、单次生成 16000 token、120 秒和 6 次 provider 尝试；整轮只执行一次。五段场景预留依次为 220000、120000、260000、280000、120000 micro-CNY，总和精确为 1 CNY；每段开始前回读累计费用，上一段 usage 未确认或已侵占下一段额度即停止。
- 预算：共享人民币账本调用前累计 `3.810222 CNY`、`0 PENDING`，用户总上限 `5 CNY`；本轮最多预留 `1 CNY`，最多剩余 `1.189778 CNY`。formal 指标按 micro-USD × 7 保守折算，客服按输入/输出 token 的 3/9 micro-CNY 计，受理未持久化 usage，因此每次按 `20000 micro-CNY` 上界结算。
- 输入和路径：只使用合成订单和仓库公开知识；真实 Chromium 可见操作经 Spring、PostgreSQL 和 LangGraph 到 DeepSeek。浏览器不直连 provider、Agent 私有接口或数据库。
- 结果边界：五项发布场景必须 5/5；L174-02 绑定现行 #173 D 的 23 小时低风险回复、五分钟候选与取消，不再沿用早期 12 小时候选描述；L174-04 固定绑定 `customer-delivery-help-v1` 与 `logistics-delay-v2` 并检查语义和 canonical 引用；L174-05 精确核对 `PACKAGE_SIGNED_NOT_RECEIVED` 原因。任何供应商、schema、安全或公开投影失败均诚实记 FAIL 并停止。发布场景不构成生产准确率、可用性或客服回答质量证明。

执行入口为 `pwsh ./scripts/issue174-live-acceptance.ps1 -ConfirmProviderSpend -RunId issue174-live-20260903a`。脚本先核对共享账本和工作树，登记唯一预算预留，再按正式锁协议运行；无调用的启动失败撤销预留，发生调用后仅在 PASS 且 usage 可信时结算；任一场景失败保留整轮 PENDING 预留并保存脱敏报告，避免未持久化尝试被漏计。最终证据写入 `docs/delivery/issue-174-live-report.json`，浏览器工件写入忽略目录 `.local/gate-evidence/<runId>/browser`。

## 2026-09-01 历史静态设计

以下内容保留当时的设计依据和未知项。涉及 `CODE_READY_NO_TESTS`、6 元上限、未授权、#169/#170/#173 未完成或 `NOT_RUN` 的陈述只描述当时快照，由上面的 2026-09-03 冻结配置取代。

## 已有真实接缝及不能借用的证据

### rag-layered-v2 静态承接

本轮把既有两份静态产物重放到最新 `origin/main`，不重做实现。已回读正式 #174 及相关 Issue/PR 状态，并以主线 [rag-layered-v2](../eval/rag-layered-v2.md) 为当前分层口径。#190 已随 PR #203 合入并关票；#169/#170 仍分别处于 Draft PR #208/#209，不能把其静态产物当作已交付回答证据。

| 层次 / 责任票 | 本票需核对的上游证据 | 不得替代或反向依赖 |
| --- | --- | --- |
| 检索层 #190 | 主线报告记录 64/64 样本完成，Recall@5 = 0.9444444444、MRR@5 = 0.8680555556，错误版本/范围/越权 Top-5 命中率均为 0%；PR #203 已合入、Issue 已关闭 | 该报告明确 `answer_quality=NOT_EVALUATED`。分数仅排序，空列表不等于语义拒答；不新增 logistic、reranker 或独立充分性模型 |
| 客户回答层 #169 | 自身真实产品回答的无答案拒答 precision ≥ 0.90、recall ≥ 0.85；结构合法性、引用真实性、语义充分性分项报告 | 问题与授权片段由同一次 DeepSeek 调用判断充分性并回答。不得以 #190 检索分数、固定模型或独立充分性实验替代 |
| 客服回答层 #170 | 自身真实 HUMAN 辅助路径的同一拒答门槛及三个分项报告；当前负责客服授权、草稿不自动发送且无业务副作用 | 不能拿客户路径成绩代替客服路径；不以 #173/#174 为本票的循环前置，不改变人工处理模式 |
| 汇总 #173 / 发布 #174 | #173 汇总确定性全栈，但当前仅 Draft/NOT_RUN；#174 核对以上三层分别交付的版本化证据，并经真实浏览器→Spring 验证发布场景 | 本票不重建检索/回答评测器，不成为上游交付前置，不把五组发布候选当完整质量样本集 |

检索层当前绑定主线 [分层验收记录](issue-190-layered-validation.md) 与 `rag-layered-v2`；#190 的回答质量仍未评估。回答层的报告、冻结样本映射及产品版本继续为空，须待 #169/#170 正式交付后绑定其协议版本、完整计数、源码版本、模型、prompt/schema、调用/重试/费用与失败分类。版本不符或证据缺失不能直接沿用 PASS。

只有检索、模型、解析均成功且回答明确说明资料不足才计正常拒答；允许说明或必要追问，**不因资料不足自动转人工**。检索/供应商/格式故障单列，保留完整样本分母，不能算正确拒答或删失败后报通过。结构合法、引用真实均不能单独证明语义充分；逐字引文需确有授权来源，不新设单条 24 字符限制，总体输出上限仍待冻结。个案事实和副作用继续由 Spring 权威控制，HUMAN 辅助失败不自动发送或改变处理模式。

保留 #189 原题、标签、哈希、数值目标与旧协议，也保留旧 FAIL、STOPPED 和全部试错费用；不回算旧成绩，不读验收错题调 prompt/选参，不复制 #190 账本。`rag-layered-v2`、场景重命名或新 run 都不能重置预算：历史累计消耗、在途/未知费用与本轮潜在消耗共同受项目 **≤6 元** 约束。主线 [#190 c6 记录](issue-190-c6-beta-strict.md) 保存的最近归档协调快照为累计上界 **0.620805 CNY**、未结算 **0 CNY**，对应快照时点的算术余量为 **5.379195 CNY**；该记录明确不是 live 账本查询，因此不能解释为当前可用额度或本票授权。本静态阶段新增费用 0、#174 预留额度 0；未来执行前仍须核对原账本。USD micro 记录必须按冻结计价/换算口径汇入同一人民币账本，未对账不得启动付费。

### 原始基线接缝观察

所有业务动作必须来自真实 Chromium 中的生产构建 React 可见控件，经同源 Spring API/SSE 到真实 PostgreSQL、LangGraph 和 DeepSeek。响应监听只观察 UI 动作产生的真实响应；浏览器不请求 provider、Agent 私有接口或数据库。fixture 只准备合成输入事实，不预写工单、消息、代次、提案、审批或执行结果。

以下路径是**基线源码观察**，不是新 API 提案或已运行证明。最终接缝须在 #173 与 #172 集成后重新核对。

| 接缝 | 基线证据 | #174 使用边界 |
| --- | --- | --- |
| 自然受理与确认 | `frontend/src/App.tsx`；`CustomerIntakeV2Controller.java`；`frontend/e2e/issue152.natural-language-intake.spec.ts`、`issue153.atomic-multi-issue-intake.spec.ts` | UI 提交进入 `POST /api/customer/v2/intakes`；澄清和确认进入 `POST /api/customer/v2/intakes/{intakeId}/messages`，现有 schema 为 `customer-intake-v2`；不发明 `/confirm` 接口 |
| 工单追加与公开投影 | `CustomerTicketV2Controller.java`；`App.tsx` | `POST /api/customer/v2/tickets/{ticketId}/messages`，`GET /api/customer/v2/tickets/{ticketId}` 及 `/events`；现有 schema 为 `public-conversation-v2`。只读真实代次、消息、流状态和待审批投影，不把客户端文案当 Spring 权威结论 |
| SSE | `CustomerTicketV2Controller.java` 的事件白名单 | 观察 `CUSTOMER_MESSAGE_ACCEPTED`、`AGENT_PROCESSING_TERMINATED`、`AGENT_REPLY_CONTENT_DELTA`、`AGENT_REPLY_COMPLETED`、`TICKET_HANDED_OFF` 等实际事件；不要求每个场景出现所有事件，也不注入内部事件制造结果 |
| 客户引用 | `App.tsx` 的 `Sources`；`ticket/TicketModels.java` 的 `PublicMessage` | 当前来源是固定三项文案，公开消息只有作者/正文/时间，不能证明 #169 真实条目引用。客户来源与版本投影待 #169 及 #190 集成，引用读取路由/字段待接，不猜接口 |
| 既有 live 脚本 | `scripts/deepseek-customer-communication-acceptance.ps1`、`scripts/issue129-acceptance-plan.psd1` | 可参考显式费用确认、固定模型、隔离资源、失败终止、脱敏记录；旧场景、预算和结果不能直接继承给 #174 |
| 既有指标 | `agent/src/baseline_agent/formal_mode_metrics.py`、各 `deepseek_*_model.py` | #129 聚合从调查代次取行动/判断/沟通指标，过滤特定沟通模式；不含 intake 全链统计，不提供完整分场景 token/结构正确率与全程事前额度控制。不得把缺失统计记为 0 或当作本票硬预算证明 |

后端控制器位于 `backend/src/main/java/com/stellogic/customeragent/ticket/`。`frontend/src/liveBrowserTestClient.ts` 的 cookie fetch helper 不是真实 Chromium 证明。`scripts/deepseek-formal-acceptance.ps1` 的 API smoke 只能补充诊断；`issue159.public-reply-stream.spec.ts` 预写代次并调用内部 publish，`issue124` 的部分场景预置澄清或精确匹配固定回复，均不能原样标为 #174 live 覆盖。不得复用其中的前端成功 mock、内部事件注入或结果 fixture。

## 五组候选场景

JSON 保存具体合成输入、fixture 语义、动作和预期，是候选清单而非可执行测试。每组建议一次；预先约定的澄清和追加包含在一次中，不允许失败后增加样本追求通过。文案只断言业务含义与事实，不逐字匹配模型生成文本，不规定工具调用顺序。

| ID | 高价值问题 | 预期公开结果 | 明确保留的限制 |
| --- | --- | --- | --- |
| L174-01 | 同一订单未收货且疑似重复扣款 | 确认前无正式工单；不确定扣款先澄清；一次确认得到两个不同工单和共同受理记录，刷新仍可分别进入 | 两个 ID 不证明全部事务竞态；两票都进入模型链路的调用必须计入总量，不能漏掉未打开的一票 |
| L174-02 | 已核实的短延迟，不满足补偿资格 | 自然受理后，经合法调查形成有事实依据的无补偿回复；无待审批提案，不声称赔付 | fixture 使用生效规则下明确不符合补偿的事实；不能以模型置信度、固定模板或“一定用了几个工具”判充分 |
| L174-03 | 长延迟符合调查条件，生成中客户追加消息 | Spring 接受追加并推进代次；旧代次后续公开片段/副作用被围栏，最新消息持久存在；最终最多显示当前有效的待审批投影，不显示已批准/已执行 | 必须观察真实旧代次在途与新消息接受的重叠；缺少稳定时序或拒绝证据则待接/未验证，不能以新代次数字冒充迟到写入拒绝证明 |
| L174-04 | 分别承接客户回答与客服辅助的规则回答、无答案说明及真实引用 | 有依据回答展示真实授权来源；资料不足时说明或追问。客户只看友好来源，HUMAN 负责客服看到授权建议但不自动发送 | 从 #169/#170 各自冻结的有答案/无答案样本绑定产品场景；不新增评测题集，不合并两条路径成绩。等 #190/#169/#170/#173，固定 Sources 文案不能算引用 |
| L174-05 | 权威事实存在冲突，仍要求自动赔付 | 真实模型调查不能形成充分事实；Spring 转 HUMAN，客户见安全说明且无待审批或执行成功；刷新仍保留人工状态 | 冲突 fixture 与真实失败路径需 #160/#173 确认；不能仅点“转人工”代替模型失败/事实冲突转人工，也不预写 HUMAN |

L174-02 与 L174-03/05 通过不同业务证据需求与终态覆盖不同合法调查路径。若需证明实际行动路径不同，只能在获准的脱敏证据中比较受控行动类别；公开终态不同本身不是工具路径不同的证明。该证据字段目前待接，不能保存原始工具入参/结果或思维链。

统一安全预期：Spring 校验业务证据权限、来源、时效、冲突及风险必需事实，并决定资格、金额、状态和副作用；知识资料的语义充分性由生成回答的同一次 DeepSeek 调用判断，不另设充分性模型。模型不能批准或执行补偿。正向场景被转人工应判预期不满足，不能因为“安全”就通过；L174-05 只有预期权威事实冲突触发的安全转人工才满足本场景，不是 L174-04 的正常资料不足拒答，供应商失效导致的转人工也不能冒充它。

## 调用前待冻结事项

项目共享 6 元上限及分层质量门槛已明确；已记录的 0.620805 CNY 仅是主线归档快照，当前 live 账本、执行配置、回答证据绑定与实际可用余额尚未冻结，JSON 中未知值使用 `null`。静态阶段的 0 次/0 费用不能作为未来 live 预算。缺任意一项，保持 NOT_RUN。

1. 受测 head/base、最终场景与 fixture 版本、各场景允许的澄清/追加轮数；#173 正式证据及原生前置完成状态。
2. DeepSeek 精确模型名称及所有接缝的 formal 模式（受理、行动、业务判断、客户沟通、客服辅助）；关闭 shadow 和 fixed fake 回退。客服辅助的最终模型及 prompt/schema 同样待绑定 #170 已交付配置。基线 `deepseek-v4-flash` 仅是已有代码值，不是本票冻结选择，更不是供应商当前可用性证明。
3. 各接缝 prompt/schema 版本和产品投影版本。基线行动为 `investigation-action-v2`，判断为 `investigation-judgment-v1`，沟通 prompt 为 `customer-communication-v2` / schema 为 `customer-reply-v1`；intake 的内联 instructions/`customer_intake_understanding` 尚缺独立冻结版本。本票不新增协议，以后冻结时记录对应已批准版本/源码提交，不复制原始 prompt。
4. 在项目历史累计共享 ≤6 元内，核对原账本及剩余额度后冻结整轮/各场景的逻辑调用、供应商尝试、token、成本子上限、wall-clock/首字/完成超时、计价与人民币换算依据、失败未知费用处理。USD micro（1 USD = 1,000,000 micro）只作记录单位，不能形成另一份额度。所有后台调用、澄清、重试和取消在途均计入，不沿用 #129 的 43/49/400000 等旧值，不因协议换名重计。
5. 全链计量与下一次调用前限额检查的已实现接缝，包括 intake、并行两票和取消在途；本阶段不实现公共预算设施。旧脚本场景结束后的聚合比较不等于整轮事前硬上限，缺此能力不得开启付费运行。
6. 核对 #190 检索报告与 #169/#170 分别交付的真实回答报告，绑定其完整样本分母、结构/引用/语义及拒答判定口径与发布场景。precision ≥ 0.90、recall ≥ 0.85 不是待选新阈值；不得改题、删失败、看错题调 prompt/选参或看结果降门槛。全部发布场景仍须满足公开行为和安全不变量，不使用额外付费 LLM judge 或独立充分性调用。
7. 协调任务单独授予的执行及费用许可、隔离资源身份与合法锁入口。不能从时间到 10 点、额度重置、锁 FREE 或存在 `-ConfirmProviderSpend` 推导许可。

## 调用和费用记录设计

只定义本票后续报告需保留的证据，不实现存储系统、公共 schema 或 #190 实验账本。浏览器产品结果与运维脱敏计量分开：产品结果只能来自 Spring 公开响应/SSE/页面；指标可由获准的后端只读聚合提供，不能将 checkpoint、数据库连接或 Agent 地址送入浏览器。

| 记录项 | 口径 / 取得方式 | 当前状态 |
| --- | --- | --- |
| 运行与场景 | 脱敏 run/scenario 标签、head/base、冻结配置版本、开始/结束时间、状态与停止原因；不保存真实 ticket/thread/order 标识 | 待运行 |
| 产品观察 | 已确认工单数、受控事件类型/相对顺序、公开状态、断言结果、语义核验结论；仅保留必要脱敏截图，网络体内存检查后丢弃 | NOT_RUN |
| 逻辑调用 | 一次实际受理/行动选择/业务判断/客户沟通/客服辅助调用计一个逻辑调用；知识充分性判断与回答若在同一次调用完成，只计一次，不拆项重计。重试仅增加供应商尝试；按场景及接缝统计 | #129 部分字段存在；intake、客服辅助、跨票关联待接 |
| 供应商尝试 | 每次真正发出请求均计一次，失败/限流/取消在途不删掉；归属不明的差额需保留且停止，不能归零 | 待全链计量 |
| token 与成本 | input/output/cache/total token 分开记录；无 usage 为未知，不等于免费；记录估算与供应商可得计费的区别及定价版本 | 现有 adapter 部分记录可参考；完整聚合待接 |
| 延迟 | UI 提交至首个公开内容片段、提交至公开终态、每调用/尝试耗时；没有片段或终态记录缺失/超时，不填 0 | 待运行及冻结超时 |
| 结构正确率 | 成功解析且满足冻结 schema 的最终逻辑输出数 / 要求结构输出的逻辑调用数；调用失败无有效输出也在分母。另列供应商返回结构错误、无响应及重试成功数量，避免隐藏重试失败 | 分类/分母尚待完整接线；样本 0 时为 null，不为 100% |
| 分层 RAG 质量 | 单独引用 #190 的检索指标；#169/#170 各自列结构合法性、引用真实性（含逐字引文）、语义充分性、拒答 precision/recall、完整样本数与故障分类。格式故障不算正确拒答，正常无答案不等于空检索或转人工；不将两个路径平均后掩盖未达标 | #190 检索报告已绑定，`answer_quality=NOT_EVALUATED`；#169/#170 回答报告仍待绑定且 NOT_RUN，指标 null；#173 仍为 Draft/NOT_RUN |
| 安全不变量 | 是否出现越权批准/执行、错误事实自动处理、旧代次公开写入、敏感数据进入公开响应或日志；逐项 PASS/FAIL/NOT_RUN，不能用平均分掩盖违规 | NOT_RUN |

禁止持久化原始 prompt、供应商响应、reasoning/思维链、密钥、会话凭据、原始工具载荷、checkpoint 和未脱敏 HAR/trace/日志。合成输入可以保存在场景清单，但不能把运行时完整 prompt 或回复当作输入附件保存。发现敏感数据时记录类型及脱敏位置，不把泄露内容复制到报告。缺少安全采集途径即阻塞相关证据，不能省略后声称通过。

计量结束需对齐每场景小计、整轮总计与项目原账本累计，覆盖全部调用及停止时在途消耗。#169/#170 等已发生费用不得因本票汇总重复入账或归零重计；采用原记录关联，新增调用才新增消耗。超时、取消或 usage 缺失时保留“成本未知”，停止继续发起调用；不得用估算小于上限证明未知费用未超限。

## 停止条件与后续接线

| 条件 | 结果与动作 |
| --- | --- |
| 未获运行/费用许可、#173 未完成、模型/prompt/schema/场景/上限未冻结、预算或引用等关键接缝缺失 | **NOT_RUN / BLOCKED**，不启动栈、不探测 provider、不进行付费试跑；报告协调任务 |
| 项目原账本未对齐、累计/在途费用未知、6 元共享余额不足，或任一上游分层证据缺失/版本不符 | **NOT_RUN / BLOCKED**；不把新协议当新额度，不替上游制造 PASS，不反向阻塞 #190/#169/#170 的独立交付 |
| 授权后锁 BUSY / RECOVERY_REQUIRED | 按 [锁规范](../agents/test-gate-lock.md) 立即停止 / 升级协调；不轮询重试、不删除残留。本阶段连锁查询也未运行 |
| 供应商不可用、余额不足、认证失败、限流、预算耗尽、模型/契约错误、超时或证据不足 | 该次发布验收 **FAIL/INCOMPLETE**，停止后续场景；保存已发生调用和未知费用。最多执行冻结配置内既有尝试，不增设外层重试、不改模型/预算、不回退 fixed fake |
| 安全不变量失败、公开投影与持久状态矛盾、非预期自动成功 | **FAIL**，立即停止；产品缺口回对应实施票，本票不修产品 |
| L174-05 的已冻结冲突触发预期 HUMAN | 仅该场景预期达成；仍需所有指标完整、无供应商异常和违规。不能把任意运行失败统称“转人工通过” |

后续接线责任：#190 提供检索层合格证据，#169/#170 分别提供客户/客服真实回答拒答门及结构、引用、语义证据；#174 汇总核对并承接真实浏览器发布，不新增上游反向依赖。#158/#159 的真实生成和流式围栏需要稳定公开观察；#160/#161 保留业务事实校验与有依据回复责任；#173 保留确定性全栈前置。受理全链计量与共享预算由协调任务指定负责者，不另建体系。#172 收缩后的接口及 #170 辅助接缝只按实际已交付代码绑定，当前不猜新路由。

获准后的正式顺序仍遵守 AGENTS.md：完成缺口及聚焦验证、格式/类型预检、一次集中风险审查、Standards/Spec、最终完整 `pwsh ./scripts/check.ps1 -Issue 174`，再按批准的流程合入/关票和 main 回读。该完整门禁不自动执行 #174 live；现有 live 脚本要求继承合法锁令牌并单独确认费用，具体串接入口尚待协调确认，不能伪造令牌或擅自改入口。live 证据与完整门禁都必须对应最终受测版本，任何受版本控制修改后不得复用旧门禁。以上为未来要求，本阶段不执行、不合并；只允许推送 Draft PR 保存静态产物。

## 阶段记录与贡献边界

- 运行记录：项目测试、formatter/lint/类型、构建、Docker、浏览器、产品 API、模型、锁、完整门禁均 **NOT_RUN**；只执行过 Git 差异空白检查 `git diff --check`，未触发项目格式工具。实际模型调用 0，供应商尝试 0，费用 0；没有正确率、延迟或运行成功率。
- 审查记录：2026-09-01，相对最新 `origin/main` 的 Standards / Spec 首轮静态 CR 共同发现一处分层证据状态矛盾；修复后两轴完整静态复审均 **PASS，0 项发现**。审查代理只读源码与文档，未运行任何测试、校验或门禁；静态 PASS 不代表任何运行门槛通过。
- 本阶段贡献：依据真实产品接缝整理五组合成验收候选、调用前冻结项、计量缺口、停止条件与证据边界。不能描述为真实 DeepSeek 验收通过、RAG 质量达标、生产稳定性提升或交付 #174。
- 本轮增量：绑定主线 #190 检索证据，明确 #169/#170/#173 仍未形成可继承的通过证据，并记录共享预算的最近归档快照。运行仍全部 NOT_RUN、无交付许可。
- 最终正式报告必须分别列出：#173 的确定性全栈证明、#174 的真实 DeepSeek 证明、未验证的生产级范围。学习项目的少量合成场景不外推为生产准确率、可用性或承载能力。
