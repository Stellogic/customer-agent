# #173 确定性全栈验收

## 状态与范围

**当前状态：FINAL_GATE_READY。** 2026-09-03 已在最新 `origin/main@4716a94fc670d19e3333acd79098bb55c8fb42fb` 上完成主线同步和聚焦复验：A–E 共 5/5 通过，F 的 prepare、before-due、resolved、resolved-restart、before-close、closed 六阶段全部通过；正式测试锁与隔离 Compose 资源均已释放。最终完整门禁仍待最终 HEAD 执行。

本轮遵循“尽快跑通学习项目”的最小范围：只把 #173 的验收脚本对齐 #172 已合入的 v2 契约，并补齐集中风险审查指出的真实流事件、旧代次隔离、HUMAN 辅助失败无副作用和证据留存断言；不修改产品实现，也不新增 RAG 实现或评测框架。#168、#169、#170、#171、#172 均已关闭且成果进入当前主线；#173 复用已归档的检索、客户回答、HUMAN 辅助接线与聚焦工程、状态画廊和 v2-only 证据，并由最终门禁统一回归现有浏览器计划。

RAG 证据保持原边界：#190 检索层已通过冻结 64 题质量门；#169 客户路径完成 48 题真实模型运行，结构/Spring 接受 42/48、独立语义 35/48，其中有答案 28/36、无答案正确拒答 7/12，用户已按学习项目定位接受为 `ACCEPTED_WITH_KNOWN_LIMITATIONS`，不得写成质量门槛 PASS；#170 的人工辅助契约、权限、失败隔离和人工审阅已正式交付，但没有独立真实模型质量结果，保持 **NOT_EVALUATED / KNOWN_LIMITATIONS**。#173 不花共享模型预算补跑，也不把固定假模型冒充真实回答质量。

- 日期：2026-09-03。本轮已重新 fetch 最新 `origin/main`，并回读正式 #149、#173 及直接依赖状态；不沿用旧主线快照判断当前契约。
- 分支：`codex/issue-173-static-acceptance`，PR #210 已转 Ready 并完成集中风险审查；继续收敛既有工作，不新建重复实现。
- 原两文件阶段仅拥有 `frontend/e2e/issue173.full-stack.spec.ts` 与本文件。协调随后明确授权最小公共接线：`scripts/issue80-acceptance.ps1` 的本票阶段调度、`scripts/browser-acceptance-plan.psd1` 的本票串行登记、`compose.yaml` 仅 `browser-acceptance` 服务的本票 phase 环境传递，以及本票专属 helper/e2e。当前新增 `scripts/issue173-clock-acceptance.ps1`、`frontend/e2e/issue173.auto-resolution-clock.spec.ts`、`frontend/e2e/support/issue173-intake.ts`；仍不改产品或 #190 模型、Agent、后端服务定义。
- 已完整读取本 worktree 的 `.agents/skills/implement/SKILL.md`。本轮已在正式测试锁内完成前端聚焦检查、A–E 真实浏览器及 F 固定时钟阶段；未调用真实模型，RAG 复用上游归档证据，完整门禁仍未运行。
- GitHub 元数据只读查询和 Git fetch 不属于产品 API 验收。沙箱 gh 401 后，宿主上下文读取成功，未据此判断用户令牌失效。
- GitHub Actions 按项目决定关闭；正式交付仍须完成集中风险审查、最终双轴审查、一次最终完整门禁、PR 合入、关票和 `origin/main` 回读。

事实源：[实施票 #173](https://github.com/Stellogic/customer-agent/issues/173)、[正式父规格 #149](https://github.com/Stellogic/customer-agent/issues/149)。本次已回读 2026-08-31 发布的 `rag-layered-v2`；仓库[父规格镜像](../specs/issue-149.md)中的旧检索拒答口径不能覆盖正式修订，本票不修改他人拥有的规格镜像。旧规格中的“普通 CI”按现行 AGENTS.md 解释为本地确定性检查配置；旧规格“门禁前不得提交”已由现行“开发期间允许频繁提交”规则替代，门禁前仍禁止合并和关票。

## 已有接缝与本轮源码

复用现有 Playwright、`support/auth.ts`、`support/browser-context.ts` 与 `support/database.ts`，不加依赖、不设计新 API。全部业务动作由生产 React 的可见控件发起；响应监听只读取这些动作产生的真实 Spring 响应，不从测试侧调用内部 Agent API 推进结果。

订单 fixture 每次生成 `ORDER-ISSUE-173-<UUID>`，仅写入本场景独有的 `synthetic_order`：客户 `customer-demo`，支付 268 CNY，默认延迟 80 小时、可补偿额 268 CNY，使用现有 `delay-policy-v1`。D 仅将延迟改为 23 小时；E 仅将订单额度改为 30 CNY。物流状态等沿用当前迁移默认值。fixture 不创建工单、消息、处理代次、领取、提案、审批或执行结果，也不删除或重置共享数据。D/E 复用 `queryFixtureSql` 对真实业务结果作只读持久化断言，SQL 不推进业务。场景数据留在本次隔离数据库，未来由该次 Compose 卷清理收尾。

| 场景 | 新源码实际断言 | 静态依据与明确限制 |
| --- | --- | --- |
| A：受理与多工单 | 自然语言描述订单和两个问题；低置信问题先澄清；一次确认返回两个不同工单及共同受理 ID；刷新总览仍有各自入口；390px 无横向溢出 | 依据 `issue153.atomic-multi-issue-intake.spec.ts`、`issue157.order-ticket-groups.spec.ts`、`App.tsx`、`intake_model.py`。不把返回两个 ID 当作事务回滚、并行调度或授权隔离证明 |
| B：多轮与恢复 | UI 建单后等待真实 Agent 代次完成，核对 LangGraph 实际产生的开始/内容事件拼接结果等于持久化 Agent 公开回复；两个真实页面并发追加消息，至少产生一个被替代代次；等待当前代次完成后，旧代次有终止事件且没有 Agent 公开消息；断线恢复后两条消息仍各出现一次且保持原工单 | 依据 `App.tsx` 的 v2 消息/SSE 接缝和生产事件投影；正向流与并发代次均来自真实 LangGraph/Spring 运行，不调用内部 publish 制造结果 |
| C：跨角色闭环 | 自然语言人工意图经 Agent 转人工；客服领取后先通过生产 UI 发起知识辅助，确定性门禁缺少本地向量模型时明确显示检索不可用、公开消息数不变且人工 composer 可继续；随后完成公开回复、标准补偿和独立审批主链 | 依据 #170 正式辅助接缝、`issue163.persistent-support-replies.spec.ts`、`issue164.standard-compensation.spec.ts`。不把基础设施失败算资料不足或真实回答质量；不预造提案，不修改其他客服分配或订单额度 |
| D：五分钟候选与取消 | 用白名单问句“请解释物流状态”完成真实受理/调查；候选 `due_at-created_at=300秒`；刷新后的公开截止时间与持久化原值相同；点击真实取消按钮，验证候选身份、公开状态及刷新后持久化取消 | 依据 `AutoResolutionPolicy`、`TimeConfiguration`、`CustomerAutoResolutionController`、`AutoResolutionNotice.tsx` 与既有 SQL 读取 helper。不模拟前端计时，不预写候选。不覆盖到期解决、72 小时重开或恰好截止竞态 |
| E：同订单额度竞争 | 同订单通过两次 UI 确认建单，遇重复提示时客户明确选择新问题；两张 HUMAN 工单分别领取并提交标准提案；两笔待审合计 53.60、不预占；两个审批页面先取得提案证据，再成对放行真实批准请求，要求 200/409 各一；数据库只占 26.80、剩余 3.20、执行记录 1 笔 | 依据 #165 的 V41 额度视图、`order_allowance_smoke.py` 契约与既有客服/审批页面。只把输入准备和请求屏障放在测试侧，不复制底层锁序测试。ACTIVE 与 CONSUMED 互斥汇总，允许正常执行推进，不把批准或执行记录存在当作执行成功 |
| F：自动到期与72小时回复边界 | 两张真实 UI 工单各自等待 Agent 候选；外层分阶段重建固定时钟，4分59秒不解决、5分钟真实 scheduler 解决，同刻再次重启仍仅一次解决且截止不变；解决后72小时截止前1秒回复第一张原票并保留历史；截止时第二张关闭、回复创建关联新票 | `issue173.auto-resolution-clock.spec.ts` 与专属 PowerShell helper；每阶段新登录，现有 artifacts 卷交接 ticketId。浏览器不改 Spring 时钟、不直接调用内部接口、不预写结案结果；只覆盖此正向链和回复边界，不复制 #162 全部到期重校验/精确竞态矩阵 |

A–F 源码没有 `skip`、条件假通过、前端成功 mock 或 SQL 预写业务结果。2026-09-03 使用生产 React、Spring、PostgreSQL/LangGraph 与固定假模型/确定性执行配置在当前主线上运行：A–E 最终 5/5 通过，F 六个时钟阶段逐阶段通过。该聚焦结果不代替最终完整门禁，也不改变上述真实模型质量限制。

## #173 全部 AC 映射

以下 AC 编号按 GitHub #173 当前正文顺序；A–F 是本票新增的当前主线聚焦证据，其余项目复用已关闭实施票的正式证据，并在最终门禁中统一回归。

| AC | 要求 | 本轮映射 / 剩余缺口 |
| --- | --- | --- |
| 1 | 纯验收票，只加最高层场景、门禁与证据 | A–F、专属 helper 与获授权的最小公共调度/串行登记/phase 传递已写源码，不改产品 |
| 2 | 产品缺口回对应实施票 | 本文记录缺口归属；不修产品、不创建或改写其他票 |
| 3 | 同订单多问题、一次确认、并行、多轮、旧代次围栏 | A/B 覆盖本票主链；B 以两个真实页面并发追加并验证实际旧代次终止且不能产生 Agent 公开消息，复用 #153/#157/#159 其余原子创建、独立调度和直接迟到写入拒绝回归 |
| 4 | 真流式、断线恢复、事实充分、有依据回复、5 分钟、72 小时 | B/D/F 覆盖调查、恢复、候选、取消、到期和72小时边界；复用 #159/#162/#169 的流式、重校验和回答证据，保留真实模型已知限制 |
| 5 | 受理协助、持久化领取、人工回复、标准补偿、额度并发、审批、撤权 | C/E 覆盖跨角色主链和额度竞争；复用 #156/#163/#164/#165/#170 的受理协助、撤权、执行与失败隔离回归 |
| 6 | 按 rag-layered-v2 覆盖客户引用、客服辅助、资料不足、错误版本、冲突和检索失败 | 最终浏览器计划统一回归 #190 确定性检索场景；C 新增 React → Spring 检索基础设施失败路径并证明不自动发送。客户回答复用 #169 真实路径证据；HUMAN 辅助复用 #170 接线与聚焦工程证据，其独立真实回答质量为 **NOT_EVALUATED**，不冒称已有结果或重复付费运行 |
| 7 | 桌面/窄屏、loading/empty/error、状态画廊、开发中无成功 | A–F 覆盖本票主链桌面/窄屏；#171/#191/#193 的状态画廊、错误路由和开发中无写入已进入主线及最终浏览器计划 |
| 8 | 浏览器及日志不含密钥、权重/路径、内部地址、prompt/reasoning/工具/checkpoint | 复用现有安全投影、日志扫描和 #169/#170 发布边界；最终完整门禁统一回归，不在本票复制扫描器 |
| 9 | 唯一 Compose project/tag/端口/卷、完整门禁、证据 | 聚焦运行均使用唯一 Compose project/tag/端口/卷并完成自有资源清理；完整门禁会在删除 Compose 卷前把浏览器 artifacts 导出至 `.local/gate-evidence/<RunId>/browser`，最终运行仍待最终 HEAD 一次执行 |
| 10 | 只证明确定性配置，不能冒充 DeepSeek | 本轮仅证明固定假模型/确定性执行配置；#174 真实 DeepSeek 仍为独立发布验收 |

## 父规格测试条目 1–40 的完整索引

编号对应父规格 `Testing Decisions`。这是开发阶段形成的追踪索引；其中“待整合”表示不在 #173 复制场景，而由已经交付的对应实施票测试在最终门禁中统一回归。当前结论以本节上方 AC 映射和最终门禁结果为准。

| 条目 | 本轮 / 后续验收责任 |
| --- | --- |
| 1–4：单一接缝、两种模型配置、真实依赖、诚实失败 | A–F 已用生产 React → Spring、真实 PostgreSQL/LangGraph 和确定性配置完成聚焦运行；D/E/F 只读数据库结果作为不变量证据。真实 DeepSeek 归 #174，完整门禁仍待最终 HEAD |
| 5：可观察行为断言 | 本轮只断言产品动作响应、公开消息、投影、控件和持久化后的可见结果；不规定私有实现 |
| 6：自然语言各种受理入口 | A 低置信与多问题，B/C 单问题；多订单、订单歧义、自然语言确认、取消仍需整合 #152–#154 |
| 7：统一事务、共同受理不可变、幂等 | A 仅创建及恢复；全回滚、不可变和重试不重建仍需整合 #153，不使用 mock 回滚作为 DB 证明 |
| 8：受理恢复及七日边界 | 待整合 #155；A 的工单总览刷新不是受理会话七日恢复 |
| 9：受理协助权限 | 待整合 #156；不得借人工工单领取覆盖受理前授权 |
| 10：重复问题及混合新旧问题 | 待整合 #154；本轮独有订单避免历史重复干扰，不声称测了去重 |
| 11：独立生命周期/模式/负责人及并行 | A 只证明两个入口；待整合 #157，须在打开各详情前证明各工单已独立启动 |
| 12：同订单最小摘要授权 | 待复用 #157 的实际产品授权契约，不读取兄弟工单完整对话作 Agent 共享输入 |
| 13：连续追加、澄清恢复、摘要与历史分页 | B 两次追加及恢复；长对话摘要/分页、澄清继续仍需 #158 的稳定最高层场景 |
| 14：生成竞态与迟到动作 | B 用两个页面并发追加，回读实际 `SUPERSEDED` 代次与终止事件，并验证当前代次完成后旧代次没有 Agent 公开消息；直接迟到写入 409 与其余细粒度时序复用 #158/#159 |
| 15：逐片流式各状态 | B 从真实 LangGraph 调查回读开始与内容事件，并验证内容按序拼接等于最终公开回复；不通过内部 publish 制造正向结果，其余状态矩阵复用 #159 |
| 16：受控进度字段 | 待真实 SSE 网络证据与非法字段检查 |
| 17–20：有依据回复、充分性正负、低风险白名单 | B 正常补偿路径仅为部分场景；继续依赖 #160/#161 的真实证据集合与 Spring 结论约束，不反推事实充分 PASS |
| 21：五分钟可控时钟与重新校验 | D 为候选300秒/取消；F 补真实建单后4分59秒、5分钟和同刻重启的正向调度源码。到期条件变化及恰好截止的回复竞态不在本次增量覆盖，不能以 F 正向结果替代 |
| 22：72 小时及重开 | F 已通过解决后72小时截止前1秒同票重开、历史保留、公开旧提示消失，以及独立样本恰好截止关闭并创建关联新票的聚焦验收 |
| 23：持久化负责客服 | C 覆盖领取、刷新和释放；重新登录、SSE 恢复、交回 Agent、解决/关闭及撤权待补 |
| 24：HUMAN 公开回复授权及辅助无副作用 | C 验证负责客服的辅助失败不会新增公开消息且人工回复仍可继续；非负责客服拒绝复用 #170 已交付回归 |
| 25：完整客服工作台 | C 覆盖详情、回复、标准补偿；订单分组/时间线与 #170 草稿待集成 |
| 26：审批回归 | C 独立审批人正向批准与不含人工对话；禁止自审、提案失效、驳回、执行和待对账待补 |
| 27：订单级额度并发 | E 新增两提案与两批准竞争，验证待审不预占、只占一笔及订单剩额。成对放行是浏览器请求屏障，不保证服务端线程精确交错；内部锁序仍依赖原 #165 测试。执行重放、失败释放和合法多笔组合未在本票覆盖 |
| 28：知识解析/版本 | #166 已有来源；待 RAG 集成，旧引用审计与无效条目验证不能仅用展示 fixture |
| 29：Embedding 契约 | #190 已正式交付并进入 main；后续接线复用其真实模型准备、编码与冻结 revision 证据，本票不重跑或改写 |
| 30：ONNX 一致性与资源 | #168 已正式交付并进入 main；后续复用其独立对照证据，不由 #173 外推 ONNX 质量结论 |
| 31–32：中文质量和消融 | 复用 #189 不变的冻结资产；#190 按 rag-layered-v2 报 Recall@5、MRR@5 与非法候选命中，#169 报客户路径实际回答拒答指标；#170 HUMAN 路径没有独立真实质量结果，保持 **NOT_EVALUATED**。#168 三路消融/可选 ONNX 保持独立；不混抄分路指标、不用检索空列表充当正确拒答 |
| 33：检索前授权过滤 | #190 检索层与 #169/#170 消费路径均已交付；本票复用其归档证据，并由当前完整门禁回归共用适配、服务端过滤、公开来源投影和当前版本 |
| 34：RAG 故障 | C 已覆盖 HUMAN 辅助检索不可用的真实 React → Spring 接缝：明确报错、不新增公开消息且人工处理继续；客户路径分类复用 #169 已归档证据，HUMAN 路径只复用 #170 接线与聚焦工程证据，不把故障或合成输出算成真实拒答质量 |
| 35：扩展、迁移、恢复及隔离 | #190 已交付数据库侧证据；本轮不重跑 Docker、迁移或模型操作，后续最高层门禁只复用并核对集成结果 |
| 36–37：高保真、键盘、焦点、窄屏、语义 | A/B/C 少量页面行为；完整原型比较及状态矩阵仍依赖 #171（含 #191/#192/#193） |
| 38：开发中无业务副作用 | #193 已交付并进入当前主线；本票由完整门禁回归其入口与无写入约束，不把 notice 文案当无写入证明 |
| 39：敏感信息边界 | 待网络、SSE、产物、日志全链收集；现有 `frontend/src/sensitive-content-patterns.json` 可复用但不能据此声称已包含全部新 Embedding 模式 |
| 40：完整规范化门禁 | 聚焦前端、B、C 与分层证据契约均已 PASS；最终完整 `check.ps1 -Issue 173` 尚为 **NOT_RUN**，通过前不合入 |

## rag-layered-v2：检索门与回答门分层承接

2026-08-31 只读回读 [#149 正式修订](https://github.com/Stellogic/customer-agent/issues/149)、[#173 AC6](https://github.com/Stellogic/customer-agent/issues/173)、[#190](https://github.com/Stellogic/customer-agent/issues/190)、[#169](https://github.com/Stellogic/customer-agent/issues/169) 和 [#170](https://github.com/Stellogic/customer-agent/issues/170)。原始正文、`updatedAt` 与 URL 保存在本 worktree `.local/rag-layered-v2/issue-<编号>.json`。本节仅更新自有验收计划，不修改产品、公共接线、规格镜像或 A–F 源码，不占用 #190 运行窗口；没有新增测试、格式、类型、构建、模型调用或锁查询。

| 层次及负责人 | 独立门槛与证据 | #173 承接方式及不能替代的结论 |
| --- | --- | --- |
| #190 检索层 | 同一完整冻结样本集上，有答案 Recall@5 ≥ 0.90、MRR@5 ≥ 0.75；错误版本、不适用范围、越权条目 Top-5 命中率各为0%。无答案题也执行并完整记录合法候选，另分无匹配、权限失败及基础设施失败 | 接入其版本化检索报告和真实授权候选契约。分数只用于排序；不以空列表、相似度、logistic 或 reranker 分数判充分性，不要求额外充分性模型/云调用；检索门不承担实际回答拒答门 |
| #169 客户实际回答层 | 问题与授权片段交给负责回答的同一次 DeepSeek 调用，判断充分性并回答；该路径拒答 precision ≥ 0.90、recall ≥ 0.85，结构合法性、引用真实性、语义充分性分别报告 | 确定性全栈验证客户可见来源、分支及 Spring 权威事实边界；复用该路径的真实回答证据，不能用检索报告、独立充分性实验或固定模型替代 |
| #170 HUMAN 辅助层 | 目标契约仍要求同一次 DeepSeek 调用判断知识回答/草稿充分性，并独立报告拒答门和三项分项；当前归档只有接线、权限、失败隔离、人工审阅及聚焦工程证据，真实质量为 **NOT_EVALUATED** | 回归当前负责客服权限、草稿必须人工审阅/发送、失败可继续人工处理；任何结果不得自行公开发送、改变处理模式或产生业务副作用，不从工程证据外推回答质量 |
| #173 / #174 汇总 | #173 汇总确定性全栈契约，#174 管理有预算的真实发布证据；#169 保留客户路径原始回答质量，#170 当前只保留 HUMAN 工程接线证据 | 两种配置不形成两套产品契约，但证据不能互换。#190 检索门与完整工程交付可独立完成，回答门不反向阻塞 #190；正式修订本身不等于任一层 PASS |

后续最高层场景按以下可观察结果分类；这是验收承接，**不是已实现或已运行的 RAG 测试**，不在本票新造响应字段、问答搜索入口或评测框架：

| 情况 | 未来确定性场景断言 | 质量统计边界 |
| --- | --- | --- |
| 检索返回合法相关片段 | 先核对授权/发布/版本/范围过滤后的候选及来源元数据，再观察实际回答分支；候选存在不能直接亮出“可以回答”或“知识充分”结论 | 只记录检索结果，不能计正确回答或拒答 |
| 有依据回答 | 一般规则说明附真实授权来源；订单、支付、补偿资格/金额/执行等个案事实继续取 Spring 权威结果。客户只见友好标题/更新时间/必要提示，客服按原权限可见版本/内部标识/引文；不泄漏不可见内容或内部原始载荷 | 结构、引文是否来自实际授权原文、语义是否足以支持回答分别判定；确定性通过只证明契约，不给真实模型质量打分 |
| 正常资料不足（包括有合法候选但不能回答所问） | 检索、模型与解析均成功，回答明确说明不足，必要时追问；不能仅因不足自动转人工。正常无匹配与检索故障保持不同状态，不伪造来源 | 只有合法资料不足回答可计作拒答，再对照冻结标签判断正误并统计 precision/recall；空列表本身、固定假模型输出或错误码都不是正确拒答证据 |
| 格式、检索/索引/Embedding/数据库或供应商故障 | 分别保留故障类别，不伪装正常无答案、不补写无来源内容；按原有限重试/受控修正规则仍失败时停止、保留进度并处理人工接管。HUMAN 辅助保持原模式，不阻碍客服继续处理 | 故障/格式失败不计正确拒答；完整样本数与失败数单列，不删除失败样本再宣称整体验收通过。具体执行协议先冻结，不在此臆造分母处理公式 |
| 错误版本、越权及知识冲突 | 错误版本/范围/越权条目不能进入结果或来源投影。知识与个案事实冲突时 Spring 事实优先；客户解释个案状态并记录冲突，HUMAN 辅助按受控失败处理，不自动发送或切换模式 | 非法候选命中与回答引用问题分别归对应层；不能把“检索到了片段”作为冲突已解决证据 |

证据承接保留最小必要清单：`rag-layered-v2` 报告版本、冻结数据哈希/原标签、受测源码与模型 revision、完整样本总数与各类失败计数；回答路径还需其模型、prompt/schema、协议/总体输出上限、调用/重试/费用和三项分项结论。真实验证前由消费票冻结执行协议与预算，不看验收错题调 prompt 或选参；未知项标 NOT_VERIFIED，不由 #173 编造数值或替消费票运行模型。

保留 #189 原题、标签、模型协议、内容哈希、数值目标、旧验收协议及全部历史 FAIL/STOPPED；新旧口径不等价，不回算旧成绩、不把规格修订写成旧实验成功，也不重置累计付费预算。#166/#189 已交付职责和关闭状态不变；#168 仍拥有消融与可选 ONNX。本次不为新合同增加单条引文24字符上限；继续核验授权来源、逐字引文真实性及冻结的合理总体输出上限，不追改旧实验合同。仅使用模拟问题、工单及示例知识，不新增外发审批、分类或脱敏系统。

本节对应能力已由 #190/#169/#170 分层交付并进入主线；#173 只汇总和回归，不复制实现或重复付费运行。#190 的检索质量结果可以标 PASS；#169 客户回答质量按归档结论标 `ACCEPTED_WITH_KNOWN_LIMITATIONS`；#170 HUMAN 真实回答质量没有独立归档结果，保持 **NOT_EVALUATED / KNOWN_LIMITATIONS**。

## 前置与后续接线

原静态阶段（2026-08-31）读取 #173 原生 `blocked_by` 的快照：#150/#162/#165 为 closed；#168/#169/#170/#171/#172 为 open，当时 #190 为 Draft PR #203。此快照不是 rag-layered-v2 发布后的重新解阻结果，不能把静态准备作为它或下游的正式交付证据。后续集成必须重新查询原生状态，不能拿旧快照永久放行。

2026-09-03 本轮只读回读：#168、#169、#170、#171、#172、#190 均已 CLOSED 且对应成果已进入最新 main。上游正式证据用于本票分层汇总；A–F 的当前主线浏览器证据和最终完整门禁仍由 #173 自己承担。

1. #168/#190/#169/#170 及其余原生前置已正式交付；从当前 main 对照消费契约并复用各自已有证据。#173 不实现或抢占共享适配，也不为 #170 补跑尚未归档的真实模型质量。
2. #172 的 v2-only 收缩已进入 main；本轮已把客户建单、取消自动解决、解决后继续与关闭后关联新票对齐当前 v2 契约，客服和审批仍使用各自正式工作台路由。
3. 发现产品功能缺陷时回对应实施票；#173 不顺手修产品，也不靠 `skip`、SQL 预写结果或内部事件制造“通过”。
4. 两个 #173 spec 已登记到 `Serial`：普通串行组运行 A–E；时钟 spec 由本票 helper 固定 `workers=1` 执行六阶段。聚焦运行已通过，最终完整门禁会按同一公共计划再执行一次。
5. 最终双轴确认后读取锁状态；FREE 时只运行一次完整 `pwsh ./scripts/check.ps1 -Issue 173`。BUSY 立即停，RECOVERY_REQUIRED 升级协调。
6. 未来运行使用规范脚本生成唯一 RunId、Compose project、镜像 tag、端口及卷，保存受测 HEAD/base、工作树状态、确定性模型配置、结果、耗时、浏览器/日志证据与自有资源清理记录。只记录可公开配置，不能在报告中复制密钥或未脱敏日志。通过后才进入合入、关闭与 `origin/main` 回读。

## #162/#165 原两文件阶段：接缝核对历史

本阶段从上一阶段提交 `00d2a6a4530919c1e49b3ffe80aa92aec958f2bd` 继续，只增加 D/E 和必要 helper 参数/既有去重交互复用，没有重做 A/B/C。依赖基线仍为含 #162/#165 的 `c19a7ebe`；没有合并他票分支或改变 RAG 等待边界。

| 只读来源 | 确认的接缝与限制 |
| --- | --- |
| `backend/src/main/java/com/stellogic/customeragent/TimeConfiguration.java`、`compose.yaml` | `DEMO_FIXED_INSTANT` 决定 Spring 固定时钟，默认 Compose 固定在 `2026-08-09T14:00:00Z`；Bean 在启动时创建，没有运行中改时钟的 HTTP 接口。浏览器自己的计时推进不会推动 Spring 调度 |
| `scripts/test-auto-resolution.ps1` | 已有 `Set-AutoResolutionClock` 通过环境值加 `docker compose ... --force-recreate ... backend` 推进到 4分59秒、5分钟及72小时边界。它是整套持久化验收的内部函数，不是供 Playwright 调用的公共 helper |
| `agent/auto_resolution_smoke.py` | 现有固定 namespace、marker 屏障和阶段调用对应预造工单/代次、内部能力及结论提交。不能直接挪用这些工单当作真实 UI → LangGraph 的证明，也不复制整套矩阵到本票 |
| `scripts/issue80-acceptance.ps1`、`frontend/e2e/issue80.session-restart-expiry.spec.ts` | 已有外层分阶段重启/重新登录的参考；后端重启使旧 session 不恢复。本票没有注册、阶段选择或跨阶段 ticketId 交接权限，也不借旧文件的 `skip` 路径冒充新覆盖 |
| `frontend/src/App.tsx`、`closure/JdbcClosureService.java` | 解决后通过现有“回复订单编号 / 回复问题类型 / 工单回复 / 发送回复”发起 `/api/customer/tickets/{id}/replies`，可验证原票重开；不是缺产品回复 API，而是缺真实 UI 工单与时钟阶段的衔接 |
| `frontend/src/SupportCompensationPanel.tsx`、`ApprovalWorkbench.tsx`、V41 迁移 | #165 没有新增对外账本 API。现有 UI 提案/审批足够承载 E；额度视图只通过既有测试侧数据库 helper 读取，不给浏览器注入数据库地址或直接发布内部接口 |

**当时识别的最小缺口（已由下一节授权增量补源码）**：需要公共编排 owner 复用已有固定时钟重建方式，给本票 UI 创建的真实工单提供分阶段运行和 ID 交接。T0 创建候选、T+4分59秒未解决、T+5分钟解决，在关闭截止前回复原票；关闭边界用另一真实 UI 工单。当时未实现新环境变量、阶段协议或文件交接，产品回复端点已存在。

D 仅检验候选/取消；原两文件阶段自动到期与重开为 **NOT_IMPLEMENTED / NOT_RUN**。后续已补 F 源码，并于 2026-09-01 完成六阶段聚焦运行。E 的输入为正常模拟执行配置；请求成对放行后只检查一笔额度被预占或消费，不使用实际执行进度挑选成功结论，不覆盖错误供应商配置或执行失败矩阵。本段其余判断保留为当时的源码核对历史。

## 本次授权增量：真实时钟阶段接线

固定审查基线为 `6b8cfde69a46a4e95b5fa435d42f1128b35a15c9`。开始时工作树干净；重新 fetch 后 `origin/main` 仍为 `c19a7ebe8ec31f7ed21048ea75fbfcfd61df1472`，已在当前 HEAD 祖先中，无需重放 A–E。原 #173 Issue 回读确认允许最高层场景、运行门禁及证据文档；协调已明确分配本票最小公共接线所有权。

复用 #162 的 `Clock.fixed` 加后端重建方式和 #80 的 artifacts 卷，不新增依赖、产品 API、Docker socket 或直接写入业务结果。实施前核对 [Playwright 环境变量参数化](https://playwright.dev/docs/test-parameterize#passing-environment-variables) 与 [Docker Compose up 的重建/等待选项](https://docs.docker.com/reference/cli/docker/compose/up/)。不直接导入执行整个 #162 smoke，不把其预造工单当成 UI 证据。

| 阶段 | Spring 固定时间（UTC） | 源码断言 |
| --- | --- | --- |
| prepare | 2026-08-09 14:00:00 | 两个独有23小时延迟订单经 UI 确认建单、真实 Agent 产生候选，持久化等待为300秒；写入真实 ticketId、订单引用、代次和原截止 |
| before-due | 2026-08-09 14:04:59 | 新登录打开原票；仍 INVESTIGATING/PENDING，原截止不变，无解决事件 |
| resolved | 2026-08-09 14:05:00 | 等待真实 scheduler 解决；页面显示自动解决及回复入口；持久化解决时间准确，关闭截止为解决后72小时 |
| resolved-restart | 2026-08-09 14:05:00 | 再次重建并新登录，原截止不重置，公开解决事件和自动解决审计各仅一次 |
| before-close | 2026-08-12 14:04:59 | 两票均未关闭；第一票经原回复控件200/REOPENED，ticketId不变、代次增加、历史候选RESOLVED保留、公开自动解决提示消失，刷新后仍成立 |
| closed | 2026-08-12 14:05:00 | 第二票真实调度关闭，回复201/LINKED_TICKET_CREATED，关联新票通过 `follow_up_of` 回指原票；第一票仍未关闭，原票仅一次关闭 |

`scripts/issue80-acceptance.ps1` 保留原隔离 project/tag/端口/卷生成、分类核验与最终清理，普通串行组后调用专属 helper，再执行原 #80 Session 重启与到期阶段。helper 首先校验继承门禁令牌；所有 Compose 命令显式带外层 project，只重建 backend、不构建镜像；每次重建后重启现有 `browser-frontend` 并等待健康，刷新 `nginx.browser.conf` 静态解析的后端地址，避免 [Compose 重建可能改变 IP](https://docs.docker.com/compose/how-tos/networking/#updating-containers-on-the-network) 导致阶段登录失败。`finally` 恢复 `DEMO_FIXED_INSTANT` 与 `ISSUE173_CLOCK_PHASE` 原值/原存在性，并以同样顺序恢复后端和浏览器代理。失败继续抛出，由原外层 `finally` 清理自有栈和卷，不吞失败或声明通过。不修改 nginx 配置或服务定义。

`compose.yaml` 只给 `browser-acceptance` 增加 `ISSUE173_CLOCK_PHASE` 传递，不改变 #190/Agent/backend 定义。新的 intake helper 仅原样搬出 A–E 已有订单准备/确认函数供 F 复用，A–E 业务步骤不重写。每阶段重新通过登录 UI 建立 Session；在现有 artifacts 卷内新建 `/artifacts/issue173-clock.json` 用于本次交接，要求前阶段完成标记匹配，不保存凭证。阶段缺失或未知直接失败，无 `skip`。

运行时每阶段输出 `issue173-clock-<phase>.json` 和对应 PNG 到隔离 artifacts 卷；JSON 包含真实工单 ID、截止和只读数据库事件计数。2026-09-01 聚焦运行已生成并消费这些临时产物，随后按资源清理要求删除自有卷；忽略目录保留 runner/transcript，但没有把临时卷冒充永久证据归档。截至静态提交 `9f2f441` 的全部运行仍是 **NOT_RUN**；后续结果见末节。

首轮静态审查 Standards 为 PASS/0，Spec 发现1项代理地址未刷新的接线问题；已集中修正专属 helper 的阶段及恢复路径，两轴仅复核此修复与文档增量，不重跑全量风险审查。最终结论随提交回报。现有外层清理会删除 artifacts 卷，正式证据归档仍需在清理前由获准的运行流程导出；本阶段不把卷内临时文件宣称为永久归档。

## 审查与证据边界

上一阶段 `00d2a6a` 提交前以 `c19a7ebe8ec31f7ed21048ea75fbfcfd61df1472` 为固定基线审查两个文件的暂存差异（`git diff --cached <base> -- <两个文件>`），不是已经运行的 HEAD 证据。两位独立审查代理首轮结果如下：

- **Standards：PASS，硬性规范发现 0、启发式发现 0。** 确认只断言公开行为和固定 UI 文案；定位器与真实产品一致，fixture 不预写结果，不清理他票领取；文档诚实标注缺口。
- **Spec：PASS，发现 0。** 确认三条源码场景及完整 AC 索引符合本轮隔离静态范围，未编造接口、假成功或跳过覆盖；RAG、v2-only 和公共登记保持待接线。
- 主代理随后按执行服务源码发现正常执行可能解决工单，将 C 的“释放本票领取”移到批准前，以免批准后控件正常消失导致测试自身竞态；不更改产品。最终暂存增量（这一顺序调整及审查记录）再次交由两轴静态复核，最终结论随提交回报协调。

截至静态提交 `9f2f441` 的运行清单均为 **NOT_RUN**：测试、格式化与格式检查、lint、类型检查、构建、浏览器、Docker/Compose、产品 API、模型调用、质量/性能评测、完整门禁。当时没有截图、运行日志或分数；付费模型调用为 0 次。以下离线验证不追认先前未运行的结果。

本阶段只对 `00d2a6a` 之后的 D/E、必要 helper 改动与本计划增量进行一次必要的 Standards / Spec 双轴静态审查；结果随最终提交回报协调，不因额度或时间目标重复全量 CR。D/E 的测试源码不继承原 #162/#165 或上一阶段的运行结论。

可用于后续简历/面试的贡献描述：基于现有真实产品接缝准备 A–F 跨层验收源码及六阶段固定时钟编排，梳理 #173 全 AC 和父规格 40 条测试决策，区分实现、静态审查与运行证据。不能表述为全栈验收已通过、RAG 已达标、生产可靠性提升或已交付 #173。

## 2026-08-31 离线聚焦窗口：结果与交接

协调明确确认 #190 归还未使用窗口后，将下一离线聚焦窗口交给 #173。开始前宿主真实锁查询为 FREE；每个运行均使用仓库 `Enter-TestGateLock -Issue 173 -CommandType offline-focused`，以 `finally` 调用 `Exit-TestGateLock`，没有替换正式锁身份或绕过锁。运行前 HEAD 为 `9f2f44183f928ff5630fd4996bbf03397e8875c5`，验证包含以下未提交修正，不能将结果归到旧 HEAD 的原始内容上。

新增 `scripts/test-issue173-clock-acceptance.ps1`：先解析本票 helper 与 #80 入口的 PowerShell 语法，再用进程内 `docker` 参数记录器执行真实编排脚本，检查六阶段顺序/业务时钟/串行 spec、重建后的代理刷新、失败停止后续阶段，以及成功/浏览器失败/后端重建失败下的环境恢复。它不调用 Docker，也不制造工单、Agent 或浏览器成功；被测 helper 的输出明确标注 `MOCK_DOCKER_HELPER`。该离线测试尚未加入公共 `check.ps1`，本窗口显式调用，不改未授权公共区域。

| 运行 ID | 结果 | 证据与处理 |
| --- | --- | --- |
| `issue173-offline-prepare-431bce6f` | 脚本语法、三种编排契约、既有分类/失败传播契约通过；离线依赖准备完成 | 本机缓存安装506个前端包，`npm ci --offline --ignore-scripts` 无网络下载；系统 Node22.15.0 产生版本警告，后续前端验证切换到已有 bundled Node24.19.0，不将警告归因产品 |
| `issue173-offline-scripts-317f5802` | PASS | 三种编排契约与 `scripts/test-browser-acceptance-plan.ps1` 均通过，真实 Docker 调用0；子进程输出另存 commands.log |
| `issue173-offline-frontend-7cd1bf57` | FAIL，已修复 | Prettier 检出两个本票 spec 格式问题；停止后续步骤、释放锁，仅格式化本票文件 |
| `issue173-offline-format-0845f0a9` | 格式 PASS；lint FAIL，已修复 | 11条 lint 错误归于两处根因：原候选快照 JSON 缺声明，以及 F 使用的 node:fs 缺类型；新增本票 `issue173-node-fs.d.ts`，沿用仓库局部 Node API 声明惯例，不新增依赖、不修改公共 node.d.ts |
| `issue173-offline-frontend-9392fd61` | PASS | Node24.19.0；本票4个 TS/d.ts 文件格式与 lint 通过；前端 `tsc --noEmit` 通过；已有 `src/AutoResolution.test.tsx` 5/5通过。组件阶段139.22秒，其中模块导入112.68秒，测试本身1.30秒，不把导入耗时当产品性能 |

本地原始日志保存在 `.local/issue173-offline/<运行ID>.log`，可复核子进程输出位于相应 `.commands.log`。最终输入摘要为 `.local/issue173-offline/validation-evidence.json`；初次 prepare 的 transcript 未收全子进程文本，因此另行复验脚本契约并用 Tee 保存输出，不伪称首个 transcript 完整。临时持锁 runner 也保留在该目录，仅作本窗口复现入口，不替代规范化完整门禁。

通过验证的关键源码 blob：

| 文件 | Git blob |
| --- | --- |
| `scripts/test-issue173-clock-acceptance.ps1` | `5fb9d17e0e2e28d233e092ad239785257f500ed1` |
| `frontend/e2e/issue173.full-stack.spec.ts` | `ec631cefda4cf78cc40c236464cfe9e443c85151` |
| `frontend/e2e/issue173.auto-resolution-clock.spec.ts` | `faeac87bcaf00d87d2a2242a53001d16571ae6dc` |
| `frontend/e2e/support/issue173-node-fs.d.ts` | `b8d9367c41ceec967496a8ef360eaa0a30938c97` |

本轮各次运行结束均记录 **LOCK_RELEASED**，最后宿主回读为 **TEST_GATE_FREE**（21:49，北京时间），离线窗口已归还，后续文档/双轴复核不持锁。未创建 Docker 容器、镜像、网络或卷；前端 node_modules 和 `.local` 仅为本 worktree 的忽略目录，不清理其他任务资源。

**真实固定时钟浏览器入口**：`scripts/issue173-clock-acceptance.ps1 -ProjectName <已准备的唯一隔离project>`，必须由外层已持锁进程调用并继承令牌；直接无令牌调用会拒绝执行。本轮已用该入口完成 F 的六阶段、两张真实 UI 工单及回复边界验证。现有 `scripts/issue80-acceptance.ps1` 会在最终门禁准备完整浏览器隔离环境并调度 F；最终 HEAD 仍须重跑，不能沿用本次聚焦结果替代完整门禁。

## 2026-09-01 历史：本地确定性聚焦验证与问题修正

本轮完整读取当前指令和测试锁协议后执行。所有 Docker/Compose 与浏览器运行均持正式锁、使用显式唯一隔离 Compose project 和门禁镜像标签；Agent 使用固定假模型/确定性动作，DeepSeek key/model 为空。未调用真实模型，未操作或重建默认 `customer-agent-baseline` Compose project。

| 验证 | 结果 | 证据边界 |
| --- | --- | --- |
| `check.ps1 -Component frontend -SkipAcceptance -Issue 173` | PASS | 前端规范检查、Compose/计划契约与前端检查镜像通过 |
| F 固定时钟/72 小时 | PASS | prepare、before-due、resolved、resolved-restart、before-close、closed 六阶段各 1/1 通过 |
| A–E 首轮 | 4/5 | A/C/D/E 通过；B 等待“待审批”投影超时，运行按失败退出 |
| B 隔离复验与诊断 | FAIL，定位为测试接缝 | 数据库确认代次 COMPLETED、提案 1 笔且真实公开回复已写入；失败来自 B 依赖由 C/E 验证的补偿投影刷新 |
| B 最小修正后的前端聚焦检查 | PASS | Prettier、ESLint、`tsc --noEmit` 与 `AutoResolution.test.tsx` 5/5 通过 |
| A–E 修正后整组复验 | PASS | 5/5 通过；B 改为等待真实代次完成和公开回复，仍不注入成功数据 |
| B 最终审查修正复验 | PASS | 去除自然语言逐字绑定后，真实持久化 Agent 正文与 UI“智能客服”气泡配对断言 1/1 通过 |

每次运行均在 `finally` 清理本次 project 的容器、网络、卷和门禁镜像并释放锁，逐次记录 **LOCK_RELEASED**。本轮只修改 B 的验收等待接缝和本文，不修产品。

以上“当前剩余”仅是 2026-09-01 的历史状态。到 2026-09-03，#169/#170/#171/#172 已交付到当前主线；本票已完成前端聚焦、B/C 真实全栈接缝和分层证据契约验证。#169 回答质量复用归档的 **KNOWN_LIMITATIONS**，#170 HUMAN 真实回答质量保持 **NOT_EVALUATED**，均不重复调用真实模型；当前只剩最终完整 `pwsh ./scripts/check.ps1 -Issue 173` 仍为 **NOT_RUN**，须在最终 HEAD 仅运行一次并通过后才可合入。
