# #173 确定性全栈验收：隔离静态准备

## 状态与范围

**CODE_READY_NO_TESTS 仅用于本轮已写源码，不表示 #173 完成或任何 AC 验收通过。**

- 日期：2026-08-31。起点为已 fetch 的 `origin/main@c19a7ebe8ec31f7ed21048ea75fbfcfd61df1472`，初始工作树干净。
- 分支：`codex/issue-173-static-acceptance`。开始前读取协调任务、任务列表、worktree 列表、GitHub #173 与开放 PR，未发现重复实现领取；#173 当时无 assignee、无评论、无开放 PR。
- 唯一拥有文件：`frontend/e2e/issue173.full-stack.spec.ts` 与本文件。不修改产品、已有测试、公共脚本、配置、注册或他票文件。
- 已完整读取本 worktree 的 `.agents/skills/implement/SKILL.md`。用户本轮限制优先：只静态实现与 Standards / Spec 双轴审查；不执行测试、格式化/格式检查、lint、类型检查、构建、Docker/Compose、浏览器、产品 API、模型或门禁，不查询/申请测试锁。
- GitHub 元数据只读查询和 Git fetch 不属于产品 API 验收。沙箱 gh 401 后，宿主上下文读取成功，未据此判断用户令牌失效。
- 不合入、不关票，不触发或等待 CI。外部审查不作为本轮阻塞；本地双轴静态审查不能代替最终完整门禁。

事实源：[实施票 #173](https://github.com/Stellogic/customer-agent/issues/173)、[父规格 #149](../specs/issue-149.md)。旧规格中的“普通 CI”按现行 AGENTS.md 解释为本地确定性检查配置；旧规格“门禁前不得提交”已由现行“开发期间允许频繁提交”规则替代，门禁前仍禁止合并和关票。

## 已有接缝与本轮源码

复用现有 Playwright、`support/auth.ts`、`support/browser-context.ts` 与 `support/database.ts`，不加依赖、不设计新 API。全部业务动作由生产 React 的可见控件发起；响应监听只读取这些动作产生的真实 Spring 响应，不通过测试侧 fetch 或内部 Agent API 推进业务。

订单 fixture 每次生成 `ORDER-ISSUE-173-<UUID>`，仅写入本场景独有的 `synthetic_order`：客户 `customer-demo`，支付 268 CNY、延迟 80 小时、可补偿额 268 CNY，使用现有 `delay-policy-v1`。物流状态等沿用当前迁移默认值。fixture 不创建工单、消息、处理代次、领取、提案、审批或执行结果，也不删除或重置共享数据。场景数据留在本次隔离数据库，未来由该次 Compose 卷清理收尾。

| 场景 | 新源码实际断言 | 静态依据与明确限制 |
| --- | --- | --- |
| A：受理与多工单 | 自然语言描述订单和两个问题；低置信问题先澄清；一次确认返回两个不同工单及共同受理 ID；刷新总览仍有各自入口；390px 无横向溢出 | 依据 `issue153.atomic-multi-issue-intake.spec.ts`、`issue157.order-ticket-groups.spec.ts`、`App.tsx`、`intake_model.py`。不把返回两个 ID 当作事务回滚、并行调度或授权隔离证明 |
| B：多轮与恢复 | UI 建单后等待真实 Agent 形成待审批投影；连续追加两条客户消息，Spring 确认接受且非重放；断线时清屏，真实同步后两条消息各出现一次且仍是原工单 | 依据 `App.tsx` 的 v2 消息与 SSE 接缝、`issue124.offline-fullstack-readiness.spec.ts`。仅对本工单事件连接做 `route.abort` 故障注入，不 `fulfill` 成功数据。未控制生成时序，不能声称旧代次迟到围栏或逐片流式覆盖 |
| C：跨角色闭环 | 自然语言人工意图经 Agent 转人工；客服领取前不见本票描述，领取后回复；客户实时看到带“客服”作者的回复；刷新恢复领取及历史；标准补偿 26.80 CNY 待审批投影；独立审批人领取对应提案、看不到人工回复，批准后详情清屏并退出队列；客服释放领取 | 依据 `issue163.persistent-support-replies.spec.ts`、`issue164.standard-compensation.spec.ts`、`SupportCompensationPanel.tsx`、`ApprovalWorkbench.tsx`。不预造提案，不修改其他客服分配或订单额度；审批成功响应和退队列不等于补偿执行成功，也不是撤权竞态证明 |

A/B/C 均为 **SOURCE_ONLY / NOT_RUN**。页面硬编码按钮及状态文案可精确匹配；Agent 自然回复不逐字断言，不限定私有图节点或工具顺序。新源码没有 `skip`、条件假通过、前端成功 mock 或 SQL 预写结果。每个场景关闭自己创建的浏览器上下文。

## #173 全部 AC 映射

以下 AC 编号按 GitHub #173 当前正文顺序，所有运行结论均为 **NOT_RUN**。

| AC | 要求 | 本轮映射 / 剩余缺口 |
| --- | --- | --- |
| 1 | 纯验收票，只加最高层场景、门禁与证据 | 仅两个新增文件；运行门禁注册未授权，待集成 |
| 2 | 产品缺口回对应实施票 | 本文记录缺口归属；不修产品、不创建或改写其他票 |
| 3 | 同订单多问题、一次确认、并行、多轮、旧代次围栏 | A 覆盖受理与分组源码，B 覆盖追加源码；并行运行、旧代次迟到片段/工具的受控竞态待补，不能以两个 ID 或提示文案替代 |
| 4 | 真流式、断线恢复、事实充分、有依据回复、5 分钟、72 小时 | B 有真实调查路径和传输恢复源码；逐片流式、充分性负例、计时及重开均待最高层接线 |
| 5 | 受理协助、持久化领取、人工回复、标准补偿、额度并发、审批、撤权 | C 串联领取/回复/刷新/补偿/审批；受理协助、跨工单额度并发、权限撤销、执行/待对账仍待补 |
| 6 | 客户引用、客服辅助、无答案、错误版本、冲突、检索失败 | 全部待 #190、#168、#169、#170 原生前置；未编造检索接口或引用结果 |
| 7 | 桌面/窄屏、loading/empty/error、状态画廊、开发中无成功 | A 窄屏、B/C 桌面与 C 窄屏可见性只是部分源码；全路由高保真、关键状态、画廊、开发中无写入待 #171/#193 集成 |
| 8 | 浏览器及日志不含密钥、权重/路径、内部地址、prompt/reasoning/工具/checkpoint | 未写完整扫描器。需收集真实网络/SSE、产物及产品日志；复用现有模式并补全 Embedding 边界后再验，不能以少数 DOM 缺失断言替代 |
| 9 | 唯一 Compose project/tag/端口/卷、完整门禁、证据 | 未运行、未登记公共清单；未来获协调授权后按锁规范执行并归档 |
| 10 | 只证明确定性配置，不能冒充 DeepSeek | 本轮甚至没有确定性运行证明；#174 真实 DeepSeek 仍为独立发布验收 |

## 父规格测试条目 1–40 的完整索引

编号对应父规格 `Testing Decisions`，此表是追踪索引而非“已覆盖”清单。现有测试仅作为接缝参考，本轮没有复跑，也不沿用其历史 PASS 作为新源码证据。

| 条目 | 本轮 / 后续验收责任 |
| --- | --- |
| 1–4：单一接缝、两种模型配置、真实依赖、诚实失败 | A/B/C 使用 UI → Spring；正式运行仍需证明生产 React、真实 PostgreSQL/LangGraph 和确定性配置。真实 DeepSeek 归 #174 |
| 5：可观察行为断言 | 本轮只断言产品动作响应、公开消息、投影、控件和持久化后的可见结果；不规定私有实现 |
| 6：自然语言各种受理入口 | A 低置信与多问题，B/C 单问题；多订单、订单歧义、自然语言确认、取消仍需整合 #152–#154 |
| 7：统一事务、共同受理不可变、幂等 | A 仅创建及恢复；全回滚、不可变和重试不重建仍需整合 #153，不使用 mock 回滚作为 DB 证明 |
| 8：受理恢复及七日边界 | 待整合 #155；A 的工单总览刷新不是受理会话七日恢复 |
| 9：受理协助权限 | 待整合 #156；不得借人工工单领取覆盖受理前授权 |
| 10：重复问题及混合新旧问题 | 待整合 #154；本轮独有订单避免历史重复干扰，不声称测了去重 |
| 11：独立生命周期/模式/负责人及并行 | A 只证明两个入口；待整合 #157，须在打开各详情前证明各工单已独立启动 |
| 12：同订单最小摘要授权 | 待复用 #157 的实际产品授权契约，不读取兄弟工单完整对话作 Agent 共享输入 |
| 13：连续追加、澄清恢复、摘要与历史分页 | B 两次追加及恢复；长对话摘要/分页、澄清继续仍需 #158 的稳定最高层场景 |
| 14：生成竞态与迟到动作 | 待 #158/#159 稳定受控时序；不能仅以 B 中“接受新消息”代替旧生成写入被拒绝 |
| 15：逐片流式各状态 | B 仅真实调查及断线恢复；须采集真实分片先后和状态，不直接调用内部 publish 制造结果 |
| 16：受控进度字段 | 待真实 SSE 网络证据与非法字段检查 |
| 17–20：有依据回复、充分性正负、低风险白名单 | B 正常补偿路径仅为部分场景；继续依赖 #160/#161 的真实证据集合与 Spring 结论约束，不反推事实充分 PASS |
| 21：五分钟可控时钟与重新校验 | #162 已交付，最高层时间控制接缝尚待确认；不新增测试专用产品端点、不直接写已解决状态 |
| 22：72 小时及重开 | 待现有关闭/重开契约与合法时钟控制接线；不实际等待 72 小时或写已关闭状态冒充 |
| 23：持久化负责客服 | C 覆盖领取、刷新和释放；重新登录、SSE 恢复、交回 Agent、解决/关闭及撤权待补 |
| 24：HUMAN 公开回复授权及辅助无副作用 | C 正向负责客服回复；非负责客服拒绝与 #170 辅助不自动发送待补 |
| 25：完整客服工作台 | C 覆盖详情、回复、标准补偿；订单分组/时间线与 #170 草稿待集成 |
| 26：审批回归 | C 独立审批人正向批准与不含人工对话；禁止自审、提案失效、驳回、执行和待对账待补 |
| 27：订单级额度并发 | #165 已合入；最高层需同订单多工单并发提案/审批/执行，不能以 C 的单提案代替 |
| 28：知识解析/版本 | #166 已有来源；待 RAG 集成，旧引用审计与无效条目验证不能仅用展示 fixture |
| 29：Embedding 契约 | 等 #190 真实模型准备、编码与冻结 revision 证据 |
| 30：ONNX 一致性与资源 | 等 #168，未通过前不声称 ONNX 默认或性能收益 |
| 31–32：中文质量和消融 | 等 #189 冻结资产、#190 质量交付与 #168 三路实测；不混抄各分路指标 |
| 33：检索前授权过滤 | 等 #190/#169/#170 实际共用适配，核对服务端过滤、公开来源投影和当前版本 |
| 34：RAG 故障 | 等 #190 故障接缝，覆盖无答案/冲突/错误版本/检索失败而不伪造成功来源 |
| 35：扩展、迁移、恢复及隔离 | 等 #190 数据库实际交付；本轮无 Docker、迁移或模型操作 |
| 36–37：高保真、键盘、焦点、窄屏、语义 | A/B/C 少量页面行为；完整原型比较及状态矩阵仍依赖 #171（含 #191/#192/#193） |
| 38：开发中无业务副作用 | 等 #193 已挂载入口；实际点击、监听请求并核对后端状态，不把 notice 文案当无写入证明 |
| 39：敏感信息边界 | 待网络、SSE、产物、日志全链收集；现有 `frontend/src/sensitive-content-patterns.json` 可复用但不能据此声称已包含全部新 Embedding 模式 |
| 40：完整规范化门禁 | 全部 NOT_RUN，注册与执行仍待授权；CI 关闭，现行规则允许静态提交，不允许未过门禁合入 |

## 前置与后续接线

2026-08-31 实时读取 #173 原生 `blocked_by`：#150/#162/#165 为 closed；#168/#169/#170/#171/#172 为 open。RAG 实际实现 #190 仍有 Draft PR #203，不能把本轮静态准备作为它或下游的正式交付证据。后续必须重新查询原生状态，不能拿此快照永久放行。

1. 等 RAG 与 UI 原生前置正式交付后，从最新 main 对照真实契约。#169 拥有与 #170 共用的适配，#173 不实现或抢占该层。RAG 四类失败、客户引用和客服草稿均保持待接线。
2. #172 未完成 v2-only 收缩。当前 UI 的客服和审批接缝仍是 `/api/support/workbench/...` 与 `/api/approver/compensation-proposals/...`；本票只观察 UI 的现有路由，不猜测 `/v2` 替代地址。收缩后须核对实际路由/响应，再改本票源码。
3. 补足上述 AC 缺口；发现产品功能缺陷，报告协调并回原实施票，不在 #173 修产品。时间与流式竞态若无稳定接缝，先记录阻塞，不能靠 `skip`、SQL 结果或人工内部事件制造“通过”。
4. **公共注册明确未完成**：`scripts/issue80-acceptance.ps1` 发现所有 `*.spec.ts` 后调用 `Assert-BrowserAcceptancePlan`；本文件不在 `scripts/browser-acceptance-plan.psd1`。按当前代码静态判断，完整门禁会因未分类文件失败。用户禁止改注册，因此保留这一缺口，不静默排除，也不能直接把本分支交给完整门禁。未来需协调授权的公共文件 owner 将其登记为串行场景，再做相应静态审查。
5. 仅协调明确放行后，先读取 `docs/agents/test-gate-lock.md` 并按规范处理锁；FREE 不是本轮运行许可，BUSY 立即停，RECOVERY_REQUIRED 升级协调。按现行交付顺序做聚焦验证、格式/类型预检、集中风险处理、双轴确认，最后完整 `pwsh ./scripts/check.ps1 -Issue 173`。本轮没有执行该命令。
6. 未来运行使用规范脚本生成唯一 RunId、Compose project、镜像 tag、端口及卷，保存受测 HEAD/base、工作树状态、确定性模型配置、结果、耗时、浏览器/日志证据与自有资源清理记录。只记录可公开配置，不能在报告中复制密钥或未脱敏日志。通过后才进入合入、关闭与 `origin/main` 回读。

## 审查与证据边界

提交前以 `c19a7ebe8ec31f7ed21048ea75fbfcfd61df1472` 为固定基线审查两个文件的暂存差异（`git diff --cached <base> -- <两个文件>`），不是已经运行的 HEAD 证据。两位独立审查代理首轮结果如下：

- **Standards：PASS，硬性规范发现 0、启发式发现 0。** 确认只断言公开行为和固定 UI 文案；定位器与真实产品一致，fixture 不预写结果，不清理他票领取；文档诚实标注缺口。
- **Spec：PASS，发现 0。** 确认三条源码场景及完整 AC 索引符合本轮隔离静态范围，未编造接口、假成功或跳过覆盖；RAG、v2-only 和公共登记保持待接线。
- 主代理随后按执行服务源码发现正常执行可能解决工单，将 C 的“释放本票领取”移到批准前，以免批准后控件正常消失导致测试自身竞态；不更改产品。最终暂存增量（这一顺序调整及审查记录）再次交由两轴静态复核，最终结论随提交回报协调。

运行清单均为 **NOT_RUN**：测试、格式化与格式检查、lint、类型检查、构建、浏览器、Docker/Compose、产品 API、模型调用、质量/性能评测、完整门禁。没有截图、运行日志或分数；付费模型调用为 0 次。

可用于后续简历/面试的贡献描述：基于现有真实产品接缝准备三条跨层验收源码，梳理 #173 全 AC 和父规格 40 条测试决策，区分实现、静态审查与运行证据，并识别前置与公共登记缺口。不能表述为全栈验收已通过、RAG 已达标、生产可靠性提升或已交付 #173。
