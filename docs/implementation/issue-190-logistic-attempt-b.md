# Issue #190 A方案：一次校准不可行

关联 #190 / PR203。**固定四特征 + L2 logistic 的这一次可行性验证结果为 INFEASIBLE，不能交付。** 73个预登记候选门槛中，0个同时满足全部质量条件；`selected=null`、`proposed_policy=null`。没有选择最接近值，没有应用模型系数或重用历史余弦策略回退。

## 运行身份与边界

- RunId：`issue190-logistic-fit-20260831b`。受测干净HEAD：`98b49949d6d835d510c4959d787b443fa95bc794`；base：`c19a7ebe8ec31f7ed21048ea75fbfcfd61df1472`。
- 保留[a轮前置失败](issue-190-logistic-attempt-a.md)；a轮未拟合，b轮才执行第一次实际拟合及第一次唯一校准选择。两次尝试不合并成一次“始终成功”记录。
- b轮仅复验已提交的float4探针修复。产品源码、四特征、BGE、RRF、数据、seal和质量门槛未变。
- 训练144题/6主题/24篇/36片段，校准72题/3主题/12篇/18片段；各自在新建隔离数据库中通过真实Spring/PostgreSQL/FTS/pgvector路径采集。没有用独立余弦脚本代替产品检索。
- 本任务没有读取留出正文/问句/标签或位置；没有运行留出、#189、完整门禁、浏览器验收或合入。原#189失败及冻结资产保留。

## 已有检查与耗时

| 阶段 | 实际结果 | 耗时/限制 |
| --- | --- | --- |
| 数值契约 | Python手算契约1项实际通过；Java特征2项/策略3项结果通过 | Python 0.04秒；Java本轮命中a轮相同源码和构建输入的缓存，不冒称重新执行 |
| 真实DB前置 | 探针整体PASS，authorization/current_version/scope均PASS；恢复前后仅全文分数按声明float4位精确比较，其他字段精确相等 | 9个HTTP观察；不是生产权限审计或完整安全保证 |
| 迁移回读 | 本轮隔离数据库迁移全部success，最高V42；main V41不改 | 可见原始safety-db.json的migration列表 |
| 训练采集 | COLLECTED，144题全部完成，数据及正文/模型/代次检查通过 | 149.17634020000696秒，含目录读取与串行HTTP，不是单请求延迟 |
| 校准采集 | COLLECTED，72题全部完成，同样检查通过 | 75.24364989998867秒 |
| 拟合与预登记选择入口 | 模型拟合完成，无异常或不收敛；导出线性分数与sklearn decision_function一致性检查通过；选择结果INFEASIBLE | 5.503483500011498秒，包含导入、输入检查和选择，未单独采集纯fit耗时 |
| 整个阶段 | 按不可行条件停止，退出非零 | 327.2918648秒，含启动、缓存构建、采集和清理 |

训练标签为72个supported、72个unsupported。标准化统计量只由训练组拟合，训练数据中有答案但未召回的情况没有被隐去；标签类别数和原始片段命中保存在报告中。

宿主环境Windows 11/CPython3.13.13，scikit-learn1.7.2、numpy2.5.2、scipy1.18.1；检索编码在Linux容器/Python3.13.14、PyTorch2.10.0+cpu、Transformers4.57.6，固定模型revision `7999e1d3359715c523056ef9478215996d62a620`。模型权重只读使用既有本地目录。付费模型API费用0元；峰值内存、总网络流量、电力/机器成本未采集。

## 为什么停止

下表只是对本次已保存的73个候选结果计数，不是另一次阈值扫描、重新拟合或选择替代参数。因为没有最终选定策略，不把任意一个候选的指标冒充交付质量。

| 预登记条件 | 门槛 | 本次73个候选中满足该单项的数量 |
| --- | ---: | ---: |
| 拒答后 answered Recall@5 | ≥0.90 | 24 |
| 拒答后 answered MRR@5 | ≥0.75 | 24 |
| 无答案precision | ≥0.90 | 16 |
| 无答案recall | ≥0.85 | 29 |
| 全部四项同时满足 | 不变 | **0** |

本次完整前置、采集和拟合均完成，没有发现运行错误足以解释不可行结果。结论限于：**预登记的四特征线性模型，在此次独立校准分区上无法同时达到既定召回与拒答条件。** 这支持停止本次A验证，不证明所有分类器、BGE或混合检索必然不足，也不能推断新留出或原冻结门会通过/失败。

尺度、系数、截距和全部候选结果保存在 `fitted_model` 等原始字段，仅作失败证据，不是已准入策略。产品配置仍 `PENDING_CALIBRATION`。未导出可用参数，因此没有可提交的成功参数冻结SHA或产品接受结果一致性PASS；Java/Python数值契约PASS不替代这些未执行项。

最小下一步是向协调报告本次A不可行并结束运行窗口。如继续，应先作新的方法/成本及独立验证规格决策；不能将本次已用于选参的校准集重新标为未见数据，也不能擅自打开封存留出进行方法赛马，不能自动实施B、改变四特征、扩168/169/170或降低#189标准。

## 证据与归还

[原始采集、拟合、安全回归、日志/XML和SHA索引](evidence/issue190-logistic-fit-20260831b/index.json)。原始文件按字节保留；日志只脱敏本机工作树路径，不含真实客户或会话cookie/token。

- 训练数据SHA256：`7f7d1fafa122a24a4774377cacde7b3acf9fafc8b11194d0878a7d7784c14e0f`。
- 校准数据SHA256：`4ba56767f8729ba064f614c856076c30f08e5852bad0255c2bf6b443c31014b6`。
- 训练观测SHA256：`0d6b2b60f18323f9ceb175c3386a526337132d0c8a9f1a679510a92359191f1a`。
- 校准观测SHA256：`b4ec9872012c90c795b0356a74f9ac3f4f7343bff207a76b16d9185265b06387`。
- safety输入SHA256：`b7126e2e63854664f63c2e2f332d3d1656f8210cab2d0700b02240fbb665b00b`。
- seal元数据文件SHA256：`0fc933afdb217763d51bfe3989482cf2c512c0a0d9829852ba9508190768189a`；未读取其承诺的留出内容。

自有Compose容器、卷、网络和镜像tag全部清理；锁释放后单次FREE回读，已主动发LOCK_RELEASED并归还阶段窗口。后续只归档和静态复核，没有重新运行。

这些是Codex按用户范围产生的本地合成实验资料，不是生产规模、线上业务收益或用户逐行手写贡献；失败与耗时可用于解释工程决策，但不得包装为质量达标的已交付项目。

## 归档双轴静态审查

固定比较 `98b4994...b5198280cfe2ad78654919834c4f329860e23557`；仅归档增量，不重新运行或评分。

### Standards

PASS，0项发现。报告、索引与日志支持前置PASS、首次拟合/唯一选择INFEASIBLE和资源释放；Java缓存如实注明。只新增证据与说明，没有方法/数据变化。

### Spec

PASS，0项新增缺陷；规格交付仍未通过。73候选无可行门槛、无proposal，错误退出和停止边界如实保留；不能进入留出、冻结验收或交付。

两轴未解决发现均0，最严重项均无；归档PASS不改变A不可行和Issue未完成的状态。
