# Issue #190 可核对的实施与面试证据

关联：[Issue #190](https://github.com/Stellogic/customer-agent/issues/190)、[PR #203](https://github.com/Stellogic/customer-agent/pull/203)。截至 2026-08-31，仍是本地学习研究项目的未交付实现，PR 保持 Draft。没有生产使用、真实客户规模、线上收益或正式交付结论。

**本日后续实测更新：** [完整阶段记录及新旧指标对照](issue-190-stage-20260831.md)已补齐。独立48题校准实测39.581秒，选定阈值0.6228445617150831；留出有答案接纳仅37.5%。随后64题冻结实测67.046秒：Recall@5=55.5556%、MRR@5=52.7778%、无答案precision=42.8571%，仍FAIL。36题有答案查询的全文候选从全空变为全有，但没有做消融，不能将改善分别归因于分词或阈值，更不能包装为上线收益。以下“本轮修复后未验证”表格保留的是运行前的历史状态，以链接阶段记录为最新事实。

## 实际边界与角色

- 已编写：固定 revision 的本地 BGE CPU 编码、模型文件校验、Spring 权威权限/发布状态/版本/范围过滤、pgvector 精确候选、PostgreSQL 全文候选、RRF 和内部检索界面、冻结评测执行器。
- 复用已有工作：#166 的知识目录与版本模型、#189 的冻结资产，以及既有身份体系、LangGraph 私有接口、规范化检查与共享锁。不能将这些全部计为 #190 新建。
- 未承担：客户或客服 Agent 消费知识（#169/#170）、#168 独立消融/ONNX模块、生产部署、训练新模型或维护外部搜索服务。
- 用户在本任务中确定范围、批准冻结验收与独立校准边界；实现代码、合成数据、诊断和文档由 Codex 在用户约束下生成、修改。不能据此声称用户逐行手写全部实现；个人实际编码、审阅和实验参与程度需要用户另行按事实说明。

## 关键决策及修复

1. 运行期间离线加载固定 `BAAI/bge-small-zh-v1.5`，显式准备与运行分离；维数512、CLS/L2、固定查询指令，CPU/Safetensors。不换模型或造假回退。
2. 候选排名前执行 Spring 权限与版本过滤，精确余弦候选与全文候选分别输出，RRF只处理合法候选。索引绑定目录代次和内容哈希。其全套浏览器/数据库验收仍未完成，不能只凭代码宣称安全性已验证。
3. 真实 PostgreSQL 暴露已有目录索引器直接绑定 `Instant` 导致语料为空，改为 JDBC `Timestamp` 参数；之后64题实测通过语料一致性检查，进入真实检索。修复保存在 `b08d0ee10f79d6f1d505e39d9e657a73c48b02ec`。
4. 首次有效质量实测失败，未修改冻结题、期望、门槛或隐藏结果。静态分析定位到缺少独立校准的 `0.80` 拒答判定，以及全部为空的全文候选。不能由此宣称模型本身不适用。
5. 本轮恢复后准备 Lucene CJK 基础词项与独立合成校准数据。18个片段、48题（32校准/16留出），单一阈值只由校准组选择。已按[预先记录的校准方法](issue-190-calibration-plan.md)执行一次；留出接纳不足，随后冻结质量仍FAIL，详见页首阶段记录。检索单请求延迟未采集。
6. 同步 main 时保护已发布迁移，未发布的向量迁移依实际已占用版本顺延；没有修改 main 已应用迁移或清库规避升级。

## 已有正常实施运行

| RunId | 内容及规模 | 实际结果 | 时间与费用 |
| --- | --- | --- | --- |
| `issue190-model-20260830a` | 固定revision模型文件；权重95,827,648字节 | 下载与逐文件校验PASS | 耗时未采集；付费模型调用0元 |
| `issue190-agent-focused-a` | 11项Agent聚焦测试及静态工具 | 测试通过；Ruff发现6处注释标点问题，阶段FAIL | 耗时未采集；0元 |
| `issue190-agent-focused-b` | 同组11项测试、Ruff/格式/Pyright | PASS，Pyright 0错误 | 耗时未采集；0元 |
| `issue190-quality-a/b` | 真实隔离PostgreSQL、Spring和Agent镜像 | 镜像构建成功；索引为空，冻结评测ERROR，未执行题目 | 耗时未采集；0元 |
| `issue190-quality-c` | 64条冻结查询：36有答案、12无答案、6错版本、6越范围、4越权 | 编码契约及语料一致性通过；质量FAIL，见下表 | 耗时未采集；0元 |

早期运行没有在报告中采集精确受测 HEAD 与未提交差异摘要，不能事后补写成一个精确的受测SHA。可确认其 main 基线为 `4cfd0efd63416c78801c318edc858abeb92c1219`，运行后的代码和质量原始报告保存于 `b08d0ee`；该提交是归档位置，**不冒称提交后重新运行过**。下表保存恢复初期、同步 main `0a9ee031ca5c5febcd0c6fb11a660c5eee83046f` 后尚未运行时的记录；本日后续运行的精确HEAD/base及指标见页首阶段记录。

### 首次有效实测的失败对照

| 指标 | `issue190-quality-c` | 冻结要求 | 本轮修复后 |
| --- | ---: | ---: | --- |
| answered Recall@5 | 0.027778 | ≥0.90 | 未验证 |
| answered MRR@5 | 0.027778 | ≥0.75 | 未验证 |
| 无答案precision | 0.255319 | ≥0.90 | 未验证 |
| 无答案recall | 1.0 | ≥0.85 | 未验证 |
| 错版本/越范围/越权Top-5命中率 | 均0 | 均0 | 未验证 |

36个有答案请求均得到向量候选，但只有1个返回合格结果；64题的全文候选全部为空。这是本地合成冻结集上的观察，不是生产召回率或线上服务效果。

环境：Linux容器、Python3.13.14、PyTorch2.10.0+cpu、Transformers4.57.6、Safetensors0.7.0、PostgreSQL18.6、pgvector0.8.6。精确宿主CPU型号、峰值内存、延迟分位数和吞吐量未采集。付费模型调用为0元不代表机器、网络、电力成本为0。

原始证据：[64题结果与候选](evidence/issue190-quality-c.json)，仅含仓库合成题和知识，无会话cookie、令牌、真实客户数据。SHA-256 `61379ae6a5c3aaf5578901d9d0c499a600e172e48ac9d69290f3b443ce5e87aa`。模型revision `7999e1d3359715c523056ef9478215996d62a620`；数据集SHA-256 `d2d5efdae565395c1dc722e14f66077558575152e23c5c8fbea58b7ebcfd2fe5`。其余本地日志未纳入版本库，不作为远程可复核原始证据。

后续只随正常授权验证补充报告的RunId、HEAD/base、源码/数据哈希、耗时与已有指标，不为简历额外压测、付费调用或建设遥测系统。正式交付前不使用“实现质量达标”“提升召回率”“已上线”等表述。

## A方案后续记录

固定四特征L2方案与216题开发资料、封存流程已实现；未承担新预训练模型训练。[预检及双CR记录](issue-190-logistic-static.md)：9项Python聚焦/类型/后端check通过，前端检查命中缓存，177.6977645秒。[第一次拟合前置尝试](issue-190-logistic-attempt-a.md)：251.339985秒，Python1项和Java5项数值契约通过，真实DB安全探针最后的全文浮点表示精确比较失败并停止。实际拟合次数0、校准选择次数0，不能作为新方法质量结论；失败原始证据与仅探针修正一并保留。两阶段付费模型API均0元，不是总机器成本。实现/实验由Codex按用户范围执行，不冒称生产收益或用户逐行手写。
