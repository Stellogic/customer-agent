# #190 模型准备与已有卷升级说明补齐

2026-09-01；Issue #190 / PR #203，base `c19a7ebe8ec31f7ed21048ea75fbfcfd61df1472`。

干净 HEAD `94765cefab95da09d4db826c10ecae902cd76551` 已完成 `issue190-final-20260901b` 完整门禁 PASS：主体 896.3 秒（构建 15.246、smoke 410.532、浏览器 442.016；不含门禁脚本自检）。64/64 检索完成，Recall@5 0.9444444444、MRR@5 0.8680555556、三项禁止命中率各 0；HTTP 200 共 54、预期权限拒绝 403 共 10，失败/未运行各 0，回答质量仍 NOT_EVALUATED。并行安全组 5 项×3、串行 49 项、会话三阶段各 1 通过/1 不适用跳过。自有资源清理并释放锁，一次宿主回读 FREE。[完整输出及报告](evidence/issue190-final-20260901b/index.json)。

收尾回读已有自动审查时确认两处真实部署文档缺陷：README 错称仅需 Docker，没有说明被 gitignore 的模型目录必须先准备；既有 PostgreSQL 数据卷不会执行新增初始化脚本，只换 pgvector 镜像不能自动在业务库安装扩展。此前门禁都使用已准备模型及空卷，不能证明这两个用户启动前提已经交代。没有再次手动触发外部审查；已收到的真实问题正常修复。

最小修复仅修改 README：列出 uv/PowerShell/首次联网要求、既有固定模型准备脚本和已校验目录复用方式；启动阶段持锁，先启动数据库，以管理员执行已有幂等 `002-knowledge-vector.sql`，再启动迁移与应用。没有增加启动框架、修改产品/迁移、提升运行角色权限或删卷。依据见 README 链接的 PostgreSQL 镜像与 pgvector 官方文档。

`issue190-startup-20260901a` 在上述 HEAD 加待提交 README/证据的脏树上运行 49.2247 秒，结果 PASS：

- README 全部 PowerShell 代码块语法解析通过；既有模型完整哈希校验通过，无下载、无加载推理。
- 在隔离 pgvector PostgreSQL 18 容器中仅提供旧 `001-databases.sql`，实际 Flyway 迁移至 V41，并写入一条合成标记；随后加入新初始化脚本、重启同一数据卷，确认 vector 扩展仍不存在。
- 实际执行 README 的管理员安装命令两次，再实际 Flyway 迁移 V42，回读 `1|preserved-before-vector|1|0`：扩展存在、原标记保留、V42 成功、两个 Spring 角色均无超管权限。
- 三端 8 个规范镜像目标使用同源构建缓存；不是再次运行全部组件测试。没有运行质量评测、浏览器、真实 API 或下载模型。新调用/费用均 0，旧预算账本未动。
- 只在验证结束后清理本 RunId 的隔离合成卷/容器/网络/镜像；没有触碰用户既有数据卷。释放自身锁后只读一次宿主 FREE，已通知协调，交付窗口保留。

[聚焦原始控制台、阶段报告、脚本快照及哈希](evidence/issue190-startup-20260901a/index.json)。归档脚本是当时 `.local` 启动器的证据快照，不是新产品入口；用于复核步骤，不应从证据目录直接运行。此实验验证 V41 数据库状态下的扩展安装与数据保留，不宣称实测了任意旧镜像、跨 PostgreSQL 主版本升级或重新联网下载全模型。

README 增量 Standards PASS / Spec PASS（各 0 阻塞）；最终仍须修订后干净 HEAD 的完整门禁。final-b 原 PASS 保留，但不作为后续文档提交的交付证据。最终结果以对应 HEAD 的正式门禁记录和 PR 回读为准，避免为补写最终结果而改变受测 HEAD。

这是学习项目的部署说明遗漏与验证补足，可用于面试说明为何组件/空库验收不能代替旧卷升级检查。用户作出最小方案及保留试错的决策，Codex 协助修复与验证；不包装成生产迁移事故或用户独立手写。历史失败、方法、提交及费用记录全部保留。
