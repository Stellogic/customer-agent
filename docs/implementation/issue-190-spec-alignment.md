# Issue #190：规格澄清与暂停边界

2026-08-31，用户在本任务的 `grill-with-docs` 对齐中确认。关联 [Issue #190](https://github.com/Stellogic/customer-agent/issues/190) / [PR203](https://github.com/Stellogic/customer-agent/pull/203)。记录起点为 `60ead9ef852ced41639a375be17dc9d6b46eac81`；工作树已有 c6 静态修改保留，本次不将其当作已验证或已批准的产品方案。

## 约束来源与正式规格核对

用户明确：正式 spec 与 tickets 是规格约束；实验文档、ADR、当前实现及本记录只能提供依据和解释，不能自行增加产品要求。只与旧实验或实现约定不同，不需要再次申请规格变更。

本次通过 GitHub 只读核对正式 #149、#189、#190、#169 正文。未读取冻结题目或留出内容。

- [#149](https://github.com/Stellogic/customer-agent/issues/149) 已规定本地 Embedding 负责向量化、DeepSeek 负责推理与回复；离线加载要求不是整个项目禁止云调用。
- [#189](https://github.com/Stellogic/customer-agent/issues/189) 冻结查询、期望结果、模型协议和质量门槛。“不调用 DeepSeek”条款约束评测资产可被独立审查，不能扩大解释成全产品禁云。
- [#190](https://github.com/Stellogic/customer-agent/issues/190) 交付本地混合检索与真实内部检索页面；客户或客服 Agent 消费前必须通过冻结检索质量门。
- [#169](https://github.com/Stellogic/customer-agent/issues/169) 承接客户知识消费；#190 正文明确客服辅助由 #170 承接。本次不改变归属或依赖。

用户最终选择保留正式规格：检索独立验收，故障处理也按原规格。因此无需修改正式 spec / tickets，也不采用此前讨论过的分层迁移拒答指标或网络错误直接返回方案。

## 已确认的最小产品方向

1. 对需要知识的回答，本地检索后，将问题和授权相关片段交给同一次 DeepSeek 调用，由它判断资料是否足够并生成回答。默认不增加独立充分性判断调用；若以后提出增加，先证明必要性、收益及额外成本与延迟，不能直接沿用实验架构。
2. 不为充分性判断新增独立搜索入口。保留 #190 原已要求的内部检索页面；用户问答不以先打开该页面为前提。
3. 使用模拟问题、模拟工单和示例知识库，允许将相关内容发送给 DeepSeek；不使用真实客户隐私或真实企业机密，不新增外发审批、分类或脱敏系统。原权限、版本、适用范围及 Spring 业务权威不变。
4. 新输出合同取消单条摘录 24 字符硬限制，保留授权来源与逐字原文真实性，以及有依据的总体输出上限。尚未选择新的总体上限；不把旧 256 token 自动作为新规格，也不继续仅为满足 24 字符修改提示或接口。
5. 资料不足时明确说明，必要时追问，不编造，不仅因资料不足自动转人工。用户要求人工时沿用现有转人工路径；原有事实冲突、风险及权限规则不因本条取消。
6. 网络、模型、工具或检索故障保留 #149 原规则：有限重试仍失败后停止当前运行、保留进度并转人工，不回退假模型。回复一次受控修正后仍不安全则转人工。此前“网络直接报错、不自动转人工”的草案已由用户撤回；本次不修改重试实现或新增流程。
7. 分别报告结构合法性、引用真实性、语义充分性。格式失败不能直接称为语义质量失败；无法进行语义评估则标为未评估。真实引用本身不证明答案充分，不能丢弃失败样本后宣称整体验证通过。

## 检索验收与历史证据

保留 #189 / #190 原规格及检索独立验收。当前评测器以最终检索结果列表计算召回、排名与拒答；本次不换接缝、不改其结果投影，不用 AI 最终回答的拒答成绩替代检索层质量证明。

单次 AI 回答是下游产品方向，不是解决 #190 既有质量阻塞的证据。当前不承诺检索已经有可通过的方案，也不继续无限实验。#168 / #169 / #170 的原依赖与正式交付要求不变。

所有旧合同、题目、标签、质量阈值、源码、提交、原始失败、STOPPED 状态、旧成绩及共享预算账本保留；本次不重算、不回滚、不重置预算。新版原则不追溯改变旧实验的 24 字符限制或禁止重试合同。

## 本次操作与后续授权

c6 继续实现和全部新增运行保持暂停。已有 c6 文档描述的是暂停前的静态计划，不能作为恢复授权；其原文件和代码不在本次改写。

本轮只读取技能、规格、票据和必要接口，保存本对齐记录。未运行测试、格式、类型、构建、模型、API 推理或评测，未取得或查询测试锁，未读取或修改活预算账本，无新增模型费用。协调此前提供的累计上界 0.620805 元 / 总预算 6 元仅为历史元数据，并非本次核账。

本记录不授权恢复实验、接入产品、运行冻结门、解阻、合入或关票。静态对齐不等于测试 PASS、质量 PASS 或正式交付。文档由 Codex 依据用户决定整理，不代表用户逐行编写代码，也不代表生产收益。
