# Issue #190 阶段实测：基础全文已工作，冻结质量仍 FAIL

本阶段由协调明确授权，未包含最终完整门禁或合入。结束后已主动归还运行窗口，协调交回 #165；不再启动测试、校准、冻结评测或最终门禁。PR #203 保持未交付，下游消费仍阻塞。

## 版本与实施边界

- main 基线：`efbdb8348dc9c12f259c69d9e8c16de5e4d3994c`。读取更新 AGENTS 的数据记录要求；此时 main 迁移最高 V40，本票未发布迁移 V41 不改已应用迁移。
- 静态方案源码 `fe93e10` 已双轴静态 PASS。同步后生成 Lucene 10.5.1 两项依赖锁；修复 Ruff 的相邻元素遍历要求、运行器解释器定位及 CLI/Spotless 导入排序差异。没有改变校准数据或预先确定的选择目标。
- 聚焦检查后源码提交：`0a6e528af679b3558c9554171d15ecfcb4a49ca1`。
- 独立报告先保存于 `24e814c`；随后把唯一选定阈值原值应用于 `cfda6338ed9c168cd58ef9b7c7004a8db450b741`，再启动原冻结质量门。
- #189 所有冻结资产、模型revision、期望与质量门槛，以及首轮 `issue190-quality-c.json` 均未修改。没有使用冻结结果再选参。

## 聚焦与组件检查

| RunId | 结果 | 说明 |
| --- | --- | --- |
| `issue190-preflight-20260831a` | FAIL | 依赖锁生成成功；Ruff要求用pairwise遍历相邻分数，随后等价修复 |
| `issue190-preflight-20260831b` | FAIL | 13项测试及Ruff通过；本地Pyright未指向项目venv，修正运行入口 |
| `issue190-preflight-20260831c` | FAIL | Agent测试/Ruff/Pyright通过、后端编译通过；独立CLI导入排序与仓库Spotless不一致 |
| `issue190-preflight-20260831d` | PASS | 改用仓库Spotless；Agent13项通过，Pyright0错误，后端Gradle check通过，前端npm check通过（164通过/3既有跳过） |

最后一轮总耗时246.258秒。各轮[原始阶段记录](evidence/issue190-preflight-20260831d.json)按对应RunId保存于同目录；[成功日志摘录](evidence/issue190-preflight-20260831d-excerpt.txt)逐行取自正常运行日志，不是完整stdout。后端测试数量未单独采集；前端结果是Vitest组件测试，不能称为真实浏览器验收。最终完整 `pwsh ./scripts/check.ps1 -Issue 190` 未运行。

预检开始HEAD为 `e71e11512259c3640203d426860b23de466497dd`，过程中生成锁与规范格式，因此记录明确标记工作树变化；其检查后源码归档到 `0a6e528`，不声称提交后再次重跑过所有检查。

## 独立校准：MEASURED，不代表质量达标

RunId `issue190-calibration-20260831a`，受测HEAD `0a6e528`，工作树干净；48题、18片段。数据SHA-256 `d07cbea9ec854a34a702f9fe38eb147ccf6de0ddc619248a8201b77a97bd397a`，来源及选择算法已在运行前记录于[校准方案](issue-190-calibration-plan.md)。选择器仅用32题校准分区，未使用16题留出分区选参。

唯一阈值：`0.6228445617150831`。没有手动取整、候选值替换或看到留出结果后再选择。

| 分区 | 题数 | 有答案接纳率 | 无答案拒绝率 | 有答案必要片段Top-5召回 |
| --- | ---: | ---: | ---: | ---: |
| 校准 | 32 | 81.25% | 93.75% | 100% |
| 留出审计 | 16 | 37.5% | 100% | 100% |

**留出接纳率较差，说明这个单阈值策略在独立主题间已经表现出泛化不足。** 最后一列只评价拒答前的候选，不能替代经过拒答后的质量指标，也不能把报告 `MEASURED` 或配置 `CALIBRATED` 当成质量PASS。

耗时39.581秒，包括本地模型加载及48题计算；环境Windows11、Python3.13.13、Torch2.10.0+cpu、Transformers4.57.6、Safetensors0.7.0。原始报告：[独立48题全部分数与参数](evidence/issue190-calibration-20260831a.json)，SHA-256 `72af95137170aeb2edcabd7f94b952c448a79b3cf6f891e9def013c9074c4eda`。此报告用文件级Git属性保留Windows原始换行字节；没有改变数据或重新运行。

## 原冻结质量：64题完整运行，FAIL

RunId `issue190-quality-20260831d`，受测HEAD `cfda6338ed9c168cd58ef9b7c7004a8db450b741`、base `efbdb8348dc9c12f259c69d9e8c16de5e4d3994c`，工作树干净。语料一致性与真实CPU编码契约PASS，无执行异常，模型仍为固定revision `7999e1d3359715c523056ef9478215996d62a620`。

| 指标 | 首次有效实测 quality-c | 本阶段 | #189冻结要求 |
| --- | ---: | ---: | ---: |
| answered Recall@5 | 2.7778% | 55.5556% | ≥90% |
| answered MRR@5 | 2.7778% | 52.7778% | ≥75% |
| 无答案precision | 25.5319% | 42.8571% | ≥90% |
| 无答案recall | 100% | 100% | ≥85% |
| 错版本Top-5命中率 | 0 | 0 | 0 |
| 越范围Top-5命中率 | 0 | 0 | 0 |
| 未授权Top-5命中率 | 0 | 0 | 0 |

36题有答案请求全部HTTP200且均有全文候选，说明基础中文全文链路确实工作；只有20题有合格结果，16题仍被全局余弦拒答策略清空。12题无答案全部拒答，所以precision为 `12/(12+16)`。三个违规命中率为0，但不替代尚未完成的浏览器与完整门禁。本阶段没有运行消融，不能把指标变化单独归功于中文分词或阈值中的某一项。

报告：[冻结64题原始结果及两路候选](evidence/issue190-quality-20260831d.json)，SHA-256 `f68adbfa172298a0f802a796c1e8d2a2bf24b64d0fce3e543312f2e655bce482`。数据集SHA-256仍为 `d2d5efdae565395c1dc722e14f66077558575152e23c5c8fbea58b7ebcfd2fe5`。评测进程耗时67.046秒（包含模型契约及登录/API请求，不是单次检索延迟）；含镜像准备的阶段总耗时未采集。Linux容器/Python3.13.14/Torch2.10.0+cpu/Transformers4.57.6/Safetensors0.7.0/PostgreSQL18.6/pgvector0.8.6。

## 失败原因与下一步边界

### 本阶段增量静态审查

固定比较 `fe93e100a726708f9ace3215bd522358388250dc...d601264b170f76fe2719979a0293d10a5860945d`，两名独立审查 Agent 只读复核，未运行测试、格式、构建、模型、校准或锁查询。

- **Standards PASS：0项阻塞发现。** 核对唯一参数与预存报告一致、相邻遍历等价、依赖锁及格式、冻结资产和旧报告未改、V41对应本次V40基线，以及失败/版本/耗时/贡献记录的真实性。
- **Spec 静态增量 PASS：0项新增缺陷，规格交付仍未通过。** 校准只用calibration分区；余弦距离升序排名、`1-distance`相似度和最高合法相似度拒答方向正确，没有把RRF当置信度；参数、模型revision和数据哈希均与原报告一致。

两个静态PASS只覆盖上述已提交内容，不取代实际质量FAIL、尚未运行的浏览器验收和最终完整门禁；本文随后仅追加审查记录。

没有新的证据表明本轮失败来自模型文件、语料装载、全文未执行、排名方向或评测运行异常。已有独立留出结果和本次完整报告共同说明：预定的最高余弦单阈值拒答方法未达到目标，不能以“组件检查通过”替代质量达标。

下一步应在独立开发数据和明确契约下决定是否采用不同的简单可回答性方法；这需要新的方案及运行安排，而不是基于本次冻结分数调低阈值。当前不实施、不再选参、不循环跑冻结集，不开放 #168/#169/#170 运行或消费者集成，也不合入/关票。所有运行结束均已单次确认自身释放并通知协调，最后窗口状态为已归还。

本阶段付费模型调用0元；不包含机器、电力或网络成本。数据均为合成或仓库演示知识，无生产规模、线上收益或用户逐行手写代码声明，贡献边界见[面试证据记录](issue-190-contribution-evidence.md)。
