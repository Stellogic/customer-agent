# #169 静态可交付检查点

日期：2026-09-01。分支：`codex/issue-169-customer-sources`。比较基线：`e34b60113a3bbcfe28a4fcd247900127ffbd234a`。

## 本检查点范围

- 客户 Agent 仅提交最小自然语言问题，Spring 固定 `CUSTOMER_PUBLIC` 范围并在检索前后复核工单 generation；#169/#170 共用 Agent 检索适配仍由本票唯一维护。
- 检索回执包含条目、版本、范围和片段位置。最终发布在同一工单事务中复核索引代次、当前版本、完整回执及客户授权，再保存公开回复。
- 同一次 DeepSeek communication 响应判断资料充分性并生成知识回答；知识分支完整缓冲。Spring 业务事实独立保留，冲突时业务事实优先并记录 `KNOWLEDGE_CONFLICT`。
- 客户公开投影只包含来源标题与更新时间。回复正文和知识答案均检查完整回执中的 articleId/chunkId，覆盖 `INSUFFICIENT_INFORMATION` / `CONFLICT` 空引用分支；客户不接收路径、分数、片段或内部正文。
- 客户来源组件覆盖 ready/loading/empty/conflict/error/recovering；断线时清除旧来源，恢复后只回读已接受的公开快照。

## 冻结材料

客户专用数据集 `issue-169-customer-answer-v1` 固定36条有答案、12条无答案，语义分母48；18个公开命题各有两种问法，不把双问法描述为36种独立规则。8项边界单列，不进入48题语义分母：范围、撤权、旧版本、索引过期、片段注入、业务冲突、公开字段及桌面/窄屏断线恢复。

冻结清单绑定数据集、执行协议、输入/输出协议源码、回答解析源码、真实Spring接缝执行器和三篇正常 `CUSTOMER_PUBLIC` 产品文章。清单只为后续真实调用固定输入，不新增产品哈希协议。状态为 **PRE_CALL_STATIC_READY / MODEL_NOT_RUN**；调用前累计账本最后读回289条SETTLED、0条PENDING、620805微元，但事故暂停期间不再次读取或修改外部账本。

## 已有证据与当前边界

事故前曾在脏工作树上得到：Python聚焦132项、Java聚焦140项、前端63项通过；runtime-e 的HTTP/PG 18项、V43事务SQL和Edge 1440/390来源及断线恢复2项通过。这些记录不能证明本静态检查点提交原样通过，也不能替代最终完整门禁。

此后新增或修改的回答执行器、索引过期/注入/冲突边界源码、冻结清单及来源组件局部截图步骤均 **NOT_RUN**。真实48题回答质量、真实自主graph个案、最终完整 `check.ps1 -Issue 169` 均 **NOT_RUN**。未转Ready、未合入、未关票，不报告 `CODE_READY` 或整票PASS。

runtime-f误删baseline资源后，协调明确暂停所有测试、格式/lint/类型检查、构建、Docker、数据库、模型及恢复写入。本检查点只允许源码/测试源码/文档编辑、独立静态双轴审查、提交和推送Draft；事故证据及执行器修复保留在独立提交 `fde0360abbdea463a49030a7a73320440e248ef0`，不混入本业务提交。
