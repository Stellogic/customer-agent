# Issue #190 C-v2完整开发验证：契约通过、语义质量失败

2026-08-31；[Issue #190](https://github.com/Stellogic/customer-agent/issues/190) / [Draft PR203](https://github.com/Stellogic/customer-agent/pull/203)。**唯一一次72题已见合成开发验证已完成：72/72契约合法，既定质量指标FAIL。** 不重试、不调参、不修改方法或数据，不进入留出/#189、产品切换或最终交付。

## 身份、固定边界与结果

- RunId：`issue190-c-v2-development-20260831a`。
- 受测干净HEAD：`07566be1a1f8d0505bd55d012ffbb34c9103352b`；fetch/base：`c19a7ebe8ec31f7ed21048ea75fbfcfd61df1472`。源码与已离线验证、双轴CR通过的 `aa5e03c8c78e0e849b14ad3c7c6fb8987948b9d4` 相同，之间只有文档/证据增量。
- 方法、prompt/schema/config、模型参数与四项阈值沿用[已冻结清单](issue-190-c-v2-offline.md)。完整72项请求清单SHA：`7234a4f5812e976f3e3efc594fc3e2b0760b46b760b0f2a8d403525fbfd5cd91`，实际72次请求的ID、顺序与字节hash全部匹配；每项恰一次，无续跑或拼接旧结果。
- 仅发送原合成问题与已授权硬过滤快照的固定Top5，无真实客户或内部业务资料。数据已见，不是盲测；本次不重新运行数据库过滤或检索，权限/版本正确性沿用原hash固定快照，不能宣称产品过滤门重新通过。

六个契约检查层 `json_syntax / decision_schema / evidence_fields / cross_fields / authorized_chunks / verbatim_quotes` 均为72/72 PASS。原始摘录、文章去重计数及每层状态均在逐题报告内；摘录合法不等于足以回答问题，也不因重复来源增加独立来源数或质量权重。

只有全部72项合法后，脚本一次计算已有指标，结果如下；没有对部分合法样本评分：

| 指标 | 实测 | 固定门槛 | 结果 |
| --- | ---: | ---: | --- |
| answered_recall_at_5 | 0.8055555555555556（29/36） | ≥0.90 | FAIL |
| answered_mrr_at_5 | 0.7037037037037037（25.333333333333332/36） | ≥0.75 | FAIL |
| unanswered_precision | 0.8333333333333334（35/42） | ≥0.90 | FAIL |
| unanswered_recall | 0.9722222222222222（35/36） | ≥0.85 | PASS |

36道可回答题接收29、拒答7；36道不可回答题误接1、正确拒答35。这里无答案precision表示42次拒答中35次确实不可回答。可回答误拒率19.4444%，不可回答误接率2.7778%。指标保持原定义及原始分母，未改题或放宽门槛。

这次明确观察到“输出契约全部合法，但语义质量不达标”。这些汇总不能单独区分候选召回、充分性判断和标注边界的根因，本阶段不据错题设计下一条规则。旧C-v1均为独立失败/不完整阶段，不重标成功，也不以新旧不可比阶段宣称准确率提升。

报告主状态为 `FAIL`，`contract_validation=PASS_72_OF_72`、`semantic_validation=FAIL`，metrics有值。入口按该质量FAIL退出1并抛通用阶段停止异常，所以launch记录 `RuntimeException`；**这不是供应商异常、契约失败或只完成部分题目**。无二次运行。

## Tokens、费用、环境与耗时

| 项目 | 实际记录 |
| --- | ---: |
| 请求 / 合法完成 / 可信usage | 72 / 72 / 72 |
| 输入 / 输出 / 总token | 34034 / 1506 / 35540 |
| 缓存命中输入 / reasoning token | 17920 / 0 |
| 本次已结算新增费用上界 | 115656微元＝0.115656元 |
| 既有 / 全局累计费用上界 | 0.079923元 / 0.195579元 |
| 未结算预留 | 0 |
| Python阶段 / 外层入口耗时 | 78.57019339999533秒 / 82.0019741秒 |

所有响应实返model为 `deepseek-v4-flash`，fingerprint均null；可观察标识未漂移，不宣称云权重不可变。环境为CPython3.13.13、Windows-11-10.0.26200-SP0，使用已有依赖，无模型安装下载。本地开始时间2026-08-31T06:17:35.2620977+08:00，结束06:18:57.2681315+08:00。

运行前重新核实[官方价格](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)与[官方Flash上下文配置](https://huggingface.co/deepseek-ai/DeepSeek-V4-Flash/raw/main/config.json)，UTC核价日为2026-08-30（本地8月31日）。仍按高峰未缓存输入3元/百万、输出9元/百万，输入上界1048576、输出帽256逐请求预留3.148032元，可信usage结算释放差额。`34034×3＋1506×9＝115656`微元；不预支缓存/空闲时段优惠。实际账单、机器成本、峰值内存未采集，费用上界不是实付金额，阶段耗时不是生产吞吐或延迟收益。

## 持久化证据与停止状态

[原始证据索引](evidence/issue190-c-v2-development-20260831a/index.json)保存原报告、完整共享账本、launch元数据/日志、启动脚本及仅汇总既有输出的summary；未补造供应商原始文本。原报告保留全部72题判定、引用、请求hash、usage及分层检查；summary没有重跑质量计算或修改判定。

最终账本SHA：`0800a19d7111b2838d7131734a62cdf6a64be48dcac1fc8c9b44d3435b9646f0`。静态归档比较确认原三个phase与前50次调用逐项不变；新phase终结为FAIL。全局122次API调用不等于122个独立质量样本，本次独立记录的开发规模仍是72题。

运行finally释放自有锁，单次宿主回读FREE并成功通知LOCK_RELEASED；阶段窗口已归还，之后仅静态归档，无再次测试/调用/评测。既有#189资产和门槛、旧失败/STOPPED、#168/#169/#170阻塞保持。实验由Codex按授权执行，不表示用户逐行手写、生产规模或线上收益。后续方向由协调决策，本次不自行继续。
