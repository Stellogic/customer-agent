# [RAG] 交付本地 BGE、pgvector 与预冻结质量门

> 父规格：[#149](../../specs/issue-149.md)
> 来源：[https://github.com/Stellogic/customer-agent/issues/167](https://github.com/Stellogic/customer-agent/issues/167)
> Issue 状态：OPEN
> 最后更新时间：08/31/2026 15:19:37
> 同步日期：2026-08-31
> 说明：GitHub Issue 是事实源；本文件为回读正文镜像。

## Parent

Part of #149

## 汇总职责与关闭条件

本票已拆为 #189、#190，只保留范围与验收汇总职责，不再作为独立实施票领取或重复创建实现 PR。原冻结资产及历史结果完整保留；当前验收归属按 #149 的 rag-layered-v2 修订。

| 原验收条目（按下文顺序） | 承接子票与证据 |
| --- | --- |
| 1–2：预冻结评测清单、覆盖和内容哈希 | #189 |
| 3：固定阈值及实际通过 | #189 保留原冻结资产；#190 提供 rag-layered-v2 检索层通过证据；回答拒答门由 #169/#170 承接，不阻塞本汇总票关闭 |
| 4：模型与 Embedding 协议、文件清单 | #189 冻结协议；#190 准备并校验模型文件、实现协议 |
| 5–10：离线模型、PyTorch、硬过滤/RRF、精确检索、真实页面及失败行为 | #190 |

关闭前必须确认 #189、#190 均正式交付，并逐项核对上述证据。#190 必须通过 rag-layered-v2 检索层质量实测及最终本地完整门禁、PR 合入 main 并关闭 Issue；静态双审 PASS、CODE_READY_NO_TESTS 或 GATE_READY 均不能替代。
下游 #168、#169、#170 直接依赖 #190 的合格交付，不因本汇总票的行政收口延迟而继续等待。

## What to build

在本地 CPU 上使用固定 revision 的 `BAAI/bge-small-zh-v1.5` 生成知识向量，以 PostgreSQL 全文检索、pgvector 与 RRF 形成可工作的混合检索纵切片，并在任何客户或客服 Agent 集成消费前通过 rag-layered-v2 检索层质量门。检索只提供授权候选，不用分数决定能否回答；由下游同一次 DeepSeek 回答调用判断资料充分性。

## Acceptance criteria

- [ ] 在编写或调优 Embedding、pgvector 查询和 RRF 实现之前，先冻结 `rag-eval-v1` 评测清单、期望结果和内容哈希；本票后续不得根据检索结果修改该集合，纠错必须单独审查并保留原哈希。
- [ ] `rag-eval-v1` 固定为至少 60 条查询：36 条有答案查询、12 条无答案查询、12 条错误版本或不适用范围查询；36 条有答案查询覆盖每个当前发布知识条目至少 3 条，并至少包含 12 条口语/省略表达和 8 条错别字、简称或同义表达。
- [ ] 按 #149 的 rag-layered-v2，检索层通过有答案 `Recall@5 >= 0.90`、`MRR@5 >= 0.75`，错误版本、不适用范围和越权条目 Top-5 命中率均为 `0%`；无答案样本仍执行并记录，但不以返回候选判拒答。原 precision `>= 0.90`、recall `>= 0.85` 数值目标转由 #169/#170 实际回答层验收，旧协议和旧结果不变。
- [ ] 评测使用固定模型完整 revision、查询指令、pooling、L2 normalization、最大长度、截断策略和 512 维输出；模型文件清单与校验值可审计。
- [ ] 模型权重不提交 Git，准备阶段下载后运行阶段只从本地路径离线加载；文件缺失或校验失败时 fail closed。
- [ ] 先以 CPU PyTorch/Safetensors 建立正确性基线，不要求 ONNX 才能完成本票。
- [ ] PostgreSQL 全文候选和 pgvector 语义候选分别可观测，RRF 在权限、发布状态、版本和适用范围硬过滤后融合。
- [ ] 当前语料规模优先使用精确向量搜索；没有规模证据时不提前固定 HNSW 或 IVFFlat。
- [ ] 真实内部检索页面展示带条目标识、版本和片段位置的结果，并覆盖桌面、窄屏、loading、empty、error 与索引过期状态。
- [ ] 模型、索引或数据库扩展不可用时停止知识结果，不以无来源内容填补。

## Blocked by

- #150
- #166
