# 客服工单与补偿

本文定义项目在客服调查与补偿领域使用的统一语言，不包含实现细节。

## 统一语言

**合成业务数据（Synthetic business data）**：
为演示与测试而虚构的客户、订单、物流和支付事实，不对应任何真实人员或交易。合成业务数据仍按本项目的敏感数据边界处理。
_避免使用_：匿名化生产数据、真实测试数据、脱敏样本

**客户（Customer）**：
提出需要调查和处理的问题、并作为客服工单服务对象的人员。MVP 中，客户与内部工作人员是不同的参与者。
_避免使用_：普通用户、客户账号、工单创建人

**内部工作人员（Internal staff member）**：
代表组织承担客服或审批职责的人员。同一名内部工作人员可以同时承担客服与审批人角色，但一种角色不会自动赋予另一种角色的业务权力。
_避免使用_：管理员、单一角色账号、客户

**客服（Support agent）**：
负责受理和调查客服工单、与客户沟通并处理建议的内部职责。同一内部工作人员可以兼具客服与审批人职责，但客服职责本身不授予审批权，也不扩大工单分配、审批租约、提案版本或禁止自审约束。
_避免使用_：坐席、人工 Agent、审批人

**负责客服（Assigned support agent）**：
当前对某一客服工单的调查和处理负责的客服。MVP 中，一个客服工单同一时刻至多有一名负责客服；页面切换、浏览器刷新、重新登录或实时连接中断不结束该责任。
_避免使用_：工单所有者、审批人、最后修改人

**历史负责客服（Previous assigned support agent）**：
最近一次在 `HUMAN` 处理模式下拥有有效工单分配的客服。该历史事实只能在其仍符合当前工单处理资格时作为软路由信号，不构成预留、归属或授权。
_避免使用_：当前负责客服、默认负责人、长期工单所有者

**客服共享队列（Shared support queue）**：
供客服发现待领取或已升级客服工单的工作集合。进入共享队列不代表任何客服已获得工单详情的访问权。
_避免使用_：公开工单列表、全员工单库

**受理协助请求（Intake assistance request）**：
受理对话因 Agent 或工具失败、客户要求人工或超出自动识别能力而进入客服受理队列的工作请求。它允许客服协助确认订单和拟建工单集，但不是客服工单，不启动工单服务级别目标，也不授予调查完整订单、形成补偿提案或执行工单动作的权力。
_避免使用_：客服工单、转人工工单、其他问题工单

**公开沟通（Public conversation）**：
客户与当前公开响应责任方围绕一张客服工单交换的、对客户可见的消息记录。一段公开沟通只处理一个订单的一项问题；客户可以同时拥有多张客服工单，并分别在各自的公开沟通中持续对话。客户在关闭等待期内就同一问题发出的公开回复可触发重开。
_避免使用_：内部记录、客服备注、Agent 跟踪

**受理对话（Intake conversation）**：
正式创建客服工单之前，客户使用自然语言描述诉求、Agent 识别订单候选和一个或多个问题并请求客户统一确认的可恢复对话。一次受理对话只处理一个订单；若识别到多个订单，完成当前订单后可用保留的描述继续下一次受理。受理对话不是客服工单，不启动工单服务级别目标，也不授予读取未确认订单详情或产生业务副作用的权力；刷新、重新登录或实时连接中断不会丢失受理进度，且系统不得据此自动创建未经确认的工单。未完成受理保留七个自然日，期满后归档；恢复归档内容时必须重新确认可能已经变化的订单事实。
_避免使用_：公开沟通、临时工单、问题类型选择器

**共同受理记录（Shared intake record）**：
客户确认拟建工单集时形成的不可变记录，包含原始诉求、已确认订单、Agent 识别的问题以及客户确认。由同一次确认创建的每张客服工单都引用同一份共同受理记录，后续各工单的公开沟通仍彼此独立。
_避免使用_：公开沟通副本、父工单、可编辑摘要

**内部记录（Internal note）**：
为调查和处理客服工单而保留的、不对客户公开的工作记录。内部记录不会因补偿提案进入审批而整体向审批人开放。
_避免使用_：客户回复、审批证据快照、公开评论

**调查事实（Investigation fact）**：
调查客服工单时从受控业务来源取得、并以明确结构记录的订单、物流、支付或政策事实。调查事实不等同于原始工具响应或 Agent 的自由形式推理。
_避免使用_：工具日志、模型解释、原始响应

**事实充分（Evidence sufficiency）**：
Agent 根据当前问题自主决定调查路径，并提出支撑结论的结构化证据集合及各证据与结论的适用关系。Spring 不预先编排完整调查流程，只校验证据权限、来源、时效、已知冲突，以及当前风险场景要求的不可缺失事实。模型主观置信度不能单独构成事实充分；Agent 可以基于通过校验的证据自主形成低风险结论。
_避免使用_：模型置信度、固定调查流程、证据数量阈值

**延迟事实（Delay fact）**：
物流延迟的不可变权威调查事实，以秒数作为精确值，并允许小时数作为由秒数确定性导出的展示值。两种表示同时持久化时必须满足数据库一致性约束；任一决定提案版本的表示都必须进入提案内容摘要。审批证据快照引用其补偿提案版本的延迟事实，不得保存可独立漂移的副本。
_避免使用_：Agent 估算时长、互不相关的小时数和秒数、仅用于展示的提案字段

**证据引用（Evidence reference）**：
将调查事实、审批证据快照或执行记录与其依据关联的稳定引用。它指向可访问的业务证据，而不是将原始 payload 复制到每个记录中。
_避免使用_：原始 payload、trace 链接、日志文本

**决策理由码（Decision reason code）**：
表明 Agent 或确定性业务规则为何作出路由、处理或建议的受控代码。它与相关调查事实和证据引用一起构成可解释记录，不保存自由形式模型推理。
_避免使用_：思维链、模型独白、自由文本理由

**审计事件（Audit event）**：
追加式记录某一参与者在特定时间对某一业务对象所作动作及其责任链的最小事实。审计事件通过引用关联业务记录，不复制完整工单、沟通或工具结果。
_避免使用_：业务数据副本、原始日志、全量 trace

**业务时间线（Business timeline）**：
面向当前负责客服的、由客服工单事实与相关审计事件组成的授权投影。它展示接手和处理所必需的责任链与业务变化，不直接暴露审计 payload、trace 或原始工具结果。
_避免使用_：审计日志、Agent trace、全局活动流

**审批视图（Approval view）**：
面向当前承担特定补偿提案审批职责的审批人的、以该提案版本为范围的授权投影。审批决定提交、提案失效、释放或重新分配后，该视图的访问权结束；它不自动授予审批人完整客服工单、执行阶段或内部审计数据的持续访问权。
_避免使用_：工单详情、审计查询、客服工作台

**审批租约（Approval lease）**：
审批人在有限时间内对特定补偿提案版本作出决定的临时排他授权。每次租约是独立的授权轮次；过期、释放、重新分配或审批结束后，旧租约不再授予审批视图或决策权。
_避免使用_：提案归属、长期分配、审批状态

**审批人（Approver）**：
对有权访问的补偿提案版本作出人工审批决定的内部职责。审批人与客服是不同职责容量；同一内部工作人员可以兼具两者，但两类资源权限保持独立，且不能绕过禁止自审约束。审批人与客户及 Agent 仍是不同参与者。
_避免使用_：管理员、审计员、Agent 审批

**审批证据快照（Approval evidence snapshot）**：
随特定补偿提案版本提交、供审批人作出决定的必要业务事实与调查依据集合。它不等同于完整客服工单、全部内部记录或原始工具输出。
_避免使用_：完整工单快照、Agent 跟踪、原始日志

**Agent**：
使用独立受限机器身份、在受理对话中识别待确认诉求，并在单一客服工单范围内自主开展多轮公开沟通和事实调查的非人工参与者。Agent 可以在受支持的低风险场景中将满足自动解决条件的工单推进为已解决，也可以生成补偿提案，但不具有审批或执行补偿的权力。
_避免使用_：虚拟客服、自动审批人、系统用户

**处理模式（Handling mode）**：
表示当前由 Agent 还是负责客服承担客服工单调查与公开响应责任的业务属性。处理模式取值为 `AGENT` 或 `HUMAN`，同一时刻只有当前模式对应的责任方可以直接向客户公开回复；在 `HUMAN` 模式下，Agent 只能提供不产生公开沟通或业务副作用的内部辅助。处理模式与客服工单的生命周期状态分开。
_避免使用_：工单状态、工单类型、Agent 运行状态

**Agent 处理代次（Agent processing generation）**：
客服工单在 `AGENT` 处理模式下，标识当前仍有权对该工单产生业务副作用的 Agent 处理轮次。客户在 Agent 生成期间追加的新公开消息会使基于旧对话的未完成输出失去公开响应权；旧代次的迟到结果不能修改业务状态或向客户发送消息。
_避免使用_：LangGraph run、`thread_id`、checkpoint 版本

**对话工作摘要（Conversation working summary）**：
为控制 Agent 上下文规模而从完整公开沟通、调查事实和知识引用中形成的持久化工作材料。完整消息仍是权威记录；工作摘要不能覆盖原始消息、创造业务事实或替代证据，并可在发现冲突时重建。
_避免使用_：公开沟通、模型记忆、工单事实

**转人工（Human handoff）**：
客服工单因事实冲突、工具失败、超出支持场景或客户要求人工而从 `AGENT` 处理模式转为 `HUMAN` 处理模式的业务事件。转人工后，Agent 不再发送公开沟通，并留下调查摘要供客服接手。
_避免使用_：工单升级、重开、Agent 失败

**客户人工偏好（Customer human preference）**：
客户明确要求由人工继续处理客服工单的当前意愿。该偏好未被客户明确解除时，工单不得恢复面向客户的自主 Agent 处理。
_避免使用_：客户切换处理模式、永久退订 Agent、客服备注

**交回 Agent（Agent handback）**：
当前负责客服在转人工原因的可验证交回条件已满足、且客户人工偏好不阻止时，将工单从 `HUMAN` 处理模式重新授权给 Agent 的业务事件。交回会创建新的 Agent 处理代次，不恢复旧执行。
_避免使用_：恢复旧 Agent、取消转人工、Agent 自主接管

**Agent 辅助（Agent assist）**：
工单处于 `HUMAN` 处理模式时，由当前负责客服发起的工单总结、知识检索、政策查询或回复草稿辅助。辅助输出只是供客服审阅的建议，不能直接发送客户消息或产生业务副作用。
_避免使用_：自主 Agent、自动回复、处理模式切换

**知识条目（Knowledge article）**：
仓库内供 Agent 与客服辅助共同检索的版本化中文 Markdown 客服说明、常见问题或操作规范，具有稳定标识、标题、版本、更新时间和适用范围。知识条目用于解释一般规则和提出建议，不构成订单、物流或支付事实，也不构成补偿资格、金额、审批或执行授权。
_避免使用_：补偿政策、模型记忆、无来源答案

**知识分段（Knowledge chunk）**：
知识条目为检索而形成的稳定、可追溯片段，携带条目标识、条目版本、适用范围、源文件和片段位置。知识分段用于召回与引用，不成为脱离原知识条目的独立业务事实。
_避免使用_：知识条目副本、自由摘要、业务事实

**混合知识检索（Hybrid knowledge retrieval）**：
在权限、发布状态、版本和适用范围过滤边界内，组合词法候选与本地 Embedding 语义候选并融合排序的知识检索。Embedding 只产生文本向量，不承担调查规划、业务判断或客户回复；DeepSeek 仍是唯一负责推理和回复的对话模型。
_避免使用_：第二个对话模型、订单事实查询、无范围向量搜索

**知识引用（Knowledge citation）**：
将公开回答或客服辅助建议关联到具体知识条目及其版本的可验证引用。客户只接收友好的来源标题和更新时间；获授权的客服可以接收条目版本、内部标识和引用片段。知识引用说明答案依据，不替代业务事实的证据引用。
_避免使用_：证据引用、搜索摘要、模型理由

**知识冲突（Knowledge conflict）**：
知识条目的一般说明与当前权威业务事实不一致，或多个适用知识版本彼此矛盾的可维护异常。当前订单、物流或支付的权威事实优先；Agent 应向客户说明个案状态并记录冲突，不得用一般知识覆盖当前业务事实。
_避免使用_：事实冲突、模型幻觉、自动知识更新

**公开处理进度（Public processing progress）**：
向客户展示 Agent 当前正在理解问题、核对业务事实、查询规则、整理回复或等待外部信息的受控状态。公开处理进度不包含模型内部推理、提示词、原始工具响应或技术异常详情。
_避免使用_：思维链、Agent 跟踪、工具日志

**有依据的自然回复（Grounded natural response）**：
Agent 使用自然语言组织、但其中业务事实来自授权调查事实、规则说明关联知识引用、金额与承诺来自 Spring 权威结构化结果的客户回复。回复发送前必须检查越权承诺、无依据事实和敏感信息；无法安全修正时不得发送，并应转人工。
_避免使用_：固定答案模板、模型自由承诺、原始工具响应

**补偿（Compensation）**：
针对受支持客服工单提供的补救方式。MVP 中仅包括优惠券或账户余额等非现金权益，以及退回原支付方式的模拟部分退款。
_避免使用_：赔付、打款、善意付款

**非现金权益（Non-cash benefit）**：
以优惠券或账户余额形式发放、而非退回原支付方式的补偿。
_避免使用_：退款、现金补偿

**部分退款（Partial refund）**：
退回原支付方式，且不超过剩余可退款金额的补偿。MVP 只模拟该动作，不会产生真实资金变动。
_避免使用_：转账、打款、全额冲销

**补偿金额（Compensation amount）**：
补偿提案与补偿执行使用的人民币金额，精确到分。模拟部分退款按适用补偿政策从实付金额计算、封顶并四舍五入，Agent 建议的金额不构成最终补偿金额。
_避免使用_：模型金额、浮点金额、真实退款金额

**可提案补偿金额（Proposable compensation amount）**：
经过确定性补偿政策计算、封顶并按分舍入后仍严格大于 `0.00 CNY` 的补偿金额。已支付状态不代表实付金额为正；非正实付金额或舍入为零的部分退款不可形成补偿提案。
_避免使用_：零金额补偿、数据库拒绝后的候选金额、Agent 建议金额

**补偿提案（Compensation proposal）**：
Agent 或负责客服针对受支持客服工单，从 Spring 允许的补偿方案中形成的具体建议，不代表已经获得执行授权。客户可以看到安全的建议类型、金额和“待审批”状态，但不得被告知补偿已经获得或执行；内部工作人员可以看到授权范围内的完整提案、证据和审批状态。普通补偿提案不得通过任意金额或方式覆盖权威补偿政策；超出标准方案的诉求属于独立的例外补偿申请。
_避免使用_：已批准补偿、已执行补偿

**例外补偿申请（Exceptional compensation request）**：
负责客服认为标准补偿方案不足时提出的高风险人工审查请求。例外补偿申请不属于普通补偿提案，也不授予 Agent、客服或审批人绕过组织授权边界的权力。
_避免使用_：普通补偿提案、任意改价、Agent 补偿

**审批（Approval）**：
具有审批人角色的人员对特定补偿提案版本作出的最终执行授权。MVP 中所有补偿都必须先获得审批，且内部工作人员不得审批自己以客服身份创建、修改或提交过的补偿提案版本；审批成功后该执行授权不得由业务角色撤销，但审批仍不代表补偿已经执行成功。
_避免使用_：Agent 决定、执行结果

**提案审查决定（Proposal decision）**：
审批人对特定补偿提案版本作出的最终批准或驳回事实。批准形成执行授权；驳回不形成执行授权，并要求转人工继续处理。
_避免使用_：提案状态、审批租约、执行结果

**补偿政策（Compensation policy）**：
用于判断补偿资格、补偿方式和额度的版本化业务规则集合。
_避免使用_：Agent 判断、提示词规则

**补偿提案版本（Compensation proposal revision）**：
补偿提案在某次提交审批时形成的不可变快照；关键内容变化会产生新版本，而不是修改原版本。派生版本继承此前版本中以客服身份创建、修改或提交内容的内部工作人员集合，只有不继承旧内容而独立创建的新补偿提案才重新建立该集合。
_避免使用_：可编辑审批单、覆盖原提案

**提案版本客服参与者（Proposal revision support participant）**：
曾以客服职责创建、修改或提交某一补偿提案版本内容的内部工作人员；派生版本继承此前参与者，单纯查看或负责客服工单不构成参与。
_避免使用_：负责客服、工单查看者、提案审批人

**权威总可补偿额度（Total available compensation amount）**：
某一订单在补偿政策以及该订单全部客服工单的既有补偿事实约束下可用于补偿的权威总额，不扣除当前有效额度预占。审批证据保存该总额，并在作出决定时与权威事实独立核对；任何单张工单都不能绕过订单范围的汇总约束。
_避免使用_：剩余可补偿额度、Agent 建议金额、原订单金额

**有效额度预占总额（Active reservation amount）**：
某一订单当前所有有效额度预占的金额合计。审批证据保存该合计，并在作出决定时独立核对，避免将已经扣除的预占再次扣减。
_避免使用_：已退款金额、已补偿金额、单笔额度预占

**剩余可补偿额度（Remaining available compensation amount）**：
权威总可补偿额度减去有效额度预占总额后的净额度，用于补偿提案的资格判断；不得把它当作权威总额再次扣除额度预占。
_避免使用_：权威总可补偿额度、Agent 建议金额

**额度预占（Compensation reservation）**：
已批准但尚未确认执行结果的补偿对权威总可补偿额度形成的临时占用；所有有效预占计入有效额度预占总额，并相应降低剩余可补偿额度。
_避免使用_：已退款、已补偿

**补偿执行（Compensation execution）**：
依据已批准的补偿提案版本尝试产生一次补偿结果的业务记录，与审批是不同事实。
_避免使用_：审批、Agent 工具调用

**补偿执行器（Compensation executor）**：
依据有效审批、不可变补偿提案版本与额度预占异步推进补偿执行的受限机器参与者。它只能操作补偿执行自身的受控状态与追加式尝试记录，不能修改提案、审批、金额、对象、方式或审批证据。
_避免使用_：人工执行员、Agent 补偿工具、审批人执行

**补偿执行尝试（Compensation execution attempt）**：
补偿执行器为推进某一补偿执行而进行的单次执行或对账尝试的追加式记录。补偿执行表达当前业务事实，执行尝试表达系统为达到该事实做过什么。
_避免使用_：补偿执行、审计事件、可观测 trace

**待对账（Pending reconciliation）**：
补偿执行因响应丢失或结果不明确而需要先确认是否已经产生补偿的状态。待对账不是失败，不得通过普通重试再次创建补偿。
_避免使用_：执行失败、重新补偿

**客服工单（Support ticket）**：
客户就一个订单的一项需要调查和处理的问题形成的工作记录。客户可以同时拥有多张处于不同处理阶段的客服工单；一张客服工单在任一时刻只处于一个生命周期状态。
_避免使用_：案件、任务单

**拟建工单集（Proposed ticket set）**：
Agent 从一次受理对话中识别出的、属于同一已确认订单的一个或多个独立问题。Agent 必须以客户可理解的方式列出拟建工单集并取得一次统一确认；低置信度的问题必须先以自然语言澄清。确认成功后，每个新问题分别形成一张客服工单，创建过程必须原子完成，不得只成功一部分。
_避免使用_：已创建工单、合并工单、问题标签集合

**订单工单组（Order ticket group）**：
客户或内部工作人员界面中，按同一订单聚合展示多张客服工单的视图分组。订单工单组方便发现和导航，不合并各工单的状态、公开沟通、负责客服、领取行为、处理模式、补偿或审批边界。
_避免使用_：父工单、订单案件、共享工单状态

**同订单工单摘要（Sibling ticket summary）**：
为避免同一订单的多个 Agent 重复处理或产生矛盾结论而提供的最小结构化投影，只包含其他工单的问题类型、当前状态、待办动作和补偿流程存在性。它不包含其他工单的完整公开沟通或自由形式工作摘要，也不授予跨工单回复或修改权限。
_避免使用_：完整关联工单、共享对话、跨工单处理权

**问题类型（Issue kind）**：
Agent 根据客户自然语言描述提出、并由 Spring 记录和校验的受控业务问题类别。客户不需要手动选择问题类型；第一阶段区分物流异常、支付与退款、订单操作与规则以及其他问题。同一订单可以产生不同问题类型，不同类型的客户回复不属于原客服工单的同一问题。创建后发现分类错误但仍是同一诉求时可以留下审计记录后修正；发现独立的新问题时必须再次取得客户确认并创建新工单。
_避免使用_：订单类型、工单状态、回复意图

**订单候选（Order candidate）**：
Agent 根据客户自然语言及 Spring 返回的当前客户可见订单摘要提出、等待客户确认和 Spring 权属校验的可能订单。客户无需预先使用订单选择器；候选订单卡片只用于帮助确认。订单候选不是已确认订单，不授予读取订单详情、形成补偿提案或产生订单副作用的权力。
_避免使用_：已确认订单、订单匹配结果、猜测订单

**重复问题匹配（Duplicate issue match）**：
拟建问题与同一订单下尚未关闭的既有客服工单被判断为可能属于同一问题的结果。Agent 必须向客户确认是否继续既有工单，不能自动合并；客户确认后不为该问题重复创建工单，同一拟建工单集中的其他新问题仍可创建。
_避免使用_：重复订单、相似文本、自动合并工单

**低风险场景（Low-risk scenario）**：
Spring 版本化白名单中允许 Agent 基于事实充分结论自动解决的场景。第一阶段仅包括不存在丢失或签收争议及待办动作的权威订单或物流状态解释、纯规则或使用方法说明，以及已经完成且无争议的非补偿性信息核对。签收未收到、疑似丢件、重复扣款、退款异常、地址修改、取消订单、任何补偿、事实冲突和等待外部信息均不属于低风险场景。
_避免使用_：问题类型、模型风险评分、默认安全场景

**自动解决候选（Auto-resolution candidate）**：
Agent 的完整回复已成功持久化，且工单同时满足低风险、事实充分、已给出明确结论、无待办动作和无待审批补偿条件后形成的延迟解决资格。资格及其期限由 Spring 持久化；页面刷新、重新登录、实时连接中断或服务重启不重置期限。界面应告知客户五分钟后将自动解决，并提供“仍需帮助”快捷操作；期间客户任何新回复都会取消该资格。期限到达时必须根据最新工单版本重新校验全部条件，仍满足时工单才进入已解决；已被 Spring 接受的新回复优先阻止自动解决。
_避免使用_：已解决、关闭等待期、固定倒计时状态

**新建（New）**：
客服工单已经创建、但尚未产生首次响应的生命周期状态。
_避免使用_：未开始、未接单

**调查中（Investigating）**：
客服工单正在由 Agent 或客服调查处理的生命周期状态。
_避免使用_：处理中、执行中

**等待客户（Waiting for customer）**：
客服工单因缺少必须由客户提供的信息而暂时无法继续调查的生命周期状态；处于该状态的时间不计入解决时长。
_避免使用_：挂起、暂停

**客户澄清请求（Customer clarification request）**：
Agent 为完成当前调查而向客户提出的、明确指出所缺业务信息的待答复请求。客户对当前有效请求的回复属于同一调查；无关的新诉求、客户人工偏好或对失效请求的回复不属于客户澄清。
_避免使用_：新业务输入、审批请求、转人工

**等待外部信息（Waiting for external information）**：
客服工单因等待物流等外部事实而暂时无法继续调查的生命周期状态；处于该状态的时间仍计入解决时长。
_避免使用_：等待客户、系统异常

**已解决（Resolved）**：
客服工单已经向客户给出明确处理结论，且当前需要执行的已授权动作已经完成，但仍处于允许重开阶段的生命周期状态。Agent 只能在自动解决候选经过五分钟且重新校验仍成立后自动进入该状态；负责客服可以依照其人工处理权限解决工单。进入该状态后，解决时长停止累计。
_避免使用_：已关闭、已完成

**已关闭（Closed）**：
客服工单在已解决状态连续经过七十二个自然小时且客户未就同一问题提出异议后进入的终态。已关闭工单不可直接重开；后续回复或新问题形成关联的新客服工单。
_避免使用_：已解决、归档

**重开（Reopen）**：
客户在关闭等待期内就同一问题提出异议，使已解决工单回到调查中的生命周期转换。重开延续原解决时长和剩余预算，不重新计算首次响应。
_避免使用_：新建工单、重置工单

**关闭等待期（Closure waiting period）**：
客服工单进入已解决后等待客户异议的七十二个自然小时；该时段不计入解决时长，期满后工单自动关闭。
_避免使用_：解决时限、SLA 宽限期

**首次响应（First response）**：
客服工单创建后，首次向客户确认已受理或提供实质处理信息的时间事件，不是生命周期状态；其十五分钟时限不因任何等待状态暂停。
_避免使用_：首次处理、首次状态变更

**服务级别目标（Service-level objective, SLO）**：
客服工单从创建起应在十五分钟内首次响应、并在累计二十四小时内解决的时间目标；MVP 按连续自然时间计算，不区分工作日、班次或优先级。
_避免使用_：预计完成时间、倒计时状态

**解决时长（Resolution time）**：
客服工单从创建到进入已解决状态所累计的、受服务级别目标约束的时间。
_避免使用_：关闭时长、处理次数

**SLA 预警（SLA warning）**：
客服工单已消耗某项服务级别目标百分之八十的风险事实。预警不改变客服工单的生命周期状态，也不授予任何补偿权限。
_避免使用_：SLA 违约、自动升级状态

**SLA 违约（SLA breach）**：
客服工单已消耗某项服务级别目标百分之百且未完成该目标所形成的不可撤销事实。后续完成目标不会抹除已经发生的违约。
_避免使用_：即将超时、当前超时状态

**升级（Escalation）**：
因 SLA 预警或违约而提高人工关注级别的处置；预警时通知当前负责客服，违约时还需纳入客服共享升级队列。升级不改变生命周期状态，不自动解决工单，也不自动批准补偿。
_避免使用_：状态流转、自动审批
